
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_rawContent:String = "socket"
fileprivate let str_viewOfTitle:[Character] = ["_","A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_centerData:[UInt8] = [0x2c,0x28,0x1c,0x24,0x1e,0x2d,0x38,0x2f,0x22,0x1d,0x1e,0x28,0x1c,0x21,0x1a,0x2d,0x38,0x2b,0x1e,0x2a,0x2e,0x1e,0x2c,0x2d,0x1c,0x1a,0x25,0x25,0x38,0x27,0x28,0x2d,0x22,0x1f,0x22,0x1c,0x1a,0x2d,0x22,0x28,0x27]

fileprivate func nonsolidColour(title num: UInt8) -> UInt8 {
    let value = Int(num) + 39
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_honeyName:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let str_tableName:String = "tip page ifisCal"

/*: "isTalking" :*/
fileprivate let str_ofData:String = "self conversationisTalkin"
fileprivate let str_messageText:[Character] = ["g"]

/*: "SOCKET 连接成功." :*/
fileprivate let str_cellValue:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","\u{63a5}","成","功"]
fileprivate let str_fromName:[Character] = ["."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_modelData:[Character] = ["S","O","C","K","E","T"]
fileprivate let str_requestTitle:String = " 连content失败,"

/*: ." :*/
fileprivate let str_arrayName:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let str_cellName:String = "SOCequal"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_mainTitle:String = "次, 切"
fileprivate let str_addToText:String = "cket域名title self"
fileprivate let str_dayTitle:String = "重新尝试，model址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_leadingValue:[Character] = ["S","O","C","K","E","T"," ","已","关","\u{95ed}",","," ","c","o","d"]
fileprivate let str_withName:String = "e:top view insert self size"

/*: , reason: :*/
fileprivate let str_logDetailValue:String = ", resection view make model"

/*: "ws:// :*/
fileprivate let str_valueEventText:String = "ws://self direction array limit"

/*: /?sid= :*/
fileprivate let str_executeData:String = "/?sid=lab file moment"

/*: &uid= :*/
fileprivate let str_layerData:String = "&uid=self level frame guard index"

/*: &platform=iphone&version= :*/
fileprivate let str_picContent:[Character] = ["&","p","l","a","t"]
fileprivate let str_managerButtonNameData:String = "form=iempty var view normal retain"
fileprivate let str_removeName:String = "pimagen"
fileprivate let str_pathData:String = "gift tosion="

/*: &packageId= :*/
fileprivate let str_shareProductData:[Character] = ["&","p","a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let str_eventLabName:String = "1v1serof info"

/*: .com:7766" :*/
fileprivate let str_productValue:String = "last"
fileprivate let str_addShowText:String = "com:try let"

/*: "43.129.185.194:7767" :*/
fileprivate let str_startData:String = "4name.129"
fileprivate let str_managerName:[Character] = [":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_effectData:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "cmd" :*/
fileprivate let str_valueTitle:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let str_beautifulValue:String = "userng"

/*: "callHeartbeat" :*/
fileprivate let str_textData:String = "error to model recommend playercallH"
fileprivate let str_makeLineName:[Character] = ["e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let str_serviceText:String = "rsuspendcisuspendv"

/*: "liveHeartBeat" :*/
fileprivate let str_dropName:[Character] = ["l","i","v","e","H","e","a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let str_labelLayerIfValue:String = "\u{0001f517} 关\u{95ed} s"

/*: "The user logs out" :*/
fileprivate let str_equalToName:[Character] = ["T","h","e"," ","u","s","e","r"," ","l","o","g","s"]
fileprivate let str_canData:String = " outvar let"

/*: "SOCKET  :*/
fileprivate let str_scaleTitle:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let str_versionTheText:[Character] = ["\u{91cd}","连","."," ","重","\u{8bd5}","\u{6b21}","数","："]

/*: , 地址： :*/
fileprivate let str_equalData:String = ", confirm址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_screenNumberervalValue:String = "SOCKnormal"
fileprivate let str_upPlayerData:String = "\u{6b63}常 不\u{9700}"
fileprivate let str_hiddenAgeData:String = "."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_centerTitle:String = "SOCKEview"
fileprivate let str_giftData:String = "\u{4e0d}\u{9700}要重新连接."

/*: "&resume=1" :*/
fileprivate let str_onData:[Character] = ["&","r","e","s","u","m"]
fileprivate let str_managerText:String = "path message self returne=1"

/*: "&lang= :*/
fileprivate let str_sharedData:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_photoValue:[Character] = ["S","O","C","K","E","T"," ","开","始"]
fileprivate let str_colorContent:[Character] = ["连","接"]
fileprivate let str_meEnterValue:[Character] = [","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let str_connectionValue:[Character] = ["o","n","N","e","t","D","i","s"]
fileprivate let str_scaleDownData:[Character] = ["a","b","l","e","d"]

/*: "errno" :*/
fileprivate let str_barName:String = "ERRNO"

/*: "data" :*/
fileprivate let str_errorValue:String = "dloada"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_lengthTableEqualValue:String = "SsucceedKE"
fileprivate let str_leadingData:String = "m"
fileprivate let str_backgroundData:[Character] = ["m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_indexValue:[UInt8] = [0x2b,0x27,0x1b,0x23,0x1d,0x2c,0xf8,0x4c,0x4a,0x4c,0x3b,0xbc,0x97,0x75,0xbe,0x8c,0x93,0x3b,0x45,0x3c,0x12,0xf8,0x3b,0x39,0x44,0x44,0x20,0x3d,0x39,0x4a,0x4c,0x3a,0x3d,0x39,0x4c,0x6]

fileprivate func extraAttention(lab num: UInt8) -> UInt8 {
    let value = Int(num) - 216
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_pairRegularData:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let str_textColorName:String = "onPfar view for"

/*: "msg" :*/
fileprivate let str_labelContent:String = "tempsg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_modelPageName:String = "🔗 处\u{7406}消"
fileprivate let str_normalMainData:String = "let let return service letName:"

/*: , errNo: :*/
fileprivate let str_voiceEqualNameTitle:String = ", errNany right"
fileprivate let str_buttonViewTitle:String = "o:point false leading"

/*: , msg: :*/
fileprivate let str_succeedName:String = "equal center, msg:"

/*: , data: :*/
fileprivate let str_acceptData:String = ", data:return background cell model var"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_playContent:String = "false check path letSOCKET 接收"
fileprivate let str_makeText:String = "异常消息：clet activity"

/*: "onReceivedGift" :*/
fileprivate let str_topTitle:String = "private true letonRece"
fileprivate let str_fatalFormerName:String = "ivedGifttab data"

/*: "onVideoMatch" :*/
fileprivate let str_modelName:[Character] = ["o","n","V","i","d","e","o","M","a"]
fileprivate let str_pageTitle:[Character] = ["t","c","h"]

/*: "onRequestCall" :*/
fileprivate let str_alongTitle:String = "onReqmore text my item in"
fileprivate let str_equalName:String = "size"

/*: "onCancelCall" :*/
fileprivate let str_processModelValue:String = "second type type row anyonCan"

/*: "onRefuseCall" :*/
fileprivate let str_appValue:String = "var to spaceonRef"
fileprivate let str_makeValue:String = "useCalllabel type app index data"

/*: "onAcceptCall" :*/
fileprivate let str_siblingText:String = "user let add target sinceonAccep"
fileprivate let str_topData:[Character] = ["t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let str_sharedTitle:[Character] = ["o","n","S","t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let str_makeName:[Character] = ["f","r","e","e"]
fileprivate let str_contentValue:[Character] = ["C","a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let str_intimateName:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let str_makeData:String = "app model price progressonMis"

/*: "acceptCall" :*/
fileprivate let str_pointChallengeData:String = "send"
fileprivate let str_partyResultText:String = "controlcontrolep"

/*: "stopCall" :*/
fileprivate let str_viewValue:String = "stopCalldetail background need"

/*: "requestCall" :*/
fileprivate let str_colorValue:String = "requestCnormal text data equal gift"
fileprivate let str_pricePathMakeName:String = "ALL"

/*: "[Invite you to a video call...]" :*/
fileprivate let str_listCoverContent:[UInt8] = [0x3b,0x29,0xe,0x16,0x9,0x14,0x5,0x40,0x19,0xf,0x15,0x40,0x14,0xf,0x40,0x1,0x40,0x16,0x9,0x4,0x5,0xf,0x40,0x3,0x1,0xc,0xc,0x4e,0x4e,0x4e,0x3d]

/*: "type" :*/
fileprivate let str_colorEventWithTitle:String = "TYPE"

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_playerText:[UInt8] = [0xf4,0xe6,0xc1,0xd9,0xc6,0xdb,0xca,0x8f,0xd6,0xc0,0xda,0x8f,0xdb,0xc0,0x8f,0xce,0x8f,0xd9,0xc0,0xc6,0xcc,0xca,0x8f,0xcc,0xce,0xc3,0xc3,0x81,0x81,0x81,0xf2]

private func toInfo(origin num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "uid" :*/
fileprivate let str_textName:String = "uroot"

/*: "nickname" :*/
fileprivate let str_leadingText:[Character] = ["n","i","c","k","n","a"]
fileprivate let str_sizeTitle:[Character] = ["m","e"]

/*: "headPic" :*/
fileprivate let str_deviceValue:String = "H"
fileprivate let str_miniData:String = "eadPictitle equal size"

/*: "onCallResume" :*/
fileprivate let str_limitListData:String = "to"
fileprivate let str_videoModelName:[Character] = ["n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let str_defineName:[Character] = ["c","a","n","c","e","l"]
fileprivate let str_hiddenValue:String = "make view let modelCall"

/*: "refuseCall" :*/
fileprivate let str_toolText:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let str_succeedValue:String = "background submitonCa"
fileprivate let str_insideTitle:[Character] = ["i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let str_sourceValue:String = "onLivlet equal icon welcome"
fileprivate let str_detailName:String = "valueivalueg"

/*: "reason" :*/
fileprivate let str_textTitle:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let str_blockValue:String = "tgiftme"

/*: "onStopLive" :*/
fileprivate let str_indexName:[Character] = ["o","n","S","t","o","p","L","i","v"]
fileprivate let str_keyTitle:String = "all"

/*: "onSendProgramGift" :*/
fileprivate let str_stageData:[Character] = ["o","n","S","e","n","d","P"]
fileprivate let str_succeedContent:String = "ropager"

/*: "onGameFs" :*/
fileprivate let str_shareTitle:[Character] = ["o","n","G","a","m","e"]
fileprivate let str_styleTopContent:String = "Fsgift label"

/*: "broadStatus" :*/
fileprivate let str_toolTitle:String = "bronormald"

/*: "onCallChatMsg" :*/
fileprivate let str_frameName:[Character] = ["o","n","C","a","l","l","C"]
fileprivate let str_listTitle:String = "border height type instance varhatMsg"

/*: "onNotificationPopupWin" :*/
fileprivate let str_hiddenData:String = "app tableonNot"
fileprivate let str_actionName:String = "TION"
fileprivate let str_closedText:String = "PopupWingift mode"

/*: "onUserBlock" :*/
fileprivate let str_nameValue:[Character] = ["o"]
fileprivate let str_targetValue:[Character] = ["n","U","s","e","r","B","l","o","c","k"]

/*: "freeCallWin" :*/
fileprivate let str_requestValue:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let str_popColorValue:String = "row segment to view imagelWin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NameSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kLet_bottomViewValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let kLet_tableDisplayValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let kLet_barContent = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kLet_netName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kLet_listTitle = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let kLet_sendText = (str_rawContent.uppercased() + String(str_viewOfTitle))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let kLet_pathData = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kLet_recordText = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let kLet_detailNetName = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kLet_firstValue = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let kLet_tabContent = NSNotification.Name(rawValue: String(bytes: str_centerData.map{nonsolidColour(title: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let kLet_countName = NSNotification.Name(rawValue: String(bytes: str_honeyName.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol LabelObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func loadDown(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol LikeMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func coverUp(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func nudgeAll(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol DateHeadObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func messageAssemblage(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func criminalRecord(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func toData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func tableGift(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol ManagingDirectorObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func actionTable(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func equalColor(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func applicationToAssemblage(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class NameSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum MessageType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FactoryShowThen.shared.direction == .rightToLeft ? 0 : kLet_scaleName,
        //: y: StatusBarHeight+190
        y: kLet_environmentContent + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FactoryShowThen.shared.direction == .rightToLeft ? 0 : kLet_scaleName,
        //: y: StatusBarHeight+190
        y: kLet_environmentContent + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: FactoryShowThen.shared.direction == .rightToLeft ? 0 : kLet_scaleName,
        //: y: StatusBarHeight+500
        y: kLet_environmentContent + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: LabelObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: LikeMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: DateHeadObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: ManagingDirectorObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = NameSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(gestureConstraint(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(withWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(str_tableName.suffix(5)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CurrentApplication.shared as! CurrentApplication).destroyFor()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(str_ofData.suffix(8)) + String(str_messageText)))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (CurrentApplication.shared as! CurrentApplication).destroyFor()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension NameSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        EnableReactiveCompatible.nameBy(msg: (String(str_cellValue) + String(str_fromName)))
        //: sendPing()
        collideWith()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_objectContent,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: kLet_sendText)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        giftWith()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        EnableReactiveCompatible.nameBy(msg: (String(str_modelData) + str_requestTitle.replacingOccurrences(of: "content", with: "接") + " error:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = WatercourseThen.share.reachability?.connection, status != .unavailable else {
        guard let status = WatercourseThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kLet_netName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = realiseInRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            EnableReactiveCompatible.nameBy(msg: (str_cellName.replacingOccurrences(of: "equal", with: "K") + "ET \u{5931}败") + "\(kLet_netName)" + (str_mainTitle.capitalized + "换\u{8fc7}So" + String(str_addToText.prefix(6)) + str_dayTitle.replacingOccurrences(of: "model", with: "地")) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.behindAll(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.greet(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        EnableReactiveCompatible.nameBy(msg: (String(str_leadingValue) + String(str_withName.prefix(2))) + "\(code)" + (String(str_logDetailValue.prefix(4)) + "ason:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.sizeOff(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kLet_firstValue {
            //: return
            return
        }
        //: if let status = WatercourseThen.share.reachability?.connection, status != .unavailable {
        if let status = WatercourseThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.behindAll(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func realiseInRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = WatercourseThen.share.appConfigMode.ws
        self.linkAddrArr = WatercourseThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = makePing()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: kLet_sendText)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = WatercourseThen.share.loginUid
        let uid = WatercourseThen.share.loginUid
        //: let sid = WatercourseThen.share.loginSessionId
        let sid = WatercourseThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(str_executeData.prefix(6))) + "\(sid)" + (String(str_layerData.prefix(5))) + "\(uid)" + (String(str_picContent) + String(str_managerButtonNameData.prefix(6)) + str_removeName.replacingOccurrences(of: "image", with: "ho") + "e&ver" + String(str_pathData.suffix(5))) + "\(kLet_licenseRequestDoingTitle)" + (String(str_shareProductData)) + "\(kLet_voiceValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func makePing() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return kLet_tableContent ? [(String(str_eventLabName.prefix(6)) + "ver.") + "\(kLet_barRecordScreenText)" + (str_productValue.replacingOccurrences(of: "last", with: ".") + String(str_addShowText.prefix(4)) + "7766")] : [(str_startData.replacingOccurrences(of: "name", with: "3") + ".185.194" + String(str_managerName))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func gestureConstraint(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            EnableReactiveCompatible.nameBy(msg: String(bytes: str_effectData.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.sizeOff(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.behindAll(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func withWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.behindAll(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.playView()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func collideWith() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kLet_bottomViewValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.migration(info: [(String(str_valueTitle)): (str_beautifulValue.replacingOccurrences(of: "user", with: "pi"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func loadFile() {
        //: endTalkingPing()
        imagePing()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: kLet_tableDisplayValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.toController()?.isKind(of: UpwardlyViewController.self) == false && self.toController()?.isKind(of: ReadCallerDelegate.self) == false) && (self.currTalkingVC?.isKind(of: UpwardlyViewController.self) == false && self.currTalkingVC?.isKind(of: ReadCallerDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.migration(info: [(String(str_valueTitle)): (String(str_textData.suffix(5)) + String(str_makeLineName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func imagePing() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func playView() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(str_serviceText.replacingOccurrences(of: "suspend", with: "e") + "edDate")] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kLet_positionClickData {
                //: WatercourseThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                WatercourseThen.share.equalSearched(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func makeList() {
        //: endLivePing()
        entire()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: kLet_barContent, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.migration(info: [(String(str_valueTitle)): (String(str_dropName))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func entire() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func giftWith() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = FailThen()
        //: let keyWindow = ListMacroDefine.getWindow()
        let keyWindow = ListMacroDefine.startLoad()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func gestureView() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ListThen.deleteItem().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard DateRangeThen.portion().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func sizeOff(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (str_labelLayerIfValue + "ocket"))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kLet_firstValue, reason: (String(str_equalToName) + String(str_canData.prefix(4))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        imagePing()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            gestureView()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func lagniappe() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = NameSocketManager.shared.realiseInRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.behindAll(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func behindAll(timeInterval: TimeInterval, type: MessageType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            EnableReactiveCompatible.nameBy(msg: (String(str_scaleTitle)) + "\(type.rawValue)" + (String(str_versionTheText)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.socket(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func socket(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                EnableReactiveCompatible.nameBy(msg: (str_screenNumberervalValue.replacingOccurrences(of: "normal", with: "E") + "T \u{8fde}接" + str_upPlayerData + "要重\u{65b0}连\u{63a5}" + str_hiddenAgeData.capitalized))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                EnableReactiveCompatible.nameBy(msg: (str_centerTitle.replacingOccurrences(of: "view", with: "T") + " 正在\u{8fde}接 " + str_giftData))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(str_onData) + String(str_managerText.suffix(3))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(str_sharedData)) + "\(GazetteThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        sizeOff(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kLet_listTitle
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        EnableReactiveCompatible.nameBy(msg: (String(str_photoValue) + String(str_colorContent) + String(str_meEnterValue)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func migration(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if WatercourseThen.share.reachability?.connection == nil || WatercourseThen.share.reachability?.connection == .unavailable {
        if WatercourseThen.share.reachability?.connection == nil || WatercourseThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(str_valueTitle)): (String(str_connectionValue) + String(str_scaleDownData)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (str_barName.lowercased()): "\(kLet_detailNetName)",
                                       //: "data": [:]]
                                       (str_errorValue.replacingOccurrences(of: "load", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            greet(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_valueTitle))] as? String == (str_beautifulValue.replacingOccurrences(of: "user", with: "pi")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                EnableReactiveCompatible.nameBy(msg: (str_lengthTableEqualValue.replacingOccurrences(of: "succeed", with: "OC") + "T \u{8fde}接保活" + str_leadingData.replacingOccurrences(of: "m", with: "c") + String(str_backgroundData)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(str_valueTitle))] as? String == (String(str_textData.suffix(5)) + String(str_makeLineName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                EnableReactiveCompatible.nameBy(msg: String(bytes: str_indexValue.map{extraAttention(lab: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(str_valueTitle))] as? String == (String(str_dropName)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                EnableReactiveCompatible.nameBy(msg: String(bytes: str_pairRegularData.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(str_valueTitle))] as? String == (str_beautifulValue.replacingOccurrences(of: "user", with: "pi")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(str_valueTitle)): (String(str_textColorName.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (str_barName.lowercased()): "\(kLet_pathData)",
                                           //: "data": [:]]
                                           (str_errorValue.replacingOccurrences(of: "load", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                greet(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.behindAll(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func greet(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(str_valueTitle))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(str_barName.lowercased())].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(str_labelContent.replacingOccurrences(of: "temp", with: "m"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (str_modelPageName + "息：cmd" + String(str_normalMainData.suffix(5))) + "\(cmdName)" + (String(str_voiceEqualNameTitle.prefix(6)) + String(str_buttonViewTitle.prefix(2))) + "\(errorNo)" + (String(str_succeedName.suffix(6))) + "\(msg)" + (String(str_acceptData.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        businessIndex(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            EnableReactiveCompatible.nameBy(msg: (String(str_playContent.suffix(9)) + String(str_makeText.prefix(6)) + "mdName:") + "\(cmdName)" + (String(str_voiceEqualNameTitle.prefix(6)) + String(str_buttonViewTitle.prefix(2))) + "\(errorNo)" + (String(str_succeedName.suffix(6))) + "\(msg)" + (String(str_acceptData.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: WatercourseThen.share.loginUserMode.freeCallTimes = 0
                WatercourseThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !WatercourseThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !WatercourseThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                AdministratorReactiveCompatible.shared.verification()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.loadDown(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_topTitle.suffix(6)) + String(str_fatalFormerName.prefix(8))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kLet_backgroundDisplayContent, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_modelName) + String(str_pageTitle)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.coverUp(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_alongTitle.prefix(5)) + "uestCal" + str_equalName.replacingOccurrences(of: "size", with: "l")) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.nudgeAll(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_processModelValue.suffix(5)) + "celCall") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_appValue.suffix(5)) + String(str_makeValue.prefix(7))) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_siblingText.suffix(7)) + String(str_topData)) { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            loadFile()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.equalColor(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_sharedTitle)) { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            imagePing()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_makeName) + String(str_contentValue))] as? Int {
                //: WatercourseThen.share.loginUserMode.freeCallTimes = freeCallTimes
                WatercourseThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: WatercourseThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            WatercourseThen.share.loginUserMode.mf_coin = json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(String(str_intimateName))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_messageTitle, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CurrentApplication.shared as! CurrentApplication).upTo()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(str_makeData.suffix(5)) + "sCoin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.tableGift(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (str_pointChallengeData.replacingOccurrences(of: "send", with: "a") + str_partyResultText.replacingOccurrences(of: "control", with: "c") + "tCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            loadFile()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.messageAssemblage(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_viewValue.prefix(8))) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            imagePing()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.criminalRecord(data: data)
            // 更新余额
            //: WatercourseThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            WatercourseThen.share.loginUserMode.mf_coin = json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(String(str_intimateName))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            equalFreedom(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_messageTitle, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (CurrentApplication.shared as! CurrentApplication).upTo()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(str_colorValue.prefix(8)) + str_pricePathMakeName.lowercased()) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: kLet_tabContent, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: WatercourseThen.share.recieve1v1TalkRequest(info: data)
                WatercourseThen.share.equalSearched(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(str_serviceText.replacingOccurrences(of: "suspend", with: "e") + "edDate")] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_listCoverContent.map{$0^96}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(str_colorEventWithTitle.lowercased())].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_playerText.map{toInfo(origin: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                CellDelegateHelper.thoughtImageDismiss(uid: String(json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(str_textName.replacingOccurrences(of: "root", with: "id"))].intValue),
                                                      //: title: data["nickname"] as? String,
                                                      title: data[(String(str_leadingText) + String(str_sizeTitle))] as? String,
                                                      //: body: body,
                                                      body: body,
                                                      //: imageUrl: data["headPic"] as? String)
                                                      imageUrl: data[(str_deviceValue.lowercased() + String(str_miniData.prefix(6)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                CellDelegateHelper.shared.nameProduct()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (str_limitListData.replacingOccurrences(of: "to", with: "o") + String(str_videoModelName)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            loadFile()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(str_limitListData.replacingOccurrences(of: "to", with: "o") + String(str_videoModelName))] = "1"
            //: WatercourseThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            WatercourseThen.share.increaseInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (CurrentApplication.shared as! CurrentApplication).destroyFor()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(str_defineName) + String(str_hiddenValue.suffix(4))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            imagePing()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.toData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                CellDelegateHelper.spread(identifier: String(json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(str_textName.replacingOccurrences(of: "root", with: "id"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                CellDelegateHelper.shared.squareBlock()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(str_toolText)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            imagePing()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.actionTable(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_succeedValue.suffix(4)) + "llMotivat" + String(str_insideTitle)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if WatercourseThen.share.loginUserMode.sex == Gender.female.rawValue {
            if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.applicationToAssemblage?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_sourceValue.prefix(5)) + "eWar" + str_detailName.replacingOccurrences(of: "value", with: "n")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ListThen.delay(content: msg, reason: json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(String(str_textTitle))].stringValue, time: json[(str_errorValue.replacingOccurrences(of: "load", with: "at"))][(str_blockValue.replacingOccurrences(of: "gift", with: "i"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_indexName) + str_keyTitle.replacingOccurrences(of: "all", with: "e")) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ListThen.deleteItem().plus()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_stageData) + str_succeedContent.replacingOccurrences(of: "page", with: "g") + "amGift") { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.sizeExecuteMsg(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_shareTitle) + String(str_styleTopContent.prefix(2))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(str_toolTitle.replacingOccurrences(of: "normal", with: "a") + "Status")] as? Int ?? -1
            //: if WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_countName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kLet_backgroundDisplayContent, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: kLet_countName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(str_frameName) + String(str_listTitle.suffix(6))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            AutomaticallyThen.way().alongFor(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_hiddenData.suffix(5)) + "ifica" + str_actionName.lowercased() + String(str_closedText.prefix(8))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            AdministratorReactiveCompatible.shared.colorOn(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_nameValue) + String(str_targetValue)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ListThen.deleteItem().plus()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            DateRangeThen.portion().scene()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: kLet_screenPathName, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension NameSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func businessIndex(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(str_colorValue.prefix(8)) + str_pricePathMakeName.lowercased()) || cmdName == (String(str_alongTitle.prefix(5)) + "uestCal" + str_equalName.replacingOccurrences(of: "size", with: "l")) {
            //: if TalkingLiveManager.shared().isLive == true {
            if ListThen.deleteItem().isLive == true {
                //: NEED_RESTORE_LIVE = true
                kLet_barScreenData = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ListThen.skinColour(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ListThen.deleteItem().plus()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard kLet_barScreenData == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(str_defineName) + String(str_hiddenValue.suffix(4))) || cmdName == (String(str_processModelValue.suffix(5)) + "celCall") ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(str_toolText)) || cmdName == (String(str_appValue.suffix(5)) + String(str_makeValue.prefix(7))) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_viewValue.prefix(8))) || cmdName == (String(str_sharedTitle))
        {
            //: NEED_RESTORE_LIVE = false
            kLet_barScreenData = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = CornerThen(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ListThen.deleteItem().needEnable()
            }
            //: alert.show()
            alert.conversationSearch()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func equalFreedom(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_makeName) + String(str_contentValue))] as? Int {
            //: WatercourseThen.share.loginUserMode.freeCallTimes = freeCallTimes
            WatercourseThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(str_requestValue) + String(str_popColorValue.suffix(4)))] as? Bool
        //: if freeCallWin == true && WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = TitleUpView()
            //: view.show()
            view.titleByShow()
            //: view.setShowData(data: data)
            view.bringHomeData(data: data)
        }
    }
}
