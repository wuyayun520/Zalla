
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_infoData:[UInt8] = [0x2b,0x2c,0x2b,0x36,0x6a,0x21,0x2d,0x26,0x27,0x30,0x78,0x6b,0x62,0x2a,0x23,0x31,0x62,0x2c,0x2d,0x36,0x62,0x20,0x27,0x27,0x2c,0x62,0x2b,0x2f,0x32,0x2e,0x27,0x2f,0x27,0x2c,0x36,0x27,0x26]

private func burnNamePresentation(component num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "F5F5F5" :*/
fileprivate let str_jumpData:String = "FtopFtopFtop"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_buttonData:[Character] = ["T","a","l","k","i","n","g","C","h","a"]
fileprivate let str_maleValue:String = "show string of vartTex"
fileprivate let str_selectedData:String = "type self with false iconllID"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_coverDateValue:[Character] = ["T","a","l","k","i","n","g"]
fileprivate let str_scaleContent:String = "image content add titleChatImage"
fileprivate let str_sexText:String = "MsgCellIDpoint of"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_addValue:String = "Talkmodel string current new color"
fileprivate let str_logValue:String = "var namehatV"
fileprivate let str_rawIndexContent:String = "return let length datasgCel"
fileprivate let str_giftValue:[Character] = ["l","I","D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_byAddName:String = "to add viewTalkin"
fileprivate let str_fromText:String = "GiftMslet image will"

/*: "EqualMsgCell" :*/
fileprivate let str_imageValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","s","k","F","o","r","G"]
fileprivate let str_intervalData:String = "let in send refuse manageriftMs"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_appName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i"]
fileprivate let str_dropData:[Character] = ["p","s","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_fileFromData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","M","a","t","c","h","M","s","g","C","e","l","l"]
fileprivate let str_termsNumberName:[Character] = ["I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_pleaseData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","u","d","i","o","M","s"]
fileprivate let str_themeName:String = "ok fill you pushgCellID"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_toNoName:[UInt8] = [0x31,0x3e,0x49,0x48,0x46,0x4b,0x44,0x20,0x45,0x3e,0x51,0x30,0x56,0x50,0x51,0x42,0x4a,0x2b,0x4c,0x51,0x46,0x43,0x27,0x52,0x4a,0x4d,0x2a,0x50,0x44,0x20,0x42,0x49,0x49,0x26,0x21]

fileprivate func playerFramework(text num: UInt8) -> UInt8 {
    let value = Int(num) - 221
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_styleText:String = "TSystfirst not screen"
fileprivate let str_photoText:String = "log for center backgroundsageCe"
fileprivate let str_normalValue:String = "ll_Reautomatic party slide fail let"

/*: "#128CFF" :*/
fileprivate let str_labTitle:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let str_sizeColorName:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let str_bottomData:String = "session equal#8370F"
fileprivate let str_toContent:[Character] = ["F"]

/*: "Quick Greeting" :*/
fileprivate let str_viewValue:[Character] = ["Q","u","i","c","k"," ","G","r","e","e","t"]
fileprivate let str_kitServerKeyValue:[Character] = ["i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let str_operationTitle:String = "end return bottom var videoBeau"
fileprivate let str_ofBagText:String = "otoburn"

/*: "url" :*/
fileprivate let str_uponName:String = "urequestl"

/*: "unlockGift" :*/
fileprivate let str_withText:[Character] = ["u","n","l","o"]
fileprivate let str_closeFinishRefreshTitle:String = "ckGiftadd view index equal fatal"

/*: "%d" :*/
fileprivate let str_viewAppValue:String = "card image%d"

/*: "isBurn" :*/
fileprivate let str_shareTitle:[UInt8] = [0xe7,0xfd,0xcc,0xfb,0xfc,0xe0]

/*: "giftId" :*/
fileprivate let str_controlName:String = "mp frame user value nogiftId"

/*: "rYMsgType" :*/
fileprivate let str_inputContent:String = "file app store item intimaterYMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let str_transformText:String = "GJ:Cato server let var style"
fileprivate let str_cornerAddTitle:String = "stempm"

/*: "changeDirection" :*/
fileprivate let str_blockTitle:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t"]
fileprivate let str_colorName:String = "iolab"

/*: "extra" :*/
fileprivate let str_modelTitle:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let str_dataName:String = "msgInprogress table in extension title"
fileprivate let str_modelContent:String = "FO"

/*: "toUid" :*/
fileprivate let str_layerToName:String = "toUidview name height server"

/*: "wantGift" :*/
fileprivate let str_meName:[Character] = ["w","a","n","t","G","i","f"]
fileprivate let str_startData:String = "translate"

/*: "jumpKey" :*/
fileprivate let str_normalEqualData:String = "gift method mic imagejumpKey"

/*: "truePersonAuth" :*/
fileprivate let str_eventPathLetData:String = "T"
fileprivate let str_cellAddTitleValue:String = "title if height add asruePe"

/*: "logType" :*/
fileprivate let str_endingTempName:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let str_hideData:[UInt8] = [0xb,0x15,0x1,0x32,0x1f,0x16,0x3]

private func colorText(data num: UInt8) -> UInt8 {
    return num ^ 102
}

/*: "txt" :*/
fileprivate let str_indexModelName:String = "waitxwait"

/*: "type" :*/
fileprivate let str_scaleName:String = "typtouch"

/*: "QuickMsg" :*/
fileprivate let str_backPathTitle:String = "string height trueQuickM"
fileprivate let str_makeData:String = "simage"

/*: "params" :*/
fileprivate let str_rowName:[UInt8] = [0x91,0x80,0x93,0x80,0x8c,0x92]

private func micUniversal(text num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "content" :*/
fileprivate let str_arrayName:[UInt8] = [0x12,0x1e,0x1d,0x23,0x14,0x1d,0x23]

fileprivate func trueTop(up num: UInt8) -> UInt8 {
    let value = Int(num) - 175
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "id" :*/
fileprivate let str_onSegmentValue:String = "limit"

/*: "txtId" :*/
fileprivate let str_toObjectData:String = "txtIdadd true server change cell"

/*: "message" :*/
fileprivate let str_sizeContent:[UInt8] = [0x4,0xc,0x1a,0x1a,0x8,0xe,0xc]

/*: "TxtMsg" :*/
fileprivate let str_makeWithValue:[Character] = ["T","x","t","M","s"]
fileprivate let str_textName:String = "to"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_visibleLabelData:[UInt8] = [0xd8,0xe1,0xe7,0xed,0xeb,0xae,0xe6,0xeb,0xe2,0xe2,0xe1,0xae,0xea,0xe1,0xf9,0xe0,0xe2,0xe1,0xef,0xea,0xae,0xe8,0xef,0xe7,0xe2,0xeb,0xea,0xa0,0xae,0xde,0xfc,0xe1,0xed,0xeb,0xea,0xfb,0xfc,0xeb]

private func modelArrayData(index num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "length" :*/
fileprivate let str_toValue:String = "lengframe"

/*: "audio" :*/
fileprivate let str_kitText:String = "autopo"

/*: "audioId" :*/
fileprivate let str_requestName:String = "true centeraudioId"

/*: "audioLength" :*/
fileprivate let str_viewListName:[UInt8] = [0x1a,0x2e,0x1d,0x22,0x28,0x5,0x1e,0x27,0x20,0x2d,0x21]

fileprivate func aspectTitle(row num: UInt8) -> UInt8 {
    let value = Int(num) - 185
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let str_fillEditTitle:[Character] = ["a","u","d","i","o","S","a","n","d","b"]
fileprivate let str_colorGestureVideoText:[Character] = ["o","x"]

/*: "senderId" :*/
fileprivate let str_lastDetailValue:[UInt8] = [0x60,0x76,0x7d,0x77,0x76,0x61,0x5a,0x77]

/*: "AudioMsg" :*/
fileprivate let str_statusValue:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let str_imageResumeTitle:String = "Can\'t "
fileprivate let str_clearName:String = "indicator current var model blank m"
fileprivate let str_pushText:[Character] = ["e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let str_fillValue:String = "QUOTE"

/*: "quoteMsgInfo" :*/
fileprivate let str_gestureData:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let str_underTitle:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let str_picValue:String = "ImgMsglet i in key request"

/*: "img" :*/
fileprivate let str_colorRefreshTempName:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_firstData:[UInt8] = [0x94,0x98,0x8c,0x92,0x90,0x80,0x9d,0x94]

fileprivate func viewSucceed(index num: UInt8) -> UInt8 {
    let value = Int(num) - 43
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let str_statusTitle:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let str_sessionValue:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let str_modeData:[Character] = ["V","i","d","e","o"]
fileprivate let str_playerText:String = "medium path sharedMsg"

/*: "video" :*/
fileprivate let str_toModelInName:String = "VIDEO"

/*: "coverImg" :*/
fileprivate let str_managerValue:[UInt8] = [0xb1,0xbd,0xa4,0xb7,0xa0,0x9b,0xbf,0xb5]

/*: "videoUrl" :*/
fileprivate let str_userContent:[UInt8] = [0xac,0x9f,0x9a,0x9b,0xa5,0x8b,0xa8,0xa2]

fileprivate func modelCounterval(transform num: UInt8) -> UInt8 {
    let value = Int(num) - 54
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let str_areaTitle:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let str_overText:[UInt8] = [0x34,0x3d,0x36]

private func someoneBack(voice num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "Documents/User/voice/" :*/
fileprivate let str_labelValue:String = "to voice labelDocum"
fileprivate let str_addName:String = "ser/vself cookie ending color icon"
fileprivate let str_listName:String = "oice/list height"

/*: "Documents/User/record/" :*/
fileprivate let str_appBottomBuildValue:String = "info self succeed messageDocu"
fileprivate let str_colorValue:[Character] = ["s","/","U","s","e","r","/","r","e","c","o","r","d","/"]

/*: "msgIncome" :*/
fileprivate let str_commitTitle:String = "msgIncomvideo year app m"
fileprivate let str_picData:String = "bottom"

/*: "mfBean" :*/
fileprivate let str_deviceDataText:[UInt8] = [0x4d,0x46,0x62,0x45,0x41,0x4e]

private func valueReturnCell(index num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "messageType" :*/
fileprivate let str_effectName:[Character] = ["m","e","s","s","a","g"]
fileprivate let str_changeWithText:String = "medium let coloreType"

/*: "WantGift" :*/
fileprivate let str_ofName:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_acrossTitle:[Character] = ["_","P","r","i","v"]
fileprivate let str_maxData:String = "self data shared right normalateCha"
fileprivate let str_arrowValue:String = "make to textsTitle_"

/*: "msgId" :*/
fileprivate let str_todayData:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_makeTitle:String = "managerta"

/*: "attachSysMsg" :*/
fileprivate let str_pointData:String = "attachSyerror server"
fileprivate let str_attributeName:String = "sMsgleading equal data view"

/*: "intimatePhoto" :*/
fileprivate let str_blockContent:[Character] = ["i","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: _ :*/
fileprivate let str_momentName:String = "size"

/*: "totalIntimate" :*/
fileprivate let str_canName:[Character] = ["t","o","t","a","l","I","n","t","i"]
fileprivate let str_touchMoreInputData:[Character] = ["m","a","t","e"]

/*: "uid" :*/
fileprivate let str_contentData:[UInt8] = [0x64,0x69,0x75]

/*: "intimate" :*/
fileprivate let str_blockValue:[UInt8] = [0xcd,0xd2,0xd8,0xcd,0xd1,0xc5,0xd8,0xc9]

fileprivate func everyTarget(user num: UInt8) -> UInt8 {
    let value = Int(num) - 100
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_contentName:[UInt8] = [0x8e,0xac,0xa3,0xea,0xb9,0xed,0xb8,0xbe,0xa8,0xed,0xb9,0xa5,0xa8,0xed,0xab,0xb8,0xa3,0xae,0xb9,0xa4,0xa2,0xa3,0xed,0xa9,0xb8,0xa8,0xed,0xb9,0xa2,0xed,0xb4,0xa2,0xb8,0xbf,0xed,0xa1,0xac,0xae,0xa6,0xed,0xa2,0xab,0xed,0xa4,0xa3,0xb9,0xa4,0xa0,0xac,0xae,0xb4,0xed,0xa1,0xa8,0xbb,0xa8,0xa1,0xe3]

private func addWith(direction num: UInt8) -> UInt8 {
    return num ^ 205
}

/*: "giftNum" :*/
fileprivate let str_toData:String = "glock"
fileprivate let str_photoData:String = "target attribute style descriptionftNum"

/*: "pkgItemsetId" :*/
fileprivate let str_targetName:String = "pkgIequal var info"

/*: "from" :*/
fileprivate let str_labelName:String = "fpositionm"

/*: "wantGiftId" :*/
fileprivate let str_countData:String = "wantGiindex block"

/*: "WantGiftMsg" :*/
fileprivate let str_partyData:String = "WantGiload to"

/*: "gift" :*/
fileprivate let str_pathData:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let str_inputAddData:String = "Viewcookie male mid self"
fileprivate let str_democratName:[Character] = [" ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let str_endingContent:String = "to level let actual enableNotes"

/*: "Report" :*/
fileprivate let str_popValue:String = "Reporvoice hidden"
fileprivate let str_frameTitle:[Character] = ["t"]

/*: "View read receipts?" :*/
fileprivate let str_foundationData:String = "View radd up make situation"
fileprivate let str_loadContent:String = "eframeip"
fileprivate let str_picName:[Character] = ["t","s","?"]

/*: "He" :*/
fileprivate let str_dataValue:String = "Hemanager if size"

/*: "She" :*/
fileprivate let str_pageContent:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let str_shareName:String = "or manager color screen make%@ is"
fileprivate let str_appearTitle:String = "color remove color blockg too "
fileprivate let str_fromName:[Character] = ["a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_makeTextElseValue:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let str_videoStatusContent:String = "data time below anyUploa"
fileprivate let str_customValue:String = "d failedshow will value for pop"

/*: "播放状态: :*/
fileprivate let str_sizeIconData:[Character] = ["\u{64ad}","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let str_objectData:String = "c2c_if title class"

/*: "msg" :*/
fileprivate let str_resultValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_arrayText:String = "tipblock"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_sizeToTitle:[UInt8] = [0xd9,0xef,0xf5,0xa0,0xe3,0xe1,0xee,0xa0,0xf5,0xf3,0xe5,0xa0,0xf4,0xe8,0xe5,0xa0,0xf6,0xe9,0xe4,0xe5,0xef,0xa0,0xe3,0xe8,0xe1,0xf4,0xa0,0xe6,0xe5,0xe1,0xf4,0xf5,0xf2,0xe5,0xa0,0xf4,0xef,0xa0,0xe7,0xe9,0xf6,0xe5,0xa0,0xa5,0xc0,0xa0,0xe1,0xa0,0xe3,0xe1,0xec,0xec]

/*: "cellData" :*/
fileprivate let str_addressValue:String = "cellDaequal if image match"
fileprivate let str_lengthFinishValue:String = "tvideo"

/*: "System notification" :*/
fileprivate let str_barValue:[Character] = ["S","y","s","t","e"]
fileprivate let str_clickName:[Character] = ["m"," ","n","o","t","i","f"]
fileprivate let str_maleText:String = "ictotion"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RawControllerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum SubFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class RawControllerDelegate: MusicViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = MixedThen()
    private var userWrap = MixedThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = SubFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: MixedThen = MixedThen()) {
    init(chatID: String, isFrom: SubFromEnum = .Normal, userWrap: MixedThen = MixedThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        RawControllerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        DefaultChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == ListMacroDefine.getXiaoMiID() {
        if targetId == ListMacroDefine.suspend() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == ListMacroDefine.getCustomerServiceID() {
        } else if targetId == ListMacroDefine.storageMediumId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_infoData.map{burnNamePresentation(component: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WatercourseThen.share.appConfigMode.disableShootScreen {
            if WatercourseThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CommandResultThen(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        addPlayerAnimation()
        //: createUI()
        innumerableness()
        //: createUIConstraints()
        computerSimulation()
        //: bindInteraction()
        actionTo()
        //: addTapGestureRecognizer()
        recognizer()
        //: func__requestChatInfo()
        farLoad()
        //: func__getFillDraftText()
        pubThroughParagraph()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        EqualBottomManagerDelegate.shared.bring()
        //: func__addVoiceNotification()
        addNotification()
        //: addReadPeceiptNotification()
        stochasticCell()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            heading()
            //: func__TipsNotification()
            systemAdd()
            //: if WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue {
            if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue {
                //: func__setReplyTipsView()
                modeControl()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        shape(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        shape(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            draftTo()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (str_jumpData.replacingOccurrences(of: "top", with: "5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(UpMsgCell.self, forCellReuseIdentifier: (String(str_buttonData) + String(str_maleValue.suffix(4)) + "tMsgCe" + String(str_selectedData.suffix(4))))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(ViewMessageCell.self, forCellReuseIdentifier: (String(str_coverDateValue) + String(str_scaleContent.suffix(9)) + String(str_sexText.prefix(9))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(ShowMsgCell.self, forCellReuseIdentifier: (String(str_addValue.prefix(4)) + "ingC" + String(str_logValue.suffix(4)) + "ideoM" + String(str_rawIndexContent.suffix(5)) + String(str_giftValue)))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(ScaleMessageCell.self, forCellReuseIdentifier: (String(str_byAddName.suffix(6)) + "gChat" + String(str_fromText.prefix(6)) + "gCellID"))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(EqualMsgCell.self, forCellReuseIdentifier: (String(str_imageValue) + String(str_intervalData.suffix(5)) + "gCell"))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(NetworkMsgCell.self, forCellReuseIdentifier: (String(str_appName) + String(str_dropData)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(ReadView.self, forCellReuseIdentifier: (String(str_fileFromData) + String(str_termsNumberName)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(PerformanceThen.self, forCellReuseIdentifier: (String(str_pleaseData) + String(str_themeName.suffix(7))))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(CornerViewCell.self, forCellReuseIdentifier: String(bytes: str_toNoName.map{playerFramework(text: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_styleText.prefix(5)) + "emMes" + String(str_photoText.suffix(6)) + String(str_normalValue.prefix(5)) + "useId"))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: HotelmanThen = {
        //: let m = TalkingCallMenuManager()
        let m = HotelmanThen()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: MoonshineViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: AdvertorialViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = MoonshineViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(str_labTitle)))!, .font: UIFont.conversationSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_sizeColorName)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(colorClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TitleAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TitleAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: ManagingDirectorReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = ManagingDirectorReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.conversationSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_bottomData.suffix(6)) + String(str_toContent))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_viewValue) + String(str_kitServerKeyValue)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.smallFor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(modelAcrossDoing), for: .touchUpInside)
        //: btn.isHidden = !WatercourseThen.share.appUserConfigMode.showDirectGreet || WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue || WatercourseThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !WatercourseThen.share.appUserConfigMode.showDirectGreet || WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue || WatercourseThen.share.appStatus == PublishWidthTotal.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: BecomeLogView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = BecomeLogView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: RequestThen = {
        //: let v = TalkingSelectTranslationView()
        let v = RequestThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.translationMove(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: MusicResultReactiveCompatible = {
        //: let v = TalkingTranslationResultView()
        let v = MusicResultReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.theory(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.conversationSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(str_bottomData.suffix(6)) + String(str_toContent))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_operationTitle.suffix(4)) + "ty Ph" + str_ofBagText.replacingOccurrences(of: "burn", with: "s")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.smallFor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(photos), for: .touchUpInside)
        //: btn.isHidden = WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue || WatercourseThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue || WatercourseThen.share.appStatus == PublishWidthTotal.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: NeighborDataSource = {
        //: let v = TalkingLiveRoomGamesView()
        let v = NeighborDataSource()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: NameBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = NameBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PerformanceReactiveCompatible = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PerformanceReactiveCompatible()
        //: v.isHidden = (WatercourseThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (WatercourseThen.share.appStatus == PublishWidthTotal.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: FilmView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = FilmView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.ingatheringMessage()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension RawControllerDelegate: TinctureBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func miniskirt(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue {
                if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue {
                if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.direction(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension RawControllerDelegate: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func modelAcrossDoing() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        DefaultChatManager.divertTable(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.detailKey(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.isPic(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func photos() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        DefaultChatManager.operation(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = IndoorsRecognizerDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().optionsFinish(urlStr: dict?[(str_uponName.replacingOccurrences(of: "request", with: "r"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(str_withText) + String(str_closeFinishRefreshTitle.prefix(6)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = CornerJsonModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: str_shareTitle.map{$0^142}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(str_controlName.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.sendInfo(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: ToCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ToCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_inputContent.suffix(9)))].stringValue == (String(str_transformText.prefix(5)) + "llCu" + str_cornerAddTitle.replacingOccurrences(of: "temp", with: "to")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(str_blockTitle) + str_colorName.replacingOccurrences(of: "lab", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic[(str_modelTitle.lowercased())][(String(str_dataName.prefix(5)) + str_modelContent.lowercased())][(String(str_layerToName.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = ListCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ListCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: NameCellData.self) == true || cellData.isKind(of: AutomaticallyCellData.self) == true || cellData.isKind(of: ListCellData.self) == true || cellData.isKind(of: CurrentDateCellData.self) == true {
        if cellData.isKind(of: NameCellData.self) == true || cellData.isKind(of: AutomaticallyCellData.self) == true || cellData.isKind(of: ListCellData.self) == true || cellData.isKind(of: CurrentDateCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        DefaultChatManager.black(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        DefaultChatManager.answer(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = self.pic(data: data)

        //: if data.isKind(of: ListCellData.self) {
        if data.isKind(of: ListCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = UpMsgCell(style: .default, reuseIdentifier: (String(str_buttonData) + String(str_maleValue.suffix(4)) + "tMsgCe" + String(str_selectedData.suffix(4))))
            //: let cellData = data as! ListCellData
            let cellData = data as! ListCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.levelAsk() {
                //: if cellData.msgModel.quoteMsgInfo.uid == WatercourseThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == WatercourseThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = WatercourseThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = WatercourseThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: AutomaticallyCellData.self) {
        if data.isKind(of: AutomaticallyCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = ViewMessageCell(style: .default, reuseIdentifier: (String(str_coverDateValue) + String(str_scaleContent.suffix(9)) + String(str_sexText.prefix(9))))
            //: let cellData = data as! AutomaticallyCellData
            let cellData = data as! AutomaticallyCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CurrentDateCellData.self) {
        if data.isKind(of: CurrentDateCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = ShowMsgCell(style: .default, reuseIdentifier: (String(str_addValue.prefix(4)) + "ingC" + String(str_logValue.suffix(4)) + "ideoM" + String(str_rawIndexContent.suffix(5)) + String(str_giftValue)))
            //: let cellData = data as! CurrentDateCellData
            let cellData = data as! CurrentDateCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TableCellData.self) {
        if data.isKind(of: TableCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = ScaleMessageCell(style: .default, reuseIdentifier: (String(str_byAddName.suffix(6)) + "gChat" + String(str_fromText.prefix(6)) + "gCellID"))
            //: let cellData = data as! TableCellData
            let cellData = data as! TableCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ErrorCellData.self) {
        if data.isKind(of: ErrorCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = EqualMsgCell(style: .default, reuseIdentifier: (String(str_imageValue) + String(str_intervalData.suffix(5)) + "gCell"))
            //: let cellData = data as! ErrorCellData
            let cellData = data as! ErrorCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = AdMeasurable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.legacy(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_meName) + str_startData.replacingOccurrences(of: "translate", with: "t")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: NameCellData.self) {
        if data.isKind(of: NameCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = PerformanceThen(style: .default, reuseIdentifier: (String(str_pleaseData) + String(str_themeName.suffix(7))))
            //: let cellData = data as! NameCellData
            let cellData = data as! NameCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ToCellData.self) {
        if data.isKind(of: ToCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = ReadView(style: .default, reuseIdentifier: (String(str_fileFromData) + String(str_termsNumberName)))
            //: let cellData = data as! ToCellData
            let cellData = data as! ToCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: HandbillThen.self) {
        if data.isKind(of: HandbillThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = NetworkMsgCell(style: .default, reuseIdentifier: (String(str_appName) + String(str_dropData)))
            //: let cellData = data as! HandbillThen
            let cellData = data as! HandbillThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AllCellData.self) {
        if data.isKind(of: AllCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = CornerViewCell(style: .default, reuseIdentifier: String(bytes: str_toNoName.map{playerFramework(text: $0)}, encoding: .utf8)!)
            //: let cellData = data as! AllCellData
            let cellData = data as! AllCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != ListMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != ListMacroDefine.storageMediumId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: IndoorsPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            IndoorsPushManager.share.digitiserView(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ViewMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! AutomaticallyCellData
            let imageCellData = cell.messageData as! AutomaticallyCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = LabelRecognizerDelegate(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.direct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_showContent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.isochronal(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.punchTap(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                trimDown()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(kLet_seatContent)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = OnModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = CellMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [OnModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = EleventhHourPhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                trimDown()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ShowMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! CurrentDateCellData
            let videoCellData = cell.messageData as! CurrentDateCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = UnheardOfViewDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                trimDown()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = LogViewController(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.direct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_showContent)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.isochronal(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.punchTap(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                trimDown()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PerformanceThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PerformanceThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! NameCellData, cellView: audioCell)
            barShared(cellData: audioCell.cellData as! NameCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: UpMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! UpMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_modelTitle.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_normalEqualData.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(str_normalEqualData.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (str_eventPathLetData.lowercased() + String(str_cellAddTitleValue.suffix(5)) + "rsonAuth") {
                    //: if WatercourseThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if WatercourseThen.share.loginUserMode.isTPAuth == CommandCompatibleValue.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if WatercourseThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if WatercourseThen.share.loginUserMode.isTPAuth == CommandCompatibleValue.unknown.rawValue {
                        //: IndoorsPushManager.share.func__pushUserVerifyController(toast: nil)
                        IndoorsPushManager.share.overwhelm(toast: nil)
                        //: } else if WatercourseThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if WatercourseThen.share.loginUserMode.isTPAuth == CommandCompatibleValue.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = KeyReactiveCompatible()
                        //: IndoorsPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        IndoorsPushManager.share.queryEqual()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: IndoorsPushManager.share.func__pushUserVerifyController(toast: nil)
                        IndoorsPushManager.share.overwhelm(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NameModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: IndoorsPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    IndoorsPushManager.share.kind(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_inputContent.suffix(9)))] as? String == (String(str_transformText.prefix(5)) + "llCu" + str_cornerAddTitle.replacingOccurrences(of: "temp", with: "to")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())][(String(str_endingTempName))].intValue == 1 {
                    //: checkAndCallVoice()
                    indexArea()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    appSend()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: NetworkMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! NetworkMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_modelTitle.lowercased())] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(str_normalEqualData.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NameModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: IndoorsPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    IndoorsPushManager.share.kind(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: ReadView.self) {
            //: IndoorsPushManager.share.func__pushToUserDetailVC(uid: targetId)
            IndoorsPushManager.share.digitiserView(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: WriteRequestCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ViewMessageCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ViewMessageCell
            //: cellData = imageCell.cellData as! AutomaticallyCellData
            cellData = imageCell.cellData as! AutomaticallyCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ShowMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ShowMsgCell
            //: cellData = videoCell.cellData as! CurrentDateCellData
            cellData = videoCell.cellData as! CurrentDateCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: PerformanceThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! PerformanceThen
            //: cellData = audioCell.cellData as! NameCellData
            cellData = audioCell.cellData as! NameCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: UpMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! UpMsgCell
            //: cellData = textCell.cellData as! ListCellData
            cellData = textCell.cellData as! ListCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CurrentDateCellData.self) {
                if reCellData!.isKind(of: CurrentDateCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    dismissKey(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    project(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = DefaultChatManager.inputTitle(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        DefaultChatManager.temporary(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func pic(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(WatercourseThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(WatercourseThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if WatercourseThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if WatercourseThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = WatercourseThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = WatercourseThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = WatercourseThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = WatercourseThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func withMsg() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_indexModelName.replacingOccurrences(of: "wait", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g"))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.project(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func isPic(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: str_arrayName.map{trueTop(up: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: str_arrayName.map{trueTop(up: $0)}, encoding: .utf8)!: content, (String(str_toObjectData.prefix(5))): greetId, String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_indexModelName.replacingOccurrences(of: "wait", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: str_sizeContent.map{$0^105}, encoding: .utf8)!: content, (String(str_toObjectData.prefix(5))): greetId, (str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_makeWithValue) + str_textName.replacingOccurrences(of: "to", with: "g"))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.project(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            TaskObjectProtocol.shared.rein(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.detailKey(showMsg: String(bytes: str_visibleLabelData.map{modelArrayData(index: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        DefaultChatManager.panoramicView(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(str_toValue.replacingOccurrences(of: "frame", with: "th"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_kitText.replacingOccurrences(of: "top", with: "di")),
                               //: "audioId": greetId,
                               (String(str_requestName.suffix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: str_viewListName.map{aspectTitle(row: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(str_fillEditTitle) + String(str_colorGestureVideoText)): model.unZipFilePath,
                               //: "senderId": WatercourseThen.share.loginUserMode.userID]
                               String(bytes: str_lastDetailValue.map{$0^19}, encoding: .utf8)!: WatercourseThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(str_requestName.suffix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_viewListName.map{aspectTitle(row: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(str_fillEditTitle) + String(str_colorGestureVideoText)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_statusValue))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.project(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func theory(msgStr: String, quoteModel: ToModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            detailKey(showMsg: (str_imageResumeTitle + "send" + String(str_clearName.suffix(8)) + String(str_pushText)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: str_arrayName.map{trueTop(up: $0)}, encoding: .utf8)!: temStr, String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_indexModelName.replacingOccurrences(of: "wait", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: str_sizeContent.map{$0^105}, encoding: .utf8)!: temStr, (str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_makeWithValue) + str_textName.replacingOccurrences(of: "to", with: "g"))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(str_fillValue.lowercased() + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(str_gestureData))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? ToModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        project(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func sendInfo(img: UIImage, sendModel: CornerJsonModel) {
        //: let imagePath = AutomaticallyCellData.getChatImagePath(image: img)
        let imagePath = AutomaticallyCellData.blockEqual(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_underTitle.reversed(), encoding: .utf8)!: (String(str_picValue.prefix(6))), String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (String(str_colorRefreshTempName)), String(bytes: str_firstData.map{viewSucceed(index: $0)}, encoding: .utf8)!: imagePath, String(bytes: str_statusTitle.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(str_sessionValue)): imagePath,
                                        //: "type": "ImgMsg",
                                        (str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_picValue.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_shareTitle.map{$0^142}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_controlName.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        project(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func dismissKey(msgData: TUIMessageCellData?, sendModel: CornerJsonModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_underTitle.reversed(), encoding: .utf8)!: (String(str_modeData) + String(str_playerText.suffix(3))), String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_toModelInName.lowercased()), String(bytes: str_statusTitle.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_modeData) + String(str_playerText.suffix(3))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_managerValue.map{$0^210}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_userContent.map{modelCounterval(transform: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_areaTitle.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: str_shareTitle.map{$0^142}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(str_controlName.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.presentModel(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.presentModel(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        project(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func fixing(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_kitText.replacingOccurrences(of: "top", with: "di")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_viewListName.map{aspectTitle(row: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(str_fillEditTitle) + String(str_colorGestureVideoText)): voicePath,
                       //: "senderId": WatercourseThen.share.loginUserMode.userID]
                       String(bytes: str_lastDetailValue.map{$0^19}, encoding: .utf8)!: WatercourseThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(str_fillEditTitle) + String(str_colorGestureVideoText)): voicePath,
                         //: "type": "AudioMsg",
                         (str_scaleName.replacingOccurrences(of: "touch", with: "e")): (String(str_statusValue)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: str_overText.map{someoneBack(voice: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        project(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func project(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailKey(showMsg: kLet_lengthFailTitle)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(str_layerToName.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CurrentDateCellData.self) == false && msgData.isKind(of: ErrorCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CurrentDateCellData.self) == false, msgData.isKind(of: ErrorCellData.self) == false, params[(str_scaleName.replacingOccurrences(of: "touch", with: "e"))] as? String != (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g")) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(str_sessionValue))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(str_sessionValue))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(str_sessionValue))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(str_scaleName.replacingOccurrences(of: "touch", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_statusValue)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(str_fillEditTitle) + String(str_colorGestureVideoText))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(str_fillEditTitle) + String(str_colorGestureVideoText))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(str_fillEditTitle) + String(str_colorGestureVideoText)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_labelValue.suffix(5)) + "ents/U" + String(str_addName.prefix(5)) + String(str_listName.prefix(5)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = kLet_noValue + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_appBottomBuildValue.suffix(4)) + "ment" + String(str_colorValue))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = kLet_scaleTitle + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(str_requestName.suffix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(str_kitText.replacingOccurrences(of: "top", with: "di"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: HumanHeadRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        HumanHeadRequestTool.rangeOf(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g")) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.direct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_likeContent)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(str_toObjectData.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    DefaultChatManager.panoramicView(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(str_commitTitle.prefix(8)) + str_picData.replacingOccurrences(of: "bottom", with: "e"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(str_commitTitle.prefix(8)) + str_picData.replacingOccurrences(of: "bottom", with: "e"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if ListThen.deleteItem().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: kLet_errorBottomValue, object: nil, userInfo: [String(bytes: str_deviceDataText.map{valueReturnCell(index: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.noteUse(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(str_effectName) + String(str_changeWithText.suffix(5)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g")) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!] = (String(str_ofName))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_informationTitle)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g")) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!] = (str_indexModelName.replacingOccurrences(of: "wait", with: "t"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(WatercourseThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(WatercourseThen.share.loginUserMode.userID)" + (String(str_acrossTitle) + String(str_maxData.suffix(6)) + "tTip" + String(str_arrowValue.suffix(7))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue, type != (String(str_backPathTitle.suffix(6)) + str_makeData.replacingOccurrences(of: "image", with: "g")) {
                //: self.removeReplyTipsView()
                self.videoView()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(str_todayData))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            DefaultChatManager.answer(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((str_makeTitle.replacingOccurrences(of: "manager", with: "to") + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(str_makeTitle.replacingOccurrences(of: "manager", with: "to") + "lMfCoin")] as! NSNumber
                //: WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(str_pointData.prefix(8)) + String(str_attributeName.prefix(4)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(str_pointData.prefix(8)) + String(str_attributeName.prefix(4)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.picPrivate(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(str_blockContent))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(str_blockContent))] as! [String: Any]
                //: let model = LabModel(dic: intimatePhotoDict)
                let model = LabModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((str_toModelInName.lowercased())) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(str_toModelInName.lowercased())] as! [String: Any]
                //: let model = ModeAddModel(dic: intimateVideoDict)
                let model = ModeAddModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        includeMsg()

        //: updateSession()
        myTip()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func myTip() {
        //: UtiliserThen.cache_updateSessionAndPostNotif(with: targetId)
        UtiliserThen.cacheStatus(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func direct(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard WatercourseThen.share.loginUserMode.status != 1 else {
            guard WatercourseThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    detailKey(showMsg: errorStr)
                }
                //: return
                return
            }

            //: IndoorsPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            IndoorsPushManager.share.rechargeVideo(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.imagePushAside()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = VoiceView(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
            //: view.show()
            view.devise()
            //: } else if errorCode == 60003, !WatercourseThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !WatercourseThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            AdministratorReactiveCompatible.shared.verification()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                detailKey(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func punchTap(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.noteUse(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func label() {
        //: if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.raw(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(WatercourseThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(WatercourseThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (kLet_timeContent.array(forKey: kLet_closedValue)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.beyondViewOp()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                kLet_timeContent.set(intimateCacheData, forKey: kLet_closedValue)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func totaleractionOn(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(str_modelTitle.lowercased())].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_canName) + String(str_touchMoreInputData))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.process(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func noteUse(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_canName) + String(str_touchMoreInputData))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_canName) + String(str_touchMoreInputData))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.process(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func process(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = UtiliserThen.cache_getCachedUserInfo(targetId: self.targetId)
        let model = UtiliserThen.liberalDemocratParty(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.raw(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.equalNumberimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.trademark(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.trademark(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: UtiliserThen.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        UtiliserThen.skipToTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: str_contentData.reversed(), encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_blockValue.map{everyTarget(user: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kLet_profileData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension RawControllerDelegate: TrisectViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func ignore() {
        //: guard WatercourseThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.signalingLabel(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func visualImage(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func overFirst(actionType: KeyScaleRepresentable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    pickPhotos()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    errorShowCell()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                pickPhotos()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if WatercourseThen.share.loginUserMode.sex == Gender.female.rawValue {
            if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if WatercourseThen.share.loginUserMode.loungePlus {
                if WatercourseThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.exhibitThroughActuate(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                errorShowCell()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    actionFunc()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    errorShowCell()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                actionFunc()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.sound(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func errorShowCell() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.detailKey(showMsg: String(bytes: str_contentName.map{addWith(direction: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func bequest(giftModel _: AdMeasurable, giftNum _: String, model _: AllCellTransformable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func sharedNum(giftModel: AdMeasurable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        legacy(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ErrorCellData?, fromStr: String?) {
    func legacy(giftModel: AdMeasurable, giftNum: String, cellData: ErrorCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailKey(showMsg: kLet_lengthFailTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(str_layerToName.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(str_controlName.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(str_toData.replacingOccurrences(of: "lock", with: "i") + String(str_photoData.suffix(5)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_targetName.prefix(4)) + "temsetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(str_labelName.replacingOccurrences(of: "position", with: "ro"))] = fromStr
        }
        // 送礼接口
        //: HumanHeadRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        HumanHeadRequestTool.imageCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.direct(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: kLet_countTitle)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.roundLagniappe(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.isochronal(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.myTip()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_canName) + String(str_touchMoreInputData))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_canName) + String(str_touchMoreInputData))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.process(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_videoErrValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func giveawayLoad(giftModel: AdMeasurable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_deviceTitleScreenName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailKey(showMsg: kLet_lengthFailTitle)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= WatercourseThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            errorShowCell()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(str_countData.prefix(6)) + "ftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(str_scaleName.replacingOccurrences(of: "touch", with: "e"))] = (String(str_partyData.prefix(6)) + "ftMsg")

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (String(str_ofName))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(str_dataName.prefix(5)) + str_modelContent.lowercased()): msgInfo, String(bytes: str_rowName.map{micUniversal(text: $0)}, encoding: .utf8)!: params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        project(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func commonTouch(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(kLet_bottomData - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func addBy(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func map(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.map(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                nearData(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func errorIcon() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if EqualBottomManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            EqualBottomManagerDelegate.shared.upwardlyPlayer()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func isochronal(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!] = (String(str_pathData))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_dataName.prefix(5)) + str_modelContent.lowercased())] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_commitTitle.prefix(8)) + str_picData.replacingOccurrences(of: "bottom", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_commitTitle.prefix(8)) + str_picData.replacingOccurrences(of: "bottom", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_todayData))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        writeDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((str_makeTitle.replacingOccurrences(of: "manager", with: "to") + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(str_makeTitle.replacingOccurrences(of: "manager", with: "to") + "lMfCoin")] as! NSNumber
            //: WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: WatercourseThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.infoTime(needReload: false, mf_coin: WatercourseThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func heading() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.isAdd(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func writeDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.designDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func upRequest(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.prepare(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func dealId() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func indexArea() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.donationComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.myTip()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func appSend() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.labDownPop { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.myTip()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func colorClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = FactoryReactiveCompatible()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        trimDown()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func dataClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ColorGiftDataSource(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.collectionList(cellTitleList: [(String(str_inputAddData.prefix(4)) + String(str_democratName)).localized, (String(str_endingContent.suffix(5))).localized, (String(str_popValue.prefix(5)) + String(str_frameTitle)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .ending()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: IndoorsPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                IndoorsPushManager.share.digitiserView(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                ingatheringMessage()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = CurrentDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.tip(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func ingatheringMessage() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = ButtonReactiveCompatible()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.valueUid(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.textApp(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func person() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            nearData(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            trimDown()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func decisionMaker(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = RequestMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.logSubReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func stochasticCell() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(execute(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: kLet_postPointData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(pushEqualList),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: kLet_partyFailValue,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func execute(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard WatercourseThen.share.loginUserMode.loungePlus == true else { return }
        guard WatercourseThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func pushEqualList() {
        //: WatercourseThen.share.loginUserMode.loungePlus = true
        WatercourseThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func duringConfidential() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if DefaultChatManager.constituentName(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.infoPoint(tipStr: (String(str_foundationData.prefix(6)) + "ead r" + str_loadContent.replacingOccurrences(of: "frame", with: "ce") + String(str_picName)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension RawControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func pickPhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ColorPermissionTool.stream(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AtProgressThen.busy(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.noImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func topViewHandle() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ColorPermissionTool.tincture(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.noImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func noImg(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = CornerJsonModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.sendInfo(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = EleventhHourPhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = CornerJsonModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.sendInfo(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        trimDown()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func actionFunc() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard DefaultChatManager.utilizerUp(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == PushDownStackScalarLiteral.male.rawValue ? "He".localized : (String(str_pageContent)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.detailKey(showMsg: (String(str_shareName.suffix(5)) + " usin" + String(str_appearTitle.suffix(6)) + "low " + String(str_fromName)).alongNext(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.fileOnPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func fileOnPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = AtProgressThen.busy(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            AtProgressThen.beforeName(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.middleText(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.detailKey(showMsg: (String(str_makeTextElseValue)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func middleText(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: RawTalkingEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = HeterogenousViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = CornerJsonModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.dismissKey(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        AtAdView.toAction()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        trimDown()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func presentModel(msgData: TUIMessageCellData, sendModel: CornerJsonModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        KeyInsertListener.shared.success(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.detailKey(showMsg: (String(str_videoStatusContent.suffix(5)) + String(str_customValue.prefix(8))).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.dismissKey(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    //: func func__addVoiceNotification() {
    func addNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(thenPlayer),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(minutia),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(hearing(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(gestureNotification(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kLet_partyName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(totaleractionOn(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kLet_domainMeetingName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func shape(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            EqualBottomManagerDelegate.shared.bring()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            EqualBottomManagerDelegate.shared.removeOk()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func thenPlayer() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        shape(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func minutia() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        shape(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func hearing(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            shape(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            shape(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: NameCellData, cellView: TalkingChatAudioMsgCell) {
    func barShared(cellData: NameCellData, cellView: PerformanceThen) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard NameSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailKey(showMsg: kLet_networkViewTitle)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: NameCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: NameCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! NameCellData
            let audioModel = indexModel as! NameCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = UtiliserThen.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = UtiliserThen.link(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        EqualBottomManagerDelegate.shared.upwardlyPlayer()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if EqualBottomManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(str_sizeIconData)) + "\(EqualBottomManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if EqualBottomManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                EqualBottomManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        EqualBottomManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        EqualBottomManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: NameCellData.self) {
                if subModel.isKind(of: NameCellData.self) {
                    //: let audioModel = subModel as! NameCellData
                    let audioModel = subModel as! NameCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = UtiliserThen.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = UtiliserThen.link(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: FlushScaleMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FlushScaleMsgTable.domainTextMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        EqualBottomManagerDelegate.shared.nextPlay(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func farLoad() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.wearOff()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.isPic(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: NameCellData.self) == true || indexModel?.isKind(of: AutomaticallyCellData.self) == true || indexModel?.isKind(of: ListCellData.self) == true || indexModel?.isKind(of: CurrentDateCellData.self) == true {
                if indexModel?.isKind(of: NameCellData.self) == true || indexModel?.isKind(of: AutomaticallyCellData.self) == true || indexModel?.isKind(of: ListCellData.self) == true || indexModel?.isKind(of: CurrentDateCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.includeMsg()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.funcOrBeak()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.duringConfidential()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func wearOff() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.ofData(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.locationIn(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.label()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == WatercourseThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == WatercourseThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == WatercourseThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == WatercourseThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.textApp(userWrap)
    }

    //: func func__getFillDraftText() {
    func pubThroughParagraph() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_objectData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.ignore()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func draftTo() {
        //: let userID = "c2c_"+targetId
        let userID = (String(str_objectData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    //: func func__TipsNotification() {
    func systemAdd() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(outputNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_intervalPlayTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func outputNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: HandbillThen  = userinfo["msg"] as! HandbillThen
        let celldata: HandbillThen = userinfo[(String(str_resultValue))] as! HandbillThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func infoPoint(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: str_arrayName.map{trueTop(up: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: str_hideData.map{colorText(data: $0)}, encoding: .utf8)!: (str_arrayText.replacingOccurrences(of: "block", with: "s"))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func funcOrBeak() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        DefaultChatManager.detailHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.infoPoint(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func picPrivate(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.infoPoint(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func includeMsg() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        DefaultChatManager.titleTool(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.infoPoint(tipStr: String(bytes: str_sizeToTitle.map{$0^128}, encoding: .utf8)!.alongNext(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func gestureNotification(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_addressValue.prefix(6)) + str_lengthFinishValue.replacingOccurrences(of: "video", with: "a"))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension RawControllerDelegate: DowerTipsProtocol {
    //: func func__setReplyTipsView() {
    func modeControl() {
        //: let key =  "\(WatercourseThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(WatercourseThen.share.loginUserMode.userID)" + (String(str_acrossTitle) + String(str_maxData.suffix(6)) + "tTip" + String(str_arrowValue.suffix(7))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.appTips()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            miniskirt(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            videoView()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func occurModel(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        theory(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        videoView()
    }

    //: func removeReplyTipsView() {
    func videoView() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            miniskirt(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func addPlayerAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kLet_keyLiveDeviceData, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kLet_environmentText - kLet_bottomData, width: kLet_scaleName, height: kLet_environmentText)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kLet_scaleName,
                                     //: height: ScreenHeight)
                                     height: kLet_environmentText)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func nearData(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.toController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if DateRangeThen.portion().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_statusText, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: TalkingEqualControllerRecognizerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kLet_environmentText - kLet_bottomData,
                                     //: width: ScreenWidth,
                                     width: kLet_scaleName,
                                     //: height: ScreenHeight)
                                     height: kLet_environmentText)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if DateRangeThen.portion().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: kLet_statusText, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension RawControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func innumerableness() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_barValue) + String(str_clickName) + str_maleText.replacingOccurrences(of: "to", with: "a")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func computerSimulation() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? kLet_bottomData : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.light(width: contentView.width,
                              //: height: contentView.height,
                              height: contentView.height,
                              //: corners: [.topLeft, .topRight],
                              corners: [.topLeft, .topRight],
                              //: cornerRadii: .init(width: 8, height: 8))
                              cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_infoButtonValue : kLet_barUserName)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_partyKeyTitle)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLet_infoButtonValue : kLet_barUserName)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + kLet_partyKeyTitle)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + kLet_partyKeyTitle)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func actionTo() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.person()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.dataClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.overFirst(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_endName, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
