
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_pathText:[UInt8] = [0x54,0x59,0x54,0x5f,0x13,0x4e,0x5a,0x4f,0x50,0x5d,0x25,0x14,0xb,0x53,0x4c,0x5e,0xb,0x59,0x5a,0x5f,0xb,0x4d,0x50,0x50,0x59,0xb,0x54,0x58,0x5b,0x57,0x50,0x58,0x50,0x59,0x5f,0x50,0x4f]

fileprivate func backgroundResult(fill num: UInt8) -> UInt8 {
    let value = Int(num) + 21
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TalkingGroupChatTextMsgCellID" :*/
fileprivate let str_sexValue:String = "Talkshow return"
fileprivate let str_withTitle:String = "oupChaname to"
fileprivate let str_userModifyData:[Character] = ["t","T","e","x","t","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_sendTitle:[Character] = ["T","a","l","k","i","n","g"]
fileprivate let str_clickTitle:[Character] = ["C","h","a","t","I","m","a","g","e","M","s","g"]
fileprivate let str_bottomTitle:String = "CellIDnot video"

/*: "UnderbellyViewCell" :*/
fileprivate let str_pointRegularData:[Character] = ["T","a","l","k","i","n","g","G","r","o","u","p","C","h","a","t","G","i","f","t","M","s"]
fileprivate let str_colorTitle:[Character] = ["g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_voiceValue:String = "make to for hide labelTalk"
fileprivate let str_receiveName:String = "tTippush party"
fileprivate let str_viewValue:String = "case varellID"

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_filterValue:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a","g","e","C","e"]
fileprivate let str_currentData:String = "LL"
fileprivate let str_taskValue:String = "_ReuseIdarray board view"

/*: "roomId" :*/
fileprivate let str_endContent:String = "roomIdview live name view if"

/*: "type" :*/
fileprivate let str_hiddenRemoveValue:[UInt8] = [0x8e,0x83,0x8a,0x9f]

private func pathStart(deadline num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "extra" :*/
fileprivate let str_assetColorModelData:String = "extrbottom"

/*: "jumpKey" :*/
fileprivate let str_titleName:[UInt8] = [0x79,0x65,0x4b,0x70,0x6d,0x75,0x6a]

/*: "Send Gift" :*/
fileprivate let str_toText:String = "Send Gheight icon add sex"
fileprivate let str_appData:[Character] = ["i","f","t"]

/*: "View info" :*/
fileprivate let str_positionViewData:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Report" :*/
fileprivate let str_maxValue:String = "Reporttitle manager name"

/*: "uid" :*/
fileprivate let str_labIndexContent:[UInt8] = [0x2d,0x31,0x3c]

private func modusVivendi(result num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "name" :*/
fileprivate let str_beginValue:[UInt8] = [0xa1,0xae,0xa2,0xaa]

private func backgroundCell(accelerate num: UInt8) -> UInt8 {
    return num ^ 207
}

/*: "Can't send blank message" :*/
fileprivate let str_imageMediumName:String = "Can\'t"
fileprivate let str_ofTitle:[Character] = ["d"," ","b","l","a","n"]
fileprivate let str_keyColorValue:String = "k mescolor make data response"

/*: "user" :*/
fileprivate let str_customValue:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let str_inviteText:[UInt8] = [0x42,0x4e,0x4f,0x55,0x44,0x4f,0x55]

/*: "msgType" :*/
fileprivate let str_timeRunningTitle:String = "msgTypesize var color let"

/*: "txt" :*/
fileprivate let str_withUserData:String = "TXT"

/*: "isGroup" :*/
fileprivate let str_moreData:String = "isGropost in empty interval title"
fileprivate let str_toData:[Character] = ["u","p"]

/*: "jumps" :*/
fileprivate let str_equalValue:String = "jucollectionps"

/*: "message" :*/
fileprivate let str_endAddressContent:[UInt8] = [0x9a,0x92,0x84,0x84,0x96,0x90,0x92]

private func popMakeOn(entry num: UInt8) -> UInt8 {
    return num ^ 247
}

/*: "TxtMsg" :*/
fileprivate let str_gestureText:String = "var label let private dataTxtMsg"

/*: "toUid" :*/
fileprivate let str_sourceName:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let str_interactionData:String = "extension labelmsgIn"
fileprivate let str_cameraData:String = "fmodel"

/*: "params" :*/
fileprivate let str_textTitle:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let str_toolName:[UInt8] = [0x85,0x89,0x88,0x92,0x83,0x88,0x92,0xb2,0x9f,0x96,0x83]

private func visualCommunication(show num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "ImgMsg" :*/
fileprivate let str_ofText:String = "text"
fileprivate let str_holderData:String = "day false content letmgMsg"

/*: "img" :*/
fileprivate let str_hiddenEngineText:String = "imtime"

/*: "imageUri" :*/
fileprivate let str_modelValue:[UInt8] = [0x9a,0x9e,0x92,0x98,0x96,0x86,0xa3,0x9a]

fileprivate func viewSize(service num: UInt8) -> UInt8 {
    let value = Int(num) - 49
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let str_topPlayerTitle:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let str_interactionValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_playerName:String = "text next size else margintotalM"
fileprivate let str_viewContent:[Character] = ["f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let str_moreImageValue:String = "start leading type view listgiftId"

/*: "giftNum" :*/
fileprivate let str_capabilityValue:String = "giftNumindex index context"

/*: "pkgItemsetId" :*/
fileprivate let str_dateData:[Character] = ["p","k","g","I","t","e","m","s","e"]
fileprivate let str_clickName:[Character] = ["t","I","d"]

/*: "msg" :*/
fileprivate let str_addName:String = "atg"

/*: "tips" :*/
fileprivate let str_feedbackText:String = "ttextps"

/*: "gift" :*/
fileprivate let str_nameValue:String = "GIFT"

/*: "nickname" :*/
fileprivate let str_frameTitle:String = "nlistkna"
fileprivate let str_borderData:[Character] = ["m","e"]

/*: "headPic" :*/
fileprivate let str_modelTitle:[UInt8] = [0x7c,0x71,0x75,0x70,0x44,0x7d,0x77]

private func priceLoad(data num: UInt8) -> UInt8 {
    return num ^ 20
}

/*: "All Numbers" :*/
fileprivate let str_indexImageValue:String = "self value contentAll Nu"
fileprivate let str_myData:[Character] = ["m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let str_instanceText:[Character] = ["s","e","n","d","N"]
fileprivate let str_contentColorMakeName:String = "UM"

/*: "toUser" :*/
fileprivate let str_extraTitle:String = "super let icon kittoUser"

/*: "msgIncome" :*/
fileprivate let str_materialValue:[Character] = ["m","s","g","I","n","c","o","m"]
fileprivate let str_inputAfterName:String = "path"

/*: "isConnection" :*/
fileprivate let str_fromModelValue:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AllRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class AllRecognizerDelegate: MusicViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = FailModelType() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = WithConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = FailModelType.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_pathText.map{backgroundResult(fill: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WatercourseThen.share.appConfigMode.disableShootScreen {
            if WatercourseThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CommandResultThen(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        numerosityUi()
        //: createUIConstraints()
        addConstraints()
        //: bindInteraction()
        dropName()
        //: addTapGestureRecognizer()
        recognizer()
        //: func__chatRoom_insertTipMsg()
        effectNameMsg()
        //: chatSettings()
        safePeriod()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            messageIn()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.notPhoto(join: false)
        //: self.destroyTipsTimer()
        self.expenderShow()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(RefreshReactiveCompatible.self, forCellReuseIdentifier: (String(str_sexValue.prefix(4)) + "ingGr" + String(str_withTitle.prefix(6)) + String(str_userModifyData)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(ViewMessageCell.self, forCellReuseIdentifier: (String(str_sendTitle) + String(str_clickTitle) + String(str_bottomTitle.prefix(6))))
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(UnderbellyViewCell.self, forCellReuseIdentifier: (String(str_pointRegularData) + String(str_colorTitle)))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(NetworkMsgCell.self, forCellReuseIdentifier: (String(str_voiceValue.suffix(4)) + "ingCha" + String(str_receiveName.prefix(4)) + "sMsgC" + String(str_viewValue.suffix(5))))
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_filterValue) + str_currentData.lowercased() + String(str_taskValue.prefix(8))))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: MoonshineViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = MoonshineViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: TitleAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TitleAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: LightTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = LightTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: ManagingDirectorReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = ManagingDirectorReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: MeView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = MeView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func safePeriod() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        ReloadThen.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        notPhoto(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func notPhoto(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(str_endContent.prefix(6))): self.conversationData.groupID, String(bytes: str_hiddenRemoveValue.map{pathStart(deadline: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: HumanHeadRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        HumanHeadRequestTool.gestureCompletion(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - TMessageControllerDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension AllRecognizerDelegate: TMessageControllerDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        //: if data.isKind(of: MixedCellData.self) {
        if data.isKind(of: MixedCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = RefreshReactiveCompatible(style: .default, reuseIdentifier: (String(str_sexValue.prefix(4)) + "ingGr" + String(str_withTitle.prefix(6)) + String(str_userModifyData)))
            //: let cellData = data as! MixedCellData
            let cellData = data as! MixedCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.asView(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: AutomaticallyCellData.self) {
        if data.isKind(of: AutomaticallyCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = ViewMessageCell(style: .default, reuseIdentifier: (String(str_sendTitle) + String(str_clickTitle) + String(str_bottomTitle.prefix(6))))
            //: let cellData = data as! AutomaticallyCellData
            let cellData = data as! AutomaticallyCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AssociationDataChatTalkingGiftCellThen.self) {
        if data.isKind(of: AssociationDataChatTalkingGiftCellThen.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = UnderbellyViewCell(style: .default, reuseIdentifier: (String(str_pointRegularData) + String(str_colorTitle)))
            //: let cellData = data as! AssociationDataChatTalkingGiftCellThen
            let cellData = data as! AssociationDataChatTalkingGiftCellThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: HandbillThen.self) {
        if data.isKind(of: HandbillThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = NetworkMsgCell(style: .default, reuseIdentifier: (String(str_voiceValue.suffix(4)) + "ingCha" + String(str_receiveName.prefix(4)) + "sMsgC" + String(str_viewValue.suffix(5))))
            //: let cellData = data as! HandbillThen
            let cellData = data as! HandbillThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(WatercourseThen.share.loginUid) {
            if userModel.uid == Int(WatercourseThen.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                asView(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                streetSmartUser(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ViewMessageCell.self) { // 图片
            //: let imageCellData = cell.messageData as! AutomaticallyCellData
            let imageCellData = cell.messageData as! AutomaticallyCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(kLet_seatContent)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = OnModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = CellMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [OnModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = EleventhHourPhotosVc(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.toController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: RefreshReactiveCompatible.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! RefreshReactiveCompatible
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_assetColorModelData.replacingOccurrences(of: "bottom", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_titleName.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NameModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: IndoorsPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    IndoorsPushManager.share.kind(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: NetworkMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! NetworkMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(str_assetColorModelData.replacingOccurrences(of: "bottom", with: "a"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: str_titleName.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<NameModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: IndoorsPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    IndoorsPushManager.share.kind(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        breathalyzer(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func expenderShow() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: NamePathModel) {
    private func streetSmartUser(user: NamePathModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ColorGiftDataSource(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.collectionList(cellTitleList: [(String(str_toText.prefix(6)) + String(str_appData)).localized, "@", (String(str_positionViewData)).localized, (String(str_maxValue.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.systemCard(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.videoEqual(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.asView(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.informIn(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: NamePathModel) {
    private func systemCard(user: NamePathModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.tvBtn()
        //: let model = TalkingChatRoomMemberModel()
        let model = AllCellTransformable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.tab(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: NamePathModel) {
    private func videoEqual(user: NamePathModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: str_labIndexContent.map{modusVivendi(result: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: str_beginValue.map{backgroundCell(accelerate: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.receiveAll(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func asView(uid: Int) {
        //: IndoorsPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        IndoorsPushManager.share.digitiserView(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func informIn(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = CurrentDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.tip(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func theory(msgStr: String, quoteModel _: ToModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            detailKey(showMsg: (str_imageMediumName + " sen" + String(str_ofTitle) + String(str_keyColorValue.prefix(5)) + "sage").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: str_titleName.reversed(), encoding: .utf8)!: (String(str_customValue)), String(bytes: str_labIndexContent.map{modusVivendi(result: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: str_inviteText.map{$0^33}, encoding: .utf8)!: temStr, (String(str_timeRunningTitle.prefix(7))): (str_withUserData.lowercased()), (String(str_moreData.prefix(5)) + String(str_toData)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(str_equalValue.replacingOccurrences(of: "collection", with: "m"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: str_endAddressContent.map{popMakeOn(entry: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: str_hiddenRemoveValue.map{pathStart(deadline: $0)}, encoding: .utf8)!: (String(str_gestureText.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_endContent.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(str_sourceName))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_interactionData.suffix(5)) + str_cameraData.replacingOccurrences(of: "model", with: "o")): msgInfo, String(bytes: str_textTitle.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        breathalyzer(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func listImg(img: UIImage) {
        //: let imagePath = AutomaticallyCellData.getChatImagePath(image: img)
        let imagePath = AutomaticallyCellData.blockEqual(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: str_toolName.map{visualCommunication(show: $0)}, encoding: .utf8)!: (str_ofText.replacingOccurrences(of: "text", with: "I") + String(str_holderData.suffix(5))), (String(str_timeRunningTitle.prefix(7))): (str_hiddenEngineText.replacingOccurrences(of: "time", with: "g")), String(bytes: str_modelValue.map{viewSize(service: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(str_topPlayerTitle)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: str_hiddenRemoveValue.map{pathStart(deadline: $0)}, encoding: .utf8)!: (str_ofText.replacingOccurrences(of: "text", with: "I") + String(str_holderData.suffix(5))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(str_endContent.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(str_interactionData.suffix(5)) + str_cameraData.replacingOccurrences(of: "model", with: "o")): msgInfo, String(bytes: str_textTitle.reversed(), encoding: .utf8)!: paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        breathalyzer(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func breathalyzer(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailKey(showMsg: kLet_lengthFailTitle)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(str_topPlayerTitle))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(str_topPlayerTitle))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(str_topPlayerTitle))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: HumanHeadRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        HumanHeadRequestTool.adminView(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.discussCustomResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(str_interactionData.suffix(5)) + str_cameraData.replacingOccurrences(of: "model", with: "o"))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(str_interactionValue))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_playerName.suffix(6)) + String(str_viewContent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_playerName.suffix(6)) + String(str_viewContent))] as! NSNumber
                //: WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func discussCustomResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard WatercourseThen.share.loginUserMode.status != 1 else {
            guard WatercourseThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    detailKey(showMsg: errorStr)
                }
                //: return
                return
            }

            //: IndoorsPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            IndoorsPushManager.share.rechargeVideo(clickEvent: kLet_countTitle, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.imagePushAside()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            selected(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = VoiceView(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
            //: view.show()
            view.devise()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                detailKey(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension AllRecognizerDelegate: TrisectViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func giveawayLoad(giftModel _: AdMeasurable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func ignore() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func visualImage(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = CommandViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.receiveAll(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func overFirst(actionType: KeyScaleRepresentable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            choice()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func sharedNum(giftModel _: AdMeasurable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func bequest(giftModel: AdMeasurable, giftNum: String, model: AllCellTransformable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            detailKey(showMsg: kLet_lengthFailTitle)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(str_sourceName))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(str_moreImageValue.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(str_capabilityValue.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(str_endContent.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_dateData) + String(str_clickName))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: HumanHeadRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        HumanHeadRequestTool.showData(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.discussCustomResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.roundLagniappe(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.cellClean(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func commonTouch(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func addBy(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func map(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.map(tap)
    }

    //: func func__starRecordBtnDown() {}
    func errorIcon() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func fixing(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension AllRecognizerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func choice() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        ColorPermissionTool.stream(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = AtProgressThen.busy(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: IndoorsPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                IndoorsPushManager.share.queryEqual()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.listImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func skinColourFrom() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        ColorPermissionTool.tincture(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.listImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func backMisconduct(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(str_endContent.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    //: func func__TipsNotification() {
    func funcForNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(listRow(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kLet_intervalPlayTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func listRow(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: HandbillThen  = userinfo["msg"] as! HandbillThen
        let celldata: HandbillThen = userinfo[(str_addName.replacingOccurrences(of: "at", with: "ms"))] as! HandbillThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func selected(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: str_inviteText.map{$0^33}, encoding: .utf8)!: tipStr, (String(str_timeRunningTitle.prefix(7))): (str_feedbackText.replacingOccurrences(of: "text", with: "i"))]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func effectNameMsg() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard WatercourseThen.share.appUserConfigMode.rTips.count > 0 else { return }
            guard WatercourseThen.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: WatercourseThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.selected(tipStr: WatercourseThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func cellClean(extral: [String: Any?], model: AllCellTransformable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(str_interactionData.suffix(5)) + str_cameraData.replacingOccurrences(of: "model", with: "o"))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(str_moreData.prefix(5)) + String(str_toData))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(str_timeRunningTitle.prefix(7)))] = (str_nameValue.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(str_interactionData.suffix(5)) + str_cameraData.replacingOccurrences(of: "model", with: "o"))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: str_labIndexContent.map{modusVivendi(result: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (str_frameTitle.replacingOccurrences(of: "list", with: "ic") + String(str_borderData)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: str_modelTitle.map{priceLoad(data: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(str_indexImageValue.suffix(6)) + String(str_myData)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((str_nameValue.lowercased())) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(str_nameValue.lowercased())] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(str_frameTitle.replacingOccurrences(of: "list", with: "ic") + String(str_borderData))] = "\(giftdic[(String(str_instanceText) + str_contentColorMakeName.lowercased())] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(str_extraTitle.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_materialValue) + str_inputAfterName.replacingOccurrences(of: "path", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_materialValue) + str_inputAfterName.replacingOccurrences(of: "path", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(str_interactionValue))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        disappearBuildTime(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_playerName.suffix(6)) + String(str_viewContent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_playerName.suffix(6)) + String(str_viewContent))] as! NSNumber
            //: WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            WatercourseThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: WatercourseThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.infoTime(needReload: false, mf_coin: WatercourseThen.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.keyInfo()
    }

    //: func initCachEffectData() {
    func messageIn() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.isAdd(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func disappearBuildTime(giftDic: [String: Any], model: AllCellTransformable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.buttonTo(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ExaggerateReactiveCompatible.share.noneBlock() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LogTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.funcAppear(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.designDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func observerArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in ExaggerateReactiveCompatible.share.noneBlock() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [LogTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.funcAppear(model: giftModel as! EnableLabelTransformable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.prepare(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension AllRecognizerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func numerosityUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func addConstraints() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(kLet_barUserName)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(kLet_barUserName)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - kLet_partyKeyTitle)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func dropName() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = CommandViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! TUIMessageCellData).isKind(of: MixedCellData.self) {
                    if (cellData as! TUIMessageCellData).isKind(of: MixedCellData.self) {
                        //: let groupCellData = cellData as! MixedCellData
                        let groupCellData = cellData as! MixedCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(str_interactionValue))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: TalkingV2Listener.shared.rx
        TalkingV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(str_fromModelValue)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
