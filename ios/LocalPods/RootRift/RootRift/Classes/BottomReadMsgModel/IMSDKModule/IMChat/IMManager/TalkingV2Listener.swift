
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let str_controlText:[Character] = ["I","M","退"]
fileprivate let str_listToName:[Character] = ["出","登","\u{5f55}","失","败","，","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let str_infoValue:String = ", deshidden lab"
fileprivate let str_indexData:String = "make normal input pushc:"

/*: ." :*/
fileprivate let str_insertData:String = "."

/*: "IM登录失败，code: :*/
fileprivate let str_nameDataTitle:[Character] = ["I","M","登","录","失","败","\u{ff0c}","c","o","d"]
fileprivate let str_stackWithTitle:[Character] = ["e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let str_editData:String = "IM获取\u{672a}读\u{6d88}\u{606f}"
fileprivate let str_makeVideoText:String = "总\u{6570}失败，"
fileprivate let str_cellValue:String = "sound cellcode:"

/*: "crushBadge" :*/
fileprivate let str_userTitle:String = "makerush"
fileprivate let str_rangeName:String = "Badgeself if fill stop"

/*: "badNumber" :*/
fileprivate let str_infoTitle:[UInt8] = [0x7b,0x78,0x7d,0x57,0x6c,0x74,0x7b,0x7c,0x6b]

private func putManager(hidden num: UInt8) -> UInt8 {
    return num ^ 25
}

/*: "plaza" :*/
fileprivate let str_countTitle:String = "pllabela"

/*: "roomNum" :*/
fileprivate let str_errorValue:String = "event first view ofroomNum"

/*: "num" :*/
fileprivate let str_equalValue:String = "pathum"

/*: "onlyOneRoomId" :*/
fileprivate let str_withName:String = "true equal translate let erroronlyO"

/*: "LIVE-CHAT" :*/
fileprivate let str_sourceName:[Character] = ["L","I"]
fileprivate let str_wrapData:[Character] = ["V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let str_showContent:[Character] = ["P","A","R","T","Y","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let str_languageValue:String = "apptra"

/*: "msgType" :*/
fileprivate let str_labelName:String = "msgTypepoint action error"

/*: "newPkgGift" :*/
fileprivate let str_phoneToValue:String = "detail listnewP"

/*: "MF:ChatMentionMsg" :*/
fileprivate let str_selectedValueNameContent:[Character] = ["M","F",":","C","h","a","t"]
fileprivate let str_objectData:String = "manager var equalMe"
fileprivate let str_popName:String = "ntigift"

/*: "uid" :*/
fileprivate let str_inputFrameText:[UInt8] = [0xf1,0xed,0xe0]

private func extensionVideo(view num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "onlineStatus" :*/
fileprivate let str_voiceValue:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let str_addName:[UInt8] = [0x29,0x33,0xe,0x25,0x37,0x15,0x33,0x25,0x32]

private func letVar(send num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "c2c_ :*/
fileprivate let str_buttonContent:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let str_makeModeName:String = "IM获取view custom"
fileprivate let str_topTitle:[Character] = ["n","v","I","d",":"]

/*: , code: :*/
fileprivate let str_errorName:String = "view size, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let str_giftValue:String = "IM插\u{5165}"
fileprivate let str_toText:String = "argetIcontent the index record define"
fileprivate let str_cellTitle:String = "d:model manager select view write"

/*: "changeDirection" :*/
fileprivate let str_editName:[Character] = ["c","h","a","n","g"]
fileprivate let str_colorName:String = "eDirestatus size request make"

/*: "rYMsgType" :*/
fileprivate let str_formatName:String = "rYMsas end pic true"

/*: "GJ:CallCustom" :*/
fileprivate let str_imageValue:[Character] = ["G","J",":","C","a","l","l","C","u"]
fileprivate let str_inputValue:[Character] = ["s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let str_pleaseData:[Character] = ["I","M","获","取","\u{4f1a}","话","\u{5217}","表","失","败","，","c"]
fileprivate let str_featherTitle:[Character] = ["o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let str_formerOfContent:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let str_infoText:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let str_cellActualData:String = "up thegroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let str_colorData:[Character] = ["I","M","删","除","会","话"]
fileprivate let str_ofName:String = "manager败"
fileprivate let str_equalText:[Character] = ["\u{ff0c}","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let str_leadingContent:String = "info"
fileprivate let str_shareText:String = "置顶会话\u{5931}"

/*: "SDK 正在连接到服务器" :*/
fileprivate let str_gestureName:[Character] = ["S","D","K"," ","\u{6b63}"]
fileprivate let str_valueText:[Character] = ["\u{5728}","连","接","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let str_viewValue:[Character] = ["S","D","K"," ","已","经","\u{6210}","功","\u{8fde}","接","到","服","\u{52a1}","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let str_borderEqualName:String = "IM连接on skin bag return"
fileprivate let str_localValue:String = "view back click，code:"

/*: , error: :*/
fileprivate let str_sizeText:String = "model extension error model var, error:"

/*: "IM账号被挤下线。" :*/
fileprivate let str_showTitle:String = "need"
fileprivate let str_quoteData:[Character] = ["账","号","被","\u{6324}","下","线","\u{3002}"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let str_pathData:[UInt8] = []

private func scaleOfMeasurement(size num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "Offline Notification" :*/
fileprivate let str_titleViewValue:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i"]
fileprivate let str_transactionManagerData:[Character] = ["c","a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let str_touchValue:String = "var actionCancel"

/*: "Re Login" :*/
fileprivate let str_methodValue:[Character] = ["R"]
fileprivate let str_coverName:String = "e Loginstatus ending"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TalkingV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol NameManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func coriolisEffect(data: [WithConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func explainUp(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func colorful(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum QualityAdDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class TalkingV2Listener: NSObject {
@objcMembers public class TalkingV2Listener: NSObject {
    //: public static let shared = TalkingV2Listener()
    public static let shared = TalkingV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [WithConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [WithConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [WithConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [WithConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [WithConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: WithConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.master()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func infoInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(kLet_recordAskNoName, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func master() {
        //: func__removeAllDelegate()
        handleInputPoke()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.exitEmpty()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func exitEmpty() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = WithConversationModel.equalEnable(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = WithConversationModel.equalEnable(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func catalogueAction() -> [WithConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if WatercourseThen.share.appStatus == AppSkinStatus.special.rawValue {
        if WatercourseThen.share.appStatus == PublishWidthTotal.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == ListMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == ListMacroDefine.storageMediumId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension TalkingV2Listener {
extension TalkingV2Listener {
    //: func func__removeAllDelegate() {
    func handleInputPoke() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func modelDelegate(_ delegate: NameManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func performance(_ delegate: NameManagerDelegate) {
        //: self.func__invokeDelegate {
        self.invoke {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func invoke(_ invocation: (NameManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! NameManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension TalkingV2Listener {
extension TalkingV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func modelDownItem() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: EnableReactiveCompatible.nameBy(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            EnableReactiveCompatible.nameBy(msg: (String(str_controlText) + String(str_listToName)) + "\(code)" + (String(str_infoValue.prefix(5)) + String(str_indexData.suffix(2))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.master()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func mutualCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = WatercourseThen.share.appUserConfigMode.uSig
        let uSigStr = WatercourseThen.share.appUserConfigMode.uSig
        //: let uidStr = WatercourseThen.share.loginUserMode.userID
        let uidStr = WatercourseThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.showPhoto()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EnableReactiveCompatible.nameBy(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            EnableReactiveCompatible.nameBy(msg: (String(str_nameDataTitle) + String(str_stackWithTitle)) + "\(code)" + (String(str_infoValue.prefix(5)) + String(str_indexData.suffix(2))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func agendum() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || WatercourseThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || WatercourseThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.detailKey(showMsg: kLet_lengthFailTitle)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension TalkingV2Listener {
extension TalkingV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func showPhoto() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EnableReactiveCompatible.nameBy(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            EnableReactiveCompatible.nameBy(msg: (str_editData + str_makeVideoText + String(str_cellValue.suffix(5))) + "\(code)" + (String(str_infoValue.prefix(5)) + String(str_indexData.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func showAcross() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: HumanHeadRequestTool.req_sysMessageList { _, result in
        HumanHeadRequestTool.timeUnitStack { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(str_userTitle.replacingOccurrences(of: "make", with: "c") + String(str_rangeName.prefix(5)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kLet_playTitleValue, object: self, userInfo: [String(bytes: str_infoTitle.map{putManager(hidden: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func willRemove(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: HumanHeadRequestTool.req_getChatRoomInfo { succeed, result, _ in
        HumanHeadRequestTool.toPlayer { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(str_countTitle.replacingOccurrences(of: "label", with: "az"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(str_errorValue.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = WithConversationModel.equalEnable(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(str_equalValue.replacingOccurrences(of: "path", with: "n"))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(str_withName.suffix(5)) + "neRoomId")] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension TalkingV2Listener: V2TIMAdvancedMsgListener {
extension TalkingV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(str_sourceName) + String(str_wrapData))) || msg.groupID.contains((String(str_showContent)))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            IndexCommandThen.statusInfoWith().modelColor(msg: msg)
            //: return
            return
        }
        //: if msg.sender == WatercourseThen.share.loginUserMode.userID { return }
        if msg.sender == WatercourseThen.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(str_languageValue.replacingOccurrences(of: "app", with: "ex"))] as! [AnyHashable: Any]
        //: let msgModel = FrameJsonModel(dic: extraDic)
        let msgModel = FrameJsonModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kLet_domainMeetingName, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(str_labelName.prefix(7)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(str_phoneToValue.suffix(4)) + "kgGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_systemScreenTitle, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: FlushScaleMsgTable.db_inserVoiceMsgToDB(dic)
                FlushScaleMsgTable.date(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.totalimateHoldOpenJudgeis(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            InsularThen.shared.sharedRequestDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            InsularThen.shared.chat(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(str_selectedValueNameContent) + String(str_objectData.suffix(2)) + str_popName.replacingOccurrences(of: "gift", with: "o") + "nMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            ReloadThen.share.quantitativeRelation(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: DefaultChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: DefaultChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != ListMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != ListMacroDefine.suspend() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.invoke { $0.colorful?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ReadPerformanceThen.shared.tableAdd(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: str_inputFrameText.map{extensionVideo(view: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: str_voiceValue.reversed(), encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: str_addName.map{letVar(send: $0)}, encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: kLet_formalText,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: UtiliserThen.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            UtiliserThen.priceAcrossSize(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            DefaultChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: kLet_postPointData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func addPrivete(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.funcStartPrivete(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(str_buttonContent)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.nonmodern(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.tableList(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                EnableReactiveCompatible.nameBy(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func funcStartPrivete(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = WatercourseThen.share.loginUserMode.userID
        let senderStr = WatercourseThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                EnableReactiveCompatible.nameBy(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func totalimateHoldOpenJudgeis(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(str_editName) + String(str_colorName.prefix(5)) + "ction")].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && IndoorsPushManager.share.queryEqual()!.isKind(of: RawControllerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kLet_numberContent) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kLet_numberContent)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension TalkingV2Listener: V2TIMConversationListener {
extension TalkingV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.nonmodern(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.tableList(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.nonmodern(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.tableList(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: WatercourseThen.share.unreadMessageNum = Int(totalUnreadCount)
        WatercourseThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.invoke {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.explainUp?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func tableList(conVersationList: [WithConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if UtiliserThen.shared().needReqUidsSet != nil {
            if UtiliserThen.nameLogGesture().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: UtiliserThen.shared().needReqUidsSet!.formUnion(set)
                UtiliserThen.nameLogGesture().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.invoke {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.coriolisEffect?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func sharedCompletion(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [WithConversationModel] = []

        // V2TIMConversation->WithConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = WithConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.toModel(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [WithConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ReloadCommandInfoTable.barFor(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.characterSetModel(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension TalkingV2Listener {
extension TalkingV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func agendaItem(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.sharedCompletion(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.tableList(conVersationList: arr as? [WithConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EnableReactiveCompatible.nameBy(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            EnableReactiveCompatible.nameBy(msg: (String(str_pleaseData) + String(str_featherTitle)) + "\(code)" + (String(str_infoValue.prefix(5)) + String(str_indexData.suffix(2))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func viewApp(targetId: String) -> WithConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func nonmodern(convList: [V2TIMConversation]) -> [WithConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [WithConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = WithConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.toModel(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.characterSetModel(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func characterSetModel(convModel: WithConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.exitEmpty()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: QualityAdDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - WatercourseThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - WatercourseThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = UtiliserThen.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = UtiliserThen.liberalDemocratParty(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(str_languageValue.replacingOccurrences(of: "app", with: "ex"))][(String(str_formerOfContent))][(String(str_infoText))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.miniMake(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.miniMake(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func miniMake(convList: [WithConversationModel], dataType: QualityAdDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func materialType(targetId: String, dataType: QualityAdDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(str_buttonContent)) + "\(targetId)" : (String(str_cellActualData.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: UtiliserThen.db_removeRecord(with: targetId)
            UtiliserThen.streetSmart(with: targetId)
            //: FlushScaleMsgTable.db_deleteVoiceMsg(WatercourseThen.share.loginUserMode.userID, toUid: targetId)
            FlushScaleMsgTable.logByName(WatercourseThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.showPhoto()

            //: } fail: { code, desc in
        } fail: { code, desc in
            EnableReactiveCompatible.nameBy(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func withTop(convModel: WithConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: EnableReactiveCompatible.nameBy(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            EnableReactiveCompatible.nameBy(msg: (str_leadingContent.replacingOccurrences(of: "info", with: "IM") + str_shareText + "败，convId:") + "\(convModel.conversationID)" + (String(str_errorName.suffix(7))) + "\(code)" + (String(str_infoValue.prefix(5)) + String(str_indexData.suffix(2))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.miniMake(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.miniMake(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension TalkingV2Listener: V2TIMSDKListener {
extension TalkingV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(str_gestureName) + String(str_valueText)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(str_viewValue)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: EnableReactiveCompatible.nameBy(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        EnableReactiveCompatible.nameBy(msg: (String(str_borderEqualName.prefix(4)) + "服务\u{5668}失败" + String(str_localValue.suffix(6))) + "\(code)" + (String(str_sizeText.suffix(8))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: EnableReactiveCompatible.nameBy(msg: "IM账号被挤下线。")
        EnableReactiveCompatible.nameBy(msg: (str_showTitle.replacingOccurrences(of: "need", with: "IM") + String(str_quoteData)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        NameSocketManager.shared.sizeOff(userLoginOut: true)
        //: TalkingV2Listener.shared.isConnection = false
        TalkingV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (CurrentApplication.shared as! CurrentApplication).noneDoingce()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: str_pathData.map{scaleOfMeasurement(size: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ReloadAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.ar.rawValue || GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.showSize(fontSize: 13)
            config.rightFont = UIFont.showSize(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        AlongThen.harvest()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        AlongThen.harvest(title: (String(str_titleViewValue) + String(str_transactionManagerData)).localized, message: msg, leftBtnTitle: (String(str_touchValue.suffix(6))).localized, rightBtnTitle: (String(str_methodValue) + String(str_coverName.prefix(7))).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ListThen.deleteItem().plus()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            DateRangeThen.portion().scene()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            WhiteIndexReactiveCompatible.showCompletion { t in
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_screenPathName, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            WhiteIndexReactiveCompatible.view { t in
                //: if t {
                if t {
                    //: TalkingV2Listener.shared.func__LogingIn { _ in
                    TalkingV2Listener.shared.mutualCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if ListThen.deleteItem().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(ListThen.deleteItem().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    NameSocketManager.shared.lagniappe()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
