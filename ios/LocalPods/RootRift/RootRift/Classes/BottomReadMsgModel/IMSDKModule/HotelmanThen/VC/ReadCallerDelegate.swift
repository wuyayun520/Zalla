
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_modelTitle:[UInt8] = [0x38,0x3f,0x38,0x25,0x79,0x32,0x3e,0x35,0x34,0x23,0x6b,0x78,0x71,0x39,0x30,0x22,0x71,0x3f,0x3e,0x25,0x71,0x33,0x34,0x34,0x3f,0x71,0x38,0x3c,0x21,0x3d,0x34,0x3c,0x34,0x3f,0x25,0x34,0x35]

private func deletePart(the num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "uid" :*/
fileprivate let str_layerTouchText:[UInt8] = [0x15,0x9,0x4]

/*: "headPic" :*/
fileprivate let str_topText:[Character] = ["h"]
fileprivate let str_upName:String = "eadPiccontent now let frame true"

/*: "nickname" :*/
fileprivate let str_requestText:String = "text"
fileprivate let str_kickValue:String = "ICKNAME"

/*: "birthday" :*/
fileprivate let str_viewValue:String = "equalir"
fileprivate let str_colorTextData:String = "thdahidden"

/*: "videoUrl" :*/
fileprivate let str_editText:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let str_modeText:String = "infoo"
fileprivate let str_quickData:String = "calistion"

/*: "cmd" :*/
fileprivate let str_userBeginName:[UInt8] = [0xb9,0xb7,0xbe]

private func collectionKey(error num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "refuseCall" :*/
fileprivate let str_assetText:String = "corner"
fileprivate let str_objectPathValue:String = "efusende"

/*: "data" :*/
fileprivate let str_leadingName:[UInt8] = [0x86,0x83,0x96,0x83]

/*: "Call interruption" :*/
fileprivate let str_mData:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i","o"]
fileprivate let str_toTitle:String = "top"

/*: "stopCall" :*/
fileprivate let str_userTitle:String = "stopCalreason if"
fileprivate let str_removeTitle:[Character] = ["l"]

/*: "logId" :*/
fileprivate let str_taskMicText:[UInt8] = [0x6b,0x68,0x60,0x4e,0x63]

private func viewTo(manager num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "cancelCall" :*/
fileprivate let str_actionData:String = "value add aspect mincancelCa"
fileprivate let str_equalValue:String = "namename"

/*: "No answer" :*/
fileprivate let str_voiceValue:[Character] = ["N","o"]
fileprivate let str_valueLabUpData:String = " answerself view to name value"

/*: "duration" :*/
fileprivate let str_detailData:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let str_contentValue:[Character] = ["S"]
fileprivate let str_kickManagerTitle:[Character] = ["h","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let str_enableTitle:String = "image modelcancelSex"

/*: "He has hung up" :*/
fileprivate let str_sizeNameTitle:String = "button in make size userHe has hu"
fileprivate let str_messageMakeValue:[Character] = ["n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_appTimeValue:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let str_colorPathData:String = "stopSmake equal shadow self"
fileprivate let str_touchYouText:[Character] = ["e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let str_sharedText:[UInt8] = [0x92,0xb5,0xa8,0xae,0xbd,0xbd,0xb2,0xb8,0xb2,0xbe,0xb5,0xaf,0xfb,0xbc,0xb4,0xb7,0xbf,0xfb,0xb8,0xb4,0xb2,0xb5,0xa8,0xf7,0xfb,0xb8,0xba,0xb7,0xb7,0xfb,0xb3,0xba,0xa8,0xfb,0xbe,0xb5,0xbf,0xbe,0xbf]

private func modelBy(explain num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "The other hung up the phone" :*/
fileprivate let str_colorValue:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p"]
fileprivate let str_actionName:String = "hconversationne"

/*: "type" :*/
fileprivate let str_dateName:String = "TYPE"

/*: "jumpUrl" :*/
fileprivate let str_labelName:String = "jumpUrlraw user model"

/*: "Cancel" :*/
fileprivate let str_makeValue:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let str_interactionValue:String = "Get size to bottom equal menu"
fileprivate let str_iconValue:String = "intitle"

/*: "addBean" :*/
fileprivate let str_cornerValue:String = "addBecount size end"
fileprivate let str_sizePushText:[Character] = ["a","n"]

/*: "refuseSex" :*/
fileprivate let str_blockTitle:String = "REFUS"
fileprivate let str_barValue:[Character] = ["e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_normalName:String = "onAcceas top smart"
fileprivate let str_computeText:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let str_fromTitle:String = "ACC"
fileprivate let str_makeData:String = "eptCallshare lab let"

/*: "Calls are cancelled" :*/
fileprivate let str_beginName:String = "Calls if self bottom post"
fileprivate let str_allowData:String = "ancshare"
fileprivate let str_labData:String = "imageimageed"

/*: "The call has ended" :*/
fileprivate let str_sizeUserData:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," ","e","n"]
fileprivate let str_kindName:String = "viewd"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReadCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let kLet_errValue = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ReadCallerDelegate: MusicViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: PublishChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: FlushPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ColorMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        NameSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        NameSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        NameSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_modelTitle.map{deletePart(the: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! UnbeknownstRecognizerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.duringAdd()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            vocalisation()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.itemData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        openImage()
        //: setupSubViewsConstraint()
        cornerBar()
        //: initNotificationObserver()
        userAcross()
        //: req_callGetUserInfo()
        serrationInfo()
        //: timeoutRules()
        rules()
        //: resumeTalk()
        reStartItem()
        //: setUpBGMPlayer()
        nationalServicePlayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WatercourseThen.share.appConfigMode.disableShootScreen {
            if WatercourseThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CommandResultThen(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        NameSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.pack()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: GiftHeadCounteractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = GiftHeadCounteractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: TinctureCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = TinctureCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func serrationInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        TalkingCloudDelegate.misinformation(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(str_topText) + String(str_upName.prefix(6)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_requestText.replacingOccurrences(of: "text", with: "n") + str_kickValue.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_viewValue.replacingOccurrences(of: "equal", with: "b") + str_colorTextData.replacingOccurrences(of: "hidden", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_editText))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_modeText.replacingOccurrences(of: "info", with: "l") + str_quickData.replacingOccurrences(of: "list", with: "t"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.nameTo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func vocalisation() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ColorPermissionTool.withDeadlineAt(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailKey(showMsg: kLet_intimateText)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (str_assetText.replacingOccurrences(of: "corner", with: "r") + str_objectPathValue.replacingOccurrences(of: "send", with: "s") + "Call"), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.smallPop()
                //: return
                return
            }

            //: self.stopBGM()
            self.pastWith()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = FlushPlayer.dataForType(type: .Response)
            //: self.startBGM()
            self.duringAdd()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func pack() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.pastWith()
        //: TalkingSocketManager.shared.callerDelegate = nil
        NameSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        NameSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        NameSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func smallPop(animated: Bool = true) {
        //: self.stopBGM()
        self.pastWith()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func information() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.viewHide(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func reStartItem() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            information()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func userAcross() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(destroy(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(filmOrNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_objectContent,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_errValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func destroy(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            smallPop()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func filmOrNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard NameSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        NameSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        NameSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        NameSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.quitDay(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ReadCallerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.detailKey(showMsg: (String(str_mData) + str_toTitle.replacingOccurrences(of: "top", with: "n")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (String(str_userTitle.prefix(7)) + String(str_removeTitle)), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (String(str_actionData.suffix(8)) + str_equalValue.replacingOccurrences(of: "name", with: "l")), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (str_assetText.replacingOccurrences(of: "corner", with: "r") + str_objectPathValue.replacingOccurrences(of: "send", with: "s") + "Call"), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.flout()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                NameSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            smallPop()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.endue(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func nationalServicePlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? AutomaticallyColumnConvertible.Call : AutomaticallyColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = FlushPlayer.dataForType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func duringAdd() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.clickPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func pastWith() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.valueStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func rules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_deviceIndexViewData : self.chatModel?.digitizerShow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            scratch()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.scratch()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func scratch() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !NameSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.detailKey(showMsg: (String(str_voiceValue) + String(str_valueLabUpData.prefix(7))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (String(str_actionData.suffix(8)) + str_equalValue.replacingOccurrences(of: "name", with: "l")), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (str_assetText.replacingOccurrences(of: "corner", with: "r") + str_objectPathValue.replacingOccurrences(of: "send", with: "s") + "Call"), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        NameSocketManager.shared.migration(info: socketDict)
        //: popCurrentViewController()
        smallPop()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func sumercalate(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(str_detailData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.valueTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.holderStage()
            //: createTalkRoom()
            information()
            //: self.stopBGM()
            self.pastWith()
        }
    }
}

// MARK: - DateHeadObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ReadCallerDelegate: DateHeadObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func toData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_contentValue) + String(str_kickManagerTitle)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_enableTitle.suffix(9)))].stringValue == PushDownStackScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_sizeNameTitle.suffix(9)) + String(str_messageMakeValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        smallPop()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func messageAssemblage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.pastWith()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.holderStage()
        //: self.createTalkRoom()
        self.information()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func criminalRecord(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_appTimeValue))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_contentValue) + String(str_kickManagerTitle)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_colorPathData.prefix(5)) + String(str_touchYouText))].stringValue == PushDownStackScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_sizeNameTitle.suffix(9)) + String(str_messageMakeValue)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: str_sharedText.map{modelBy(explain: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_colorValue) + str_actionName.replacingOccurrences(of: "conversation", with: "o")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.tiedUpKey()
        //: self.popCurrentViewController()
        self.smallPop()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: kLet_messageTitle, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func tableGift(msg: String, data: [String: Any]) {
        //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(str_dateName.lowercased())] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_labelName.prefix(7)))] as? String else { return }
            //: let httpsUrl = WatercourseThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = WatercourseThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ReloadConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: IndoorsPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            IndoorsPushManager.share.feather(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AlongThen.harvest(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(str_makeValue)).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(str_interactionValue.prefix(4)) + "gold co" + str_iconValue.replacingOccurrences(of: "title", with: "s")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AlongThen.teachingMethod()
                //: } rightBlock: {
            } rightBlock: {
                //: IndoorsPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                IndoorsPushManager.share.rechargeVideo(clickEvent: kLet_deviceMaxVersionValue, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func applicationToAssemblage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_cornerValue.prefix(5)) + String(str_sizePushText))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.rawDataComponentAttractorAnimaExhibit(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.galaxy(point: addBean)
        }
    }
}

// MARK: - ManagingDirectorObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ReadCallerDelegate: ManagingDirectorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func actionTable(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_contentValue) + String(str_kickManagerTitle)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_blockTitle.lowercased() + String(str_barValue))].stringValue == PushDownStackScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_sizeNameTitle.suffix(9)) + String(str_messageMakeValue)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        smallPop()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func equalColor(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.pastWith()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.holderStage()
        //: self.createTalkRoom()
        self.information()
    }
}

// MARK: - LabelObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ReadCallerDelegate: LabelObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func loadDown(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_assetText.replacingOccurrences(of: "corner", with: "r") + str_objectPathValue.replacingOccurrences(of: "send", with: "s") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.detailKey(showMsg: (String(str_voiceValue) + String(str_valueLabUpData.prefix(7))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.flout()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                NameSocketManager.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.smallPop()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_normalName.prefix(6)) + String(str_computeText)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else { return }
            //: IndoorsPushManager.share.func__jumpToWebRecharge(sufficient: false)
            IndoorsPushManager.share.rechargeVideo(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_normalName.prefix(6)) + String(str_computeText)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailKey(showMsg: msg)
            //: self.popCurrentViewController()
            self.smallPop()
        }
    }
}

// MARK: - ContactObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ReadCallerDelegate: ContactObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func term() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (str_fromTitle.lowercased() + String(str_makeData.prefix(7))), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func freshman() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailKey(showMsg: (String(str_beginName.prefix(6)) + "are c" + str_allowData.replacingOccurrences(of: "share", with: "e") + str_labData.replacingOccurrences(of: "image", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (String(str_actionData.suffix(8)) + str_equalValue.replacingOccurrences(of: "name", with: "l")), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailKey(showMsg: (String(str_beginName.prefix(6)) + "are c" + str_allowData.replacingOccurrences(of: "share", with: "e") + str_labData.replacingOccurrences(of: "image", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (str_assetText.replacingOccurrences(of: "corner", with: "r") + str_objectPathValue.replacingOccurrences(of: "send", with: "s") + "Call"), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_layerTouchText.map{$0^96}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.detailKey(showMsg: (String(str_sizeUserData) + str_kindName.replacingOccurrences(of: "view", with: "de")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_userBeginName.map{collectionKey(error: $0)}, encoding: .utf8)!: (String(str_userTitle.prefix(7)) + String(str_removeTitle)), String(bytes: str_leadingName.map{$0^226}, encoding: .utf8)!: [String(bytes: str_taskMicText.map{viewTo(manager: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.tiedUpKey()
        //: self.popCurrentViewController()
        self.smallPop()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func maxObserver(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.changeState(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func duringModel(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.viewError(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func astatine() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        NameSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ColorMiniView.upIndex()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.toController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.want()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.transfusion(talkingTime)
        //: popCurrentViewController()
        smallPop()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func instanceTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.transfusion(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func instill(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.countercalateWithDowery(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ReadCallerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func openImage() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func cornerBar() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
