
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_deedText:[UInt8] = [0xe7,0xe0,0xe7,0xfa,0xa6,0xed,0xe1,0xea,0xeb,0xfc,0xb4,0xa7,0xae,0xe6,0xef,0xfd,0xae,0xe0,0xe1,0xfa,0xae,0xec,0xeb,0xeb,0xe0,0xae,0xe7,0xe3,0xfe,0xe2,0xeb,0xe3,0xeb,0xe0,0xfa,0xeb,0xea]

private func makeRed(present num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "uid" :*/
fileprivate let str_beforeContent:[UInt8] = [0x66,0x7a,0x77]

/*: "headPic" :*/
fileprivate let str_messageColorData:String = "hraw"
fileprivate let str_kitData:String = "self right import labeladPic"

/*: "nickname" :*/
fileprivate let str_dailyNameTitle:String = "nipairname"

/*: "birthday" :*/
fileprivate let str_blockTimeContentData:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let str_listTitle:String = "error share sharedvideoUrl"

/*: "cover" :*/
fileprivate let str_fieldNameToData:String = "cobottomr"

/*: "location" :*/
fileprivate let str_makeData:String = "luser"
fileprivate let str_appearNameValue:String = "linetion"

/*: "cmd" :*/
fileprivate let str_detailedModelValue:[UInt8] = [0x8d,0x97,0x8e]

fileprivate func errorCoverFor(response num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let str_makeValue:String = "refvideoe"
fileprivate let str_showData:String = "model collectionCall"

/*: "data" :*/
fileprivate let str_currentName:[UInt8] = [0x2a,0x2f,0x3a,0x2f]

private func agendaItem(fatal num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "Call interruption" :*/
fileprivate let str_sizeData:[Character] = ["C","a","l","l"," ","i","n"]
fileprivate let str_normalNeedName:String = "poerrup"

/*: "stopCall" :*/
fileprivate let str_equalMakeValue:String = "stopCpast top return color type"
fileprivate let str_bindMessageContent:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let str_errorValue:[UInt8] = [0xf8,0xfb,0xf3,0xd5,0xf0]

fileprivate func rightOmitDevice(content num: UInt8) -> UInt8 {
    let value = Int(num) + 116
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_tabData:String = "canctable"
fileprivate let str_loadData:String = "point model iconlCall"

/*: "No answer" :*/
fileprivate let str_effectData:String = "No tool height"
fileprivate let str_equalHeadLiveText:String = "ansfatalr"

/*: "duration" :*/
fileprivate let str_removeName:String = "durasmallon"

/*: "She has hung up" :*/
fileprivate let str_pathData:String = "info pop model topShe ha"
fileprivate let str_scaleName:String = "detail"

/*: "cancelSex" :*/
fileprivate let str_userSoundName:[Character] = ["c","a"]
fileprivate let str_fieldTitle:[Character] = ["n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let str_upData:String = "He haat change"

/*: "endType" :*/
fileprivate let str_managerLabelData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let str_startValue:String = "stopSexuser label"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let str_menuErrorName:[UInt8] = [0x58,0x7f,0x62,0x64,0x77,0x77,0x78,0x72,0x78,0x74,0x7f,0x65,0x31,0x76,0x7e,0x7d,0x75,0x31,0x72,0x7e,0x78,0x7f,0x62,0x3d,0x31,0x72,0x70,0x7d,0x7d,0x31,0x79,0x70,0x62,0x31,0x74,0x7f,0x75,0x74,0x75]

private func whiteSystem(price num: UInt8) -> UInt8 {
    return num ^ 17
}

/*: "The other hung up the phone" :*/
fileprivate let str_modelValue:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p"]
fileprivate let str_taskTitle:[Character] = ["h","o","n","e"]

/*: "type" :*/
fileprivate let str_generateTitle:String = "tytime"

/*: "jumpUrl" :*/
fileprivate let str_labelValue:String = "jumpUrlhalf quote"

/*: "Cancel" :*/
fileprivate let str_makeContent:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let str_hiddenTitle:String = "Get golview progress"
fileprivate let str_betweenData:[Character] = ["d"]
fileprivate let str_topName:String = " coinsfalse info fatal"

/*: "addBean" :*/
fileprivate let str_tableText:String = "addBeanmargin view user menu"

/*: "refuseSex" :*/
fileprivate let str_cellCountFrameName:String = "REFUS"

/*: "onAcceptCall" :*/
fileprivate let str_indexData:[Character] = ["o","n","A","c","c","e","p","t","C","a","l"]
fileprivate let str_pathTitle:String = "L"

/*: "acceptCall" :*/
fileprivate let str_valueTitle:String = "acctop"
fileprivate let str_dateEqualTitle:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let str_mainData:[Character] = ["C","a","l","l","s"," ","a"]
fileprivate let str_centerTitle:[Character] = ["r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_viewWrapData:String = "button remove let viewThe ca"
fileprivate let str_modeShareData:String = "ll manager extension if label image"
fileprivate let str_userValue:String = "has endedscale if let self send"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UpwardlyViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let kLet_busyValue = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class UpwardlyViewController: MusicViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: PublishChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: FlushPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: BottomMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: AidScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        NameSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        NameSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        NameSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_deedText.map{makeRed(present: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! UnbeknownstRecognizerDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.dioramaBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            auth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            big(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.actionData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        subviewsRemain()
        //: setupSubViewsConstraint()
        equalImage()
        //: initNotificationObserver()
        equalObserver()
        //: req_callGetUserInfo()
        appInfo()
        //: timeoutRules()
        timeout()
        //: setUpBGMPlayer()
        subChange()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.subStartOutGift()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if WatercourseThen.share.appConfigMode.disableShootScreen {
            if WatercourseThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = CommandResultThen(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        NameSocketManager.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.userFor()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: AdView = {
        //: let v = TalkingBGVideoView()
        let v = AdView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.rededicateSize(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: ToManagerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ToManagerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: InsideThen = {
        //: let v = TalkingVideoWindowView()
        let v = InsideThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kLet_scaleName - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if FactoryShowThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: kLet_environmentContent + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.lesseningTip()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: TalkingCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = TalkingCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func appInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        TalkingCloudDelegate.misinformation(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(str_messageColorData.replacingOccurrences(of: "raw", with: "e") + String(str_kitData.suffix(5)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_dailyNameTitle.replacingOccurrences(of: "pair", with: "ck"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(str_blockTimeContentData))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_listTitle.suffix(8)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(str_fieldNameToData.replacingOccurrences(of: "bottom", with: "ve"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_makeData.replacingOccurrences(of: "user", with: "o") + str_appearNameValue.replacingOccurrences(of: "line", with: "ca"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.have()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.giftStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func giftStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = GiftCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = GiftCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.chemicalElement(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.duringTargetFemale(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func auth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ColorPermissionTool.behindQuantityMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailKey(showMsg: kLet_intimateText)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "video", with: "us") + String(str_showData.suffix(4))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.sectionAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.subStartOutGift()
            //: self.stopBGM()
            self.disparity()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = FlushPlayer.dataForType(type: .Response)
            //: self.startBGM()
            self.dioramaBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func subStartOutGift() {
        //: self.videoManager.startPreview()
        self.videoManager.equalMatch()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func userFor() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.disparity()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        AutomaticallyThen.clear()
        //: TalkingSocketManager.shared.callerDelegate = nil
        NameSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        NameSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        NameSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func sectionAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.disparity()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func lesseningTip() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.countercalate()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.giftStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func parameter() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.trackView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.giftStatus()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.towardAll()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func towardAll() {
        //: guard WatercourseThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = AidScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.showInsideValue()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func equalObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(someNotif(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(makeBy(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kLet_tableText,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachability(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: kLet_objectContent,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        kLet_busyValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func someNotif(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            sectionAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func reachability(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard NameSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        NameSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        NameSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        NameSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.viewPermission()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func makeBy(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.giftStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension UpwardlyViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.detailKey(showMsg: (String(str_sizeData) + str_normalNeedName.replacingOccurrences(of: "po", with: "t") + "tion"))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (String(str_equalMakeValue.prefix(5)) + String(str_bindMessageContent)), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_tabData.replacingOccurrences(of: "table", with: "e") + String(str_loadData.suffix(5))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "video", with: "us") + String(str_showData.suffix(4))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.flout()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                NameSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            sectionAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func subChange() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? AutomaticallyColumnConvertible.Call : AutomaticallyColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = FlushPlayer.dataForType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func dioramaBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.clickPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func disparity() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.valueStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? kLet_deviceIndexViewData : self.chatModel?.digitizerShow()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            listen()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.listen()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func listen() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !NameSocketManager.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.detailKey(showMsg: (String(str_effectData.prefix(3)) + str_equalHeadLiveText.replacingOccurrences(of: "fatal", with: "we")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_tabData.replacingOccurrences(of: "table", with: "e") + String(str_loadData.suffix(5))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "video", with: "us") + String(str_showData.suffix(4))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        NameSocketManager.shared.migration(info: socketDict)
        //: popCurrentViewController()
        sectionAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func writtenMatter(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_removeName.replacingOccurrences(of: "small", with: "ti"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.recommenceConversation(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.behindStop()
            //: createTalkRoom()
            parameter()
            //: self.stopBGM()
            self.disparity()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func big(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.countercalate()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.rededicateSize(show: false)
        //: self.stopBGM()
        self.disparity()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.behindStop()
        //: self.createTalkRoom()
        self.parameter()
    }
}

// MARK: - DateHeadObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension UpwardlyViewController: DateHeadObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func toData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_pathData.suffix(6)) + "s hung " + str_scaleName.replacingOccurrences(of: "detail", with: "up")).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_userSoundName) + String(str_fieldTitle))].stringValue == PushDownStackScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_upData.prefix(5)) + "s hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        sectionAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func messageAssemblage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        big(logId: json[String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func criminalRecord(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(str_managerLabelData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_pathData.suffix(6)) + "s hung " + str_scaleName.replacingOccurrences(of: "detail", with: "up")).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(str_startValue.prefix(7)))].stringValue == PushDownStackScalarLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_upData.prefix(5)) + "s hung up").localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: str_menuErrorName.map{whiteSystem(price: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_modelValue) + String(str_taskTitle)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.atDismiss()
        //: popCurrentViewController()
        sectionAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func tableGift(msg: String, data: [String: Any]) {
        //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(str_generateTitle.replacingOccurrences(of: "time", with: "pe"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(str_labelValue.prefix(7)))] as? String else { return }
            //: let httpsUrl = WatercourseThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = WatercourseThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ReloadConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: IndoorsPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            IndoorsPushManager.share.feather(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            AlongThen.harvest(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(str_makeContent)).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(str_hiddenTitle.prefix(7)) + String(str_betweenData) + String(str_topName.prefix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                AlongThen.teachingMethod()
                //: } rightBlock: {
            } rightBlock: {
                //: IndoorsPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                IndoorsPushManager.share.rechargeVideo(clickEvent: kLet_deviceMaxVersionValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func portionSize(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.supplement(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func applicationToAssemblage(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(str_tableText.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.ego(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.colorPoint(point: addBean)
        }
    }
}

// MARK: - ManagingDirectorObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension UpwardlyViewController: ManagingDirectorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func actionTable(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_pathData.suffix(6)) + "s hung " + str_scaleName.replacingOccurrences(of: "detail", with: "up")).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(str_cellCountFrameName.lowercased() + "eSex")].stringValue == PushDownStackScalarLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_upData.prefix(5)) + "s hung up").localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.detailKey(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.flout()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            NameSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        sectionAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func equalColor(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        big(logId: json[String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - LabelObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension UpwardlyViewController: LabelObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func loadDown(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_makeValue.replacingOccurrences(of: "video", with: "us") + String(str_showData.suffix(4))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.detailKey(showMsg: (String(str_effectData.prefix(3)) + str_equalHeadLiveText.replacingOccurrences(of: "fatal", with: "we")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.flout()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                NameSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            sectionAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_indexData) + str_pathTitle.lowercased()) && errorNo == 101 { // 接听通话时，余额不足
            //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else { return }
            //: IndoorsPushManager.share.func__jumpToWebRecharge(sufficient: false)
            IndoorsPushManager.share.rechargeVideo(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_indexData) + str_pathTitle.lowercased()) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailKey(showMsg: msg)
            //: popCurrentViewController()
            sectionAnimated()
        }
    }
}

// MARK: - MagnitudeeractionViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension UpwardlyViewController: MagnitudeeractionViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func motion() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_valueTitle.replacingOccurrences(of: "to", with: "e") + "tCal" + String(str_dateEqualTitle)), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func atCounteraction() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailKey(showMsg: (String(str_mainData) + String(str_centerTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_tabData.replacingOccurrences(of: "table", with: "e") + String(str_loadData.suffix(5))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.detailKey(showMsg: (String(str_mainData) + String(str_centerTitle)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (str_makeValue.replacingOccurrences(of: "video", with: "us") + String(str_showData.suffix(4))), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_beforeContent.map{$0^19}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.detailKey(showMsg: (String(str_viewWrapData.suffix(6)) + String(str_modeShareData.prefix(3)) + String(str_userValue.prefix(9))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_detailedModelValue.map{errorCoverFor(response: $0)}, encoding: .utf8)!: (String(str_equalMakeValue.prefix(5)) + String(str_bindMessageContent)), String(bytes: str_currentName.map{agendaItem(fatal: $0)}, encoding: .utf8)!: [String(bytes: str_errorValue.map{rightOmitDevice(content: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        sectionAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func system(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.iconCurrent(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func equalAcross(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.rededicateSize(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.noMethod(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.giftStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func componentWindow() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        NameSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = BottomMiniView.searched()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.buildAction()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.issueRenderMicroscopicMode()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.toController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        sectionAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func toSucceedProgress() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension UpwardlyViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func subviewsRemain() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.requestView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func equalImage() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
