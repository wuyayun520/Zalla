
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_arrayValue:[UInt8] = [0xc,0xb,0xc,0x11,0x4d,0x6,0xa,0x1,0x0,0x17,0x5f,0x4c,0x45,0xd,0x4,0x16,0x45,0xb,0xa,0x11,0x45,0x7,0x0,0x0,0xb,0x45,0xc,0x8,0x15,0x9,0x0,0x8,0x0,0xb,0x11,0x0,0x1]

private func orderedSeries(app num: UInt8) -> UInt8 {
    return num ^ 101
}

/*: "Load..." :*/
fileprivate let str_methodValue:String = "bean app firstLoad.."
fileprivate let str_frameValue:String = "project"

/*: "Skip" :*/
fileprivate let str_colorEventName:String = "key icon import userSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_fileTitle:[UInt8] = [0x4f,0x7b,0x7b,0x77,0x5b,0x76,0x76,0x73,0x35,0x55,0x68,0x7b,0x70,0x7d,0x6c,0x5b,0x76,0x51,0x7a,0x2f,0x2e,0x79,0x6c,0x6a,0x6f,0x68,0x79,0x6e,0x6c,0x2e,0x30]

fileprivate func closeTo(start num: UInt8) -> UInt8 {
    let value = Int(num) - 7
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_followVersionTitle:[UInt8] = [0x31,0x23,0x28,0x1e,0x29,0x31,0xe8,0x29,0x28,0xa,0x1b,0x21,0x1f,0xd,0x22,0x29,0x31,0xe0,0xe0,0x29,0x28,0xa,0x1b,0x21,0x1f,0xd,0x22,0x29,0x31,0xe2,0xe3,0xf5]

fileprivate func latencyPeriod(to num: UInt8) -> UInt8 {
    let value = Int(num) - 186
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_valueData:String = "jsEvlive size add"
fileprivate let str_bagModelName:String = "succeed self appPageSho"
fileprivate let str_firstCollectionData:String = "let share background typew): "

/*: --- :*/
fileprivate let str_labTitle:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_colorTitle:[UInt8] = [0x62,0x7c,0x7b,0x71,0x7a,0x62,0x3b,0x7a,0x7b,0x45,0x74,0x72,0x70,0x5d,0x7c,0x71,0x70,0x33,0x33,0x7a,0x7b,0x45,0x74,0x72,0x70,0x5d,0x7c,0x71,0x70,0x3d,0x3c,0x2e]

private func offdVar(front num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_messageLocalTitle:String = "jsEvcolor to"
fileprivate let str_textTitle:String = "array mininPag"
fileprivate let str_modeAllValue:String = "make to equal patheHide): "

/*: "title" :*/
fileprivate let str_ofCommitText:String = "TITLE"

/*: "canGoBack" :*/
fileprivate let str_contentData:String = "canGoBacmake selected male view render"
fileprivate let str_minViewAddTitle:[Character] = ["k"]

/*: "PHPSESSID= :*/
fileprivate let str_backMakePlainText:[Character] = ["P","H","P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let str_modelGiftLetData:String = ";uid=let view image info tool"

/*: "Cookie" :*/
fileprivate let str_infoClearTitle:[Character] = ["C","o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let str_noInsideTitle:String = "true text available source letnav_"
fileprivate let str_actionValue:String = "search top slideblack_"
fileprivate let str_partyTitle:String = "appearr"

/*: "ico_intimate_list_sm" :*/
fileprivate let str_photoBorderName:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_picFollowingData:[UInt8] = [0x79,0x72,0x7e,0x68,0x70,0x78,0x73,0x69,0x33,0x7a,0x78,0x69,0x58,0x71,0x78,0x70,0x78,0x73,0x69,0x6e,0x5f,0x64,0x49,0x7c,0x7a,0x53,0x7c,0x70,0x78,0x35,0x3a,0x7f,0x72,0x79,0x64,0x3a,0x34,0x46,0x2d,0x40,0x33,0x6e,0x69,0x64,0x71,0x78,0x33,0x7f,0x7c,0x7e,0x76,0x7a,0x6f,0x72,0x68,0x73,0x79,0x20,0x3a,0x6f,0x7a,0x7f,0x7c,0x35,0x2d,0x31,0x2d,0x31,0x2d,0x31,0x2d,0x34,0x3a]

private func fireAgeMessage(to num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "Alert" :*/
fileprivate let str_liveGoldTitle:String = "self size to to modelAlert"

/*: "OK" :*/
fileprivate let str_contentHiddenName:String = "Orequest"

/*: "Cancle" :*/
fileprivate let str_sizeName:String = "Cancleequal to self text"

/*: "Done" :*/
fileprivate let str_fileLayerValue:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let str_regularShowValue:[Character] = ["j","s"," ","c","a","l","l"," "]
fileprivate let str_transformBrandErrorText:String = "METHO"
fileprivate let str_factorVoiceName:String = "layer field enable wrap letme = "

/*: , message =  :*/
fileprivate let str_labelData:String = "equal view, mess"

/*: "http://" :*/
fileprivate let str_diskValue:String = "self view start data sizehttp://"

/*: "https://" :*/
fileprivate let str_textValue:[Character] = ["h","t"]
fileprivate let str_herGiftValue:String = "define lettps://"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_modelValue:[UInt8] = [0x6b,0x7c,0x6f,0x3d,0x7e,0x72,0x72,0x76,0x74,0x78,0x53,0x7c,0x70,0x78,0x6e,0x3d,0x20,0x3d,0x79,0x72,0x7e,0x68,0x70,0x78,0x73,0x69,0x33,0x7e,0x72,0x72,0x76,0x74,0x78,0x33,0x6e,0x6d,0x71,0x74,0x69,0x35,0x3a,0x26,0x3d,0x3a,0x34,0x33,0x70,0x7c,0x6d,0x35,0x7b,0x68,0x73,0x7e,0x69,0x74,0x72,0x73,0x35,0x7e,0x72,0x72,0x76,0x74,0x78,0x34,0x3d,0x66,0x3d,0x6f,0x78,0x69,0x68,0x6f,0x73,0x3d,0x7e,0x72,0x72,0x76,0x74,0x78,0x33,0x6e,0x6d,0x71,0x74,0x69,0x35,0x3a,0x20,0x3a,0x34,0x46,0x2d,0x40,0x3d,0x60,0x3d,0x34,0x26,0x17]

private func holdfastData(text num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "uid= :*/
fileprivate let str_cellValue:String = "effect falseuid="

/*: ;domain= :*/
fileprivate let str_sinceSessionValue:String = ";domlabel field make a"

/*: ;path=/" :*/
fileprivate let str_centerTitle:String = "dismiss before view;path=/"

/*: "document.cookie=' :*/
fileprivate let str_makeToName:String = "doccontent"
fileprivate let str_mobileName:String = "ie=\'"

/*: ';\n" :*/
fileprivate let str_insideValue:String = "\';\n"

/*: ;domin= :*/
fileprivate let str_playName:String = "player current camera source time;domin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_shareText:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let str_pointData:String = "\'; };\n"

/*: '" :*/
fileprivate let str_cellTitle:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let str_resultData:[Character] = ["h","5","D","o"]
fileprivate let str_topName:[Character] = ["m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let str_iconData:String = "return player currentgetAll"
fileprivate let str_atNameValue:String = "s = view path"

/*: "PHPSESSID" :*/
fileprivate let str_playerName:String = "indexPS"

/*: "uid" :*/
fileprivate let str_startValue:String = "UID"

/*: "appCookies =  :*/
fileprivate let str_hiddenValue:[Character] = ["a","p","p","C","o"]
fileprivate let str_sourceTitle:String = "ocreateie"

/*: "UID" :*/
fileprivate let str_playerIndexData:String = "UIdismiss"

/*: "navigator.userAgent" :*/
fileprivate let str_managerData:[Character] = ["n","a","v","i","g","a","t","o","r","."]
fileprivate let str_allowStyleText:String = "version"
fileprivate let str_sectionName:[Character] = ["s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let str_playValue:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_makeData:String = "equalunwu"
fileprivate let str_giftData:String = "player intimate model disabled remainone x"
fileprivate let str_pathData:String = "uvoiceu"
fileprivate let str_addTitle:[Character] = ["s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let str_countLiveTitle:String = "key add voice xunwu"
fileprivate let str_buttonData:[Character] = ["g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let str_popIndexValue:String = " xuncolor any"
fileprivate let str_sectionValue:String = "let true challengedleId"
fileprivate let str_sharedViewContentData:String = "/"

/*:   webkit/1" :*/
fileprivate let str_theTitle:String = "color make  we"

/*: "  :*/
fileprivate let str_contentValue:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let str_indexData:String = "background share table xunw"
fileprivate let str_timeListName:String = "u_deto file start"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AtDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class AtDelegate: NasalCavityViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ReloadConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: VoicePublishNameConvertible?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: VoicePublishNameConvertible?, webConfig: ReloadConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_arrayValue.map{orderedSeries(app: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        withObject()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        quantityHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(str_methodValue.suffix(6)) + str_frameValue.replacingOccurrences(of: "project", with: "."))
        //: addNotification()
        isothermal()
        //: func__checkConfig()
        funcConfig()
        //: func__setUpWebView()
        explainCancel()
        //: func__registerObserver()
        seer()
        //: func__beginStartRequest()
        sharedRequest()
        //: func_checkAppleIAP()
        showOff()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kLet_topMaxTitle, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            toRecordConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.smallFor(), .font: UIFont.conversationSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_colorEventName.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension AtDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func isothermal() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(choiceDown),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: kLet_userPositionName,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(withObject),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func choiceDown() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_fileTitle.map{closeTo(start: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func withObject() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_followVersionTitle.map{latencyPeriod(to: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_valueData.prefix(4)) + "ent(on" + String(str_bagModelName.suffix(7)) + String(str_firstCollectionData.suffix(4))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func quantityHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_colorTitle.map{offdVar(front: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_messageLocalTitle.prefix(4)) + "ent(o" + String(str_textTitle.suffix(4)) + String(str_modeAllValue.suffix(8))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension AtDelegate {
    //: func func__registerObserver() {
    func seer() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (str_ofCommitText.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_contentData.prefix(8)) + String(str_minViewAddTitle)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(viewAdd), name: kLet_messageTitle, object: nil)
    }

    //: func func__setUpWebView() {
    func explainCancel() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        bagReply()
        //: func__addBridgeMethod()
        official()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.modelCard()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            AtAdView.linkLine(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(kLet_environmentContent)
            }
        }
    }

    //: func func__beginStartRequest() {
    func sharedRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.timeRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            timeRequest()
        }
    }

    //: func func__startRequest() {
    func timeRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = WatercourseThen.share.loginUid
        let uid = WatercourseThen.share.loginUid
        //: let ssid = WatercourseThen.share.loginSessionId
        let ssid = WatercourseThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(str_backMakePlainText)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(str_modelGiftLetData.prefix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(str_infoClearTitle)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        makeColor()
        //: func__initRightButton()
        liveEqual()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func showOff() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            NameLikeTransactionObserver.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.overdoPrice(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            NameLikeTransactionObserver.shared.withTransactions()
        }
    }

    //: func func__initLeftButton() {
    func leftLine() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.withView(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.withView(name: (String(str_noInsideTitle.suffix(4)) + "back_" + String(str_actionValue.suffix(6)) + str_partyTitle.replacingOccurrences(of: "appear", with: "no"))), style: .plain, target: self, action: #selector(coverFrom(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func liveEqual() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(VoicePublishNameConvertible.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.withView(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.withView(name: (String(str_photoBorderName))), style: .plain, target: self, action: #selector(column))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func coverFrom(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        outViewMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.toController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: AtDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! AtDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! AtDelegate).withObject()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func funcConfig() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func contentSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        AtAdView.toAction()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func makeColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_picFollowingData.map{fireAgeMessage(to: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func valueUrl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: IndoorsPushManager.share.func__pushToWebVC(urlStr: url)
            IndoorsPushManager.share.feather(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func userSex(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func viewAdd() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: kLet_messageTitle, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func column() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = MusicResultDataSource(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText))
        //: view.show()
        view.gestureApp()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension AtDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            AtAdView.linkLine(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            contentSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        leftLine()
        //: func__clearJSBgColor()
        makeColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            contentSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            contentSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(str_liveGoldTitle.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(str_sizeName.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(str_fileLayerValue)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension AtDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_regularShowValue) + str_transformBrandErrorText.lowercased() + "d na" + String(str_factorVoiceName.suffix(5))) + "\(message.name)" + (String(str_labelData.suffix(6)) + "age = ") + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.doScreenplayMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func official() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in MethodValueConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func outViewMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in MethodValueConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension AtDelegate {
    //: func func__addUserScript() {
    func noDevice() {
        //: var htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(str_diskValue.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_diskValue.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(str_textValue) + String(str_herGiftValue.suffix(6)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(str_textValue) + String(str_herGiftValue.suffix(6))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = WatercourseThen.share.loginUserMode.userID
        let tempUid: String = WatercourseThen.share.loginUserMode.userID
        //: let tempSessionID: String = WatercourseThen.share.loginSessionId
        let tempSessionID: String = WatercourseThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_modelValue.map{holdfastData(text: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_cellValue.suffix(4))) + "\(tempUid)" + (String(str_sinceSessionValue.prefix(4)) + "ain=") + "\(htmlDomain)" + (String(str_centerTitle.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((str_makeToName.replacingOccurrences(of: "content", with: "u") + "ment.cook" + str_mobileName) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(str_backMakePlainText)) + "\(tempSessionID)" + (String(str_playName.suffix(7))) + "\(htmlDomain)" + (String(str_centerTitle.suffix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((str_makeToName.replacingOccurrences(of: "content", with: "u") + "ment.cook" + str_mobileName) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_shareText.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_cellValue.suffix(4))) + "\(tempUid)" + (String(str_sinceSessionValue.prefix(4)) + "ain=") + "\(htmlDomain)" + (String(str_centerTitle.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_makeToName.replacingOccurrences(of: "content", with: "u") + "ment.cook" + str_mobileName) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(str_cellValue.suffix(4))) + "\(tempSessionID)" + (String(str_sinceSessionValue.prefix(4)) + "ain=") + "\(htmlDomain)" + (String(str_centerTitle.suffix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((str_makeToName.replacingOccurrences(of: "content", with: "u") + "ment.cook" + str_mobileName) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func toRecordConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_resultData) + String(str_topName)) + "\(htmlDomain)")
            //: let userID: String = WatercourseThen.share.loginUserMode.userID
            let userID: String = WatercourseThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_iconData.suffix(6)) + "Cookie" + String(str_atNameValue.prefix(4))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (str_playerName.replacingOccurrences(of: "index", with: "PH") + "ESSID") {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (str_startValue.lowercased()), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.sharedRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.featherName(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func featherName(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_resultData) + String(str_topName)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: GazetteThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_hiddenValue) + str_sourceTitle.replacingOccurrences(of: "create", with: "k") + "s = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (str_playerIndexData.replacingOccurrences(of: "dismiss", with: "D")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (str_startValue.lowercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (str_playerName.replacingOccurrences(of: "index", with: "PH") + "ESSID"), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (str_playerName.replacingOccurrences(of: "index", with: "PH") + "ESSID")
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.sharedRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func bagReply() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(str_managerData) + str_allowStyleText.replacingOccurrences(of: "version", with: "u") + String(str_sectionName))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(str_managerData) + str_allowStyleText.replacingOccurrences(of: "version", with: "u") + String(str_sectionName)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(str_playValue))) {
                let str = "xunwu_iphone xunwu_version/\(kLet_licenseRequestDoingTitle) xunwu_package/\(kLet_voiceValue) xunwu_bundleId/\(kLet_clickReplaceHeightValue)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.blowerCloseDominoGetDeviceIdentifier { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_indexData.suffix(5)) + String(str_timeListName.prefix(4)) + "vice_id/") + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
