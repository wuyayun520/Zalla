
//: Declare String Begin

/*: "cmd" :*/
fileprivate let str_normalTitle:[UInt8] = [0x87,0x91,0x88]

fileprivate func inElse(at num: UInt8) -> UInt8 {
    let value = Int(num) - 36
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_keyDismissCellValue:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let str_viewValue:[UInt8] = [0xa3,0xa6,0xb3,0xa6]

private func labArray(domain num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "logId" :*/
fileprivate let str_willValue:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let str_liveLabelData:String = "#3local8"
fileprivate let str_pushViewTitle:String = "7hidden"

/*: "Tap to start video chatting" :*/
fileprivate let str_normalValue:String = "leading data numberTap t"
fileprivate let str_allName:String = "art vilet value frame make"
fileprivate let str_whiteValue:String = "chattinview"

/*: "icon_close_nor" :*/
fileprivate let str_contentData:String = "ichange"
fileprivate let str_collectionText:[Character] = ["n","_","c","l","o","s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let str_frameReportName:String = "icoequal"
fileprivate let str_touchTitleData:String = "if size make lab viewws_n"

/*: "%@ chatting" :*/
fileprivate let str_modelRequestData:[Character] = ["%","@"," ","c","h","a","t","t","i"]
fileprivate let str_dataTitle:String = "nrange"

/*: "icon_lounge" :*/
fileprivate let str_pauseValue:String = "icon_loutime app"
fileprivate let str_errorTitle:String = "nking"

/*: "icon_rank_coin" :*/
fileprivate let str_toolValue:String = "icon_if window"
fileprivate let str_toTitle:String = "oremove"

/*: "vip_btn_top" :*/
fileprivate let str_pairValue:String = "vip_info size to guard size"

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_frameTitle:String = "area type view rightbtn_ne"
fileprivate let str_hiddenLeadingName:String = "viewttings"
fileprivate let str_normalData:[Character] = ["_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let str_tableName:String = "if extension video app text%@ Coin"
fileprivate let str_userValue:[Character] = ["s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let str_contextContent:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r"]
fileprivate let str_requestName:[Character] = ["r","o","r"]

/*: "matchId" :*/
fileprivate let str_currentValue:String = "MATCH"
fileprivate let str_toValue:[Character] = ["I","d"]

/*: "errno" :*/
fileprivate let str_dataName:[UInt8] = [0x6d,0x7a,0x7a,0x66,0x67]

private func appGreen(transaction num: UInt8) -> UInt8 {
    return num ^ 8
}

/*: "VIPFee" :*/
fileprivate let str_scaleName:[Character] = ["V"]
fileprivate let str_pathContent:[Character] = ["I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let str_arrayValue:String = "type return type videoMatchi"
fileprivate let str_errorData:String = "in to play labelng now"

/*: "userTouch" :*/
fileprivate let str_userCollectionValue:String = "usstartr"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_iconContentData:String = "var conversationmf/f"
fileprivate let str_behindText:String = "intimate let main removel/ge"
fileprivate let str_fillData:[Character] = ["t","V","i","d","e"]
fileprivate let str_recordTitle:[Character] = ["o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let str_mVideoData:[UInt8] = [0xb,0x17,0x14,0x12,0xf9,0x1e,0x15,0xa]

fileprivate func mediumText(observer num: UInt8) -> UInt8 {
    let value = Int(num) + 91
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let str_userTitle:[UInt8] = [0x9f,0xb5,0xbb,0x6d,0xbc,0xab,0x66,0xa8,0xab,0xab,0xb4,0x66,0xa8,0xa7,0xb8,0xb8,0xab,0xaa,0x66,0xac,0xb8,0xb5,0xb3,0x66,0xb8,0xab,0xa9,0xab,0xaf,0xbc,0xaf,0xb4,0xad,0x66,0xa9,0xa7,0xb2,0xb2,0xb9]

fileprivate func colorEqual(voice num: UInt8) -> UInt8 {
    let value = Int(num) + 186
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let str_showContent:[UInt8] = [0x85,0x99,0x83,0x84,0x95,0x93]

/*: "type" :*/
fileprivate let str_modelViewName:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let str_quickValue:[UInt8] = [0x38,0x24,0x29]

/*: "fromFreeCall" :*/
fileprivate let str_textData:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let str_loadContent:[Character] = ["r","e","q","u","e","s","t","C","a","l"]
fileprivate let str_drawFrameTitle:String = "L"

/*: "onVideoMatch" :*/
fileprivate let str_colorValue:[Character] = ["o","n","V","i","d","e","o","M","a","t"]
fileprivate let str_tableText:[Character] = ["c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_changeVersionFrameName:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "refuseCall" :*/
fileprivate let str_managerName:[Character] = ["r","e","f"]
fileprivate let str_imageValue:String = "useCallapp model"

/*: "onCancelCall" :*/
fileprivate let str_makeName:String = "place let container var labonCanc"

/*: "retryAfter" :*/
fileprivate let str_frameData:String = "RE"
fileprivate let str_clearModelName:String = "model import text elsetryAfter"

/*: "onRequestCall" :*/
fileprivate let str_playerBirthActualValue:[Character] = ["o","n","R","e","q"]
fileprivate let str_listenData:[Character] = ["u","e","s"]
fileprivate let str_aspectValue:[Character] = ["t","C","a","l","l"]

/*: "failType" :*/
fileprivate let str_toData:String = "view image button background harvestfailT"
fileprivate let str_contentName:String = "ypresponse"

/*: "callWaitingTime" :*/
fileprivate let str_taskStatusEqualName:String = "callWapic layer normal time true"
fileprivate let str_pathName:String = "gTimecan to ok name"

/*: "toUid" :*/
fileprivate let str_assetMakeData:String = "of manager normal view videotoUid"

/*: "Free 1 min" :*/
fileprivate let str_pushActionData:[Character] = ["F","r","e","e"," ","1"]
fileprivate let str_valueData:[Character] = [" ","m","i","n"]

/*: "%@/min" :*/
fileprivate let str_atText:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReadFactoryReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ReadFactoryReactiveCompatible: MusicViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.exaggerateMini()
            }
        }
        //: self.resetCamera()
        self.doQueryed()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        exaggerateSubviews()
        //: setupSubViewsConstraint()
        indoors()
        //: bindInteraction()
        appDoingEffect()
        //: videoManager.startPreview(isMatch: true)
        videoManager.equalMatch(isMatch: true)
        //: setPlayer(isRandon: false)
        objectUser(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            blockList()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_normalTitle.map{inElse(at: $0)}, encoding: .utf8)!: (String(str_keyDismissCellValue)), String(bytes: str_viewValue.map{labArray(domain: $0)}, encoding: .utf8)!: [(String(str_willValue)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        previous()
        //: if TalkingSocketManager.shared.isTalking == false {
        if NameSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            NameSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            NameSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            NameSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            NameSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: TalkingCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = TalkingCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = PublishChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .ending()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(blockList), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kLet_scaleName + 1, height: kLet_environmentText + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (str_liveLabelData.replacingOccurrences(of: "local", with: "22") + str_pushViewTitle.replacingOccurrences(of: "hidden", with: "0")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_normalValue.suffix(5)) + "o st" + String(str_allName.prefix(6)) + "deo " + str_whiteValue.replacingOccurrences(of: "view", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.withView(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.withView(name: (str_contentData.replacingOccurrences(of: "change", with: "co") + String(str_collectionText))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(subjectMatter), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: MediaControl = {
        //: let btn = TalkingButton()
        let btn = MediaControl()
        //: btn.setImage(UIImage.withView(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.withView(name: (str_frameReportName.replacingOccurrences(of: "equal", with: "n") + "_vie" + String(str_touchTitleData.suffix(4)) + "umber")), for: .normal)
        //: btn.setImage(UIImage.withView(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.withView(name: (str_frameReportName.replacingOccurrences(of: "equal", with: "n") + "_vie" + String(str_touchTitleData.suffix(4)) + "umber")), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.conversationSize(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_modelRequestData) + str_dataTitle.replacingOccurrences(of: "range", with: "g")).alongNext(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.withView(name: "icon_lounge")
        img.image = UIImage.withView(name: (String(str_pauseValue.prefix(8)) + str_errorTitle.replacingOccurrences(of: "king", with: "ge")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.withView(name: "icon_rank_coin")
        img.image = UIImage.withView(name: (String(str_toolValue.prefix(5)) + "rank_c" + str_toTitle.replacingOccurrences(of: "remove", with: "in")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.conversationSize(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.withView(name: "vip_btn_top")
        imgV.image = UIImage.withView(name: (String(str_pairValue.prefix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: MediaControl = {
        //: let btn = TalkingButton()
        let btn = MediaControl()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(masculineClick), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.withView(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.withView(name: (String(str_frameTitle.suffix(6)) + "arby_s" + str_hiddenLeadingName.replacingOccurrences(of: "view", with: "e") + String(str_normalData))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.withView(name: "icon_rank_coin")
        imgV.image = UIImage.withView(name: (String(str_toolValue.prefix(5)) + "rank_c" + str_toTitle.replacingOccurrences(of: "remove", with: "in")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .toUser()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(str_tableName.suffix(7)) + String(str_userValue)).alongNext(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.conversationSize(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: CornerThen = //: return CornerThen(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ReadFactoryReactiveCompatible {
    //: private func setPlayer(isRandon: Bool) {
    private func objectUser(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = StreamReactiveCompatible.default.picName(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_contextContent) + String(str_requestName)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func fromBy(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(str_currentValue.lowercased() + String(str_toValue)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: str_normalTitle.map{inElse(at: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: str_dataName.map{appGreen(transaction: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        HumanHeadReactiveCompatible.modelDelete(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.someAdd()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(str_currentValue.lowercased() + String(str_toValue))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func playerError() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        HumanHeadReactiveCompatible.builder { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(str_scaleName) + String(str_pathContent))].intValue
            //: self.refreshVideoCallPrice()
            self.exaggerateMini()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ReadFactoryReactiveCompatible {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func masculineClick() {
        //: IndoorsPushManager.share.func__pushToSubscribeAlert()
        IndoorsPushManager.share.digitizerParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func subjectMatter() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        toBtn()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_hundredText)

        //: if WatercourseThen.share.appConfigMode.displayMatchV4PopUp, WatercourseThen.share.loginUserMode.freeCallTimes > 0,
        if WatercourseThen.share.appConfigMode.displayMatchV4PopUp, WatercourseThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.conversationSearch()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        toController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func blockList() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard NameSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailKey(showMsg: kLet_networkViewTitle)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        fillEvent()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_arrayValue.suffix(6)) + String(str_errorData.suffix(6))).localized
        //: setPlayer(isRandon: true)
        objectUser(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if WatercourseThen.share.loginUserMode.randomVideoType == 2 {
        if WatercourseThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_pointValue)
            //: req_getVideoInfo()
            bloomer()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_viewUserContent)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            NameSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            NameSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            NameSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            NameSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            reinInErrno()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func reinInErrno(cmd: String = (str_userCollectionValue.replacingOccurrences(of: "start", with: "e") + "Touch"), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        previous()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        fromBy(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func someAdd() {
        // 停止定时器
        //: destroyWaitingTimer()
        previous()
        //: showCloseBtn()
        toBtn()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_normalValue.suffix(5)) + "o st" + String(str_allName.prefix(6)) + "deo " + str_whiteValue.replacingOccurrences(of: "view", with: "g")).localized
        //: setPlayer(isRandon: false)
        objectUser(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if NameSocketManager.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            NameSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            NameSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            NameSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            NameSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        exaggerateMini()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func previous() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func bloomer() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = IndexRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_iconContentData.suffix(4)) + "reeCal" + String(str_behindText.suffix(4)) + String(str_fillData) + String(str_recordTitle))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_mVideoData.map{mediumText(observer: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_endVersionTitle.getStarted(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.someAdd()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: WatercourseThen.share.loginUserMode.randomVideoType = 1
                    WatercourseThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.blockList()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.detailKey(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = UnknownModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = CommandPlayerManager()
                //: player.setMute(bEnable: true)
                player.enableGenerate(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.shouldUserAdd(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                MakeReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ColorPermissionTool.behindQuantityMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.afterward()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func afterward() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kLet_depthIconValue.mightHaveBeenStubZapErrorUploadTo(eventID: kLet_fileValue)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard MakeReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.detailKey(showMsg: String(bytes: str_userTitle.map{colorEqual(voice: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            MakeReactiveCompatible.shared.detailComment(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.someAdd()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = TalkingPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.toController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.someAdd()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func fillEvent() {
        //: if WatercourseThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if WatercourseThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(WatercourseThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(WatercourseThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func toBtn() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func doQueryed() {
        guard NameSocketManager.shared.isTalking == false else {
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.equalMatch()
        }
    }
}

// MARK: - LikeMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ReadFactoryReactiveCompatible: LikeMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func coverUp(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = AchromaticColorMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(str_currentValue.lowercased() + String(str_toValue)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: str_showContent.map{$0^246}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: str_modelViewName.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: str_quickValue.map{$0^77}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = AlbumKeyRepresentable.randow
        //: if WatercourseThen.share.loginUserMode.freeCallTimes > 0 && WatercourseThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if WatercourseThen.share.loginUserMode.freeCallTimes > 0 && WatercourseThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(str_textData)))
            //: TalkingSocketManager.shared.isFreeCall = true
            NameSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: str_normalTitle.map{inElse(at: $0)}, encoding: .utf8)!: (String(str_loadContent) + str_drawFrameTitle.lowercased()), String(bytes: str_viewValue.map{labArray(domain: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        NameSocketManager.shared.migration(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            NameSocketManager.shared.migration(info: [String(bytes: str_normalTitle.map{inElse(at: $0)}, encoding: .utf8)!: (String(str_keyDismissCellValue)), String(bytes: str_viewValue.map{labArray(domain: $0)}, encoding: .utf8)!: [(String(str_willValue)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.reinInErrno(cmd: (String(str_colorValue) + String(str_tableText)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func nudgeAll(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(str_willValue))].intValue
    }
}

// MARK: - DateHeadObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ReadFactoryReactiveCompatible: DateHeadObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func toData(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func criminalRecord(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func tableGift(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func messageAssemblage(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            CellDelegateHelper.thoughtImageDismiss(body: String(bytes: str_changeVersionFrameName.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = AchromaticColorMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            spoon(toUid: String(uid))
        }
        //: stop_randomVideo()
        someAdd()
        // 开启视频通话时先销毁当前预览画面
        videoManager.detectBy()
        //: WatercourseThen.share.start1v1TalkCall(info: data, situation: .Calling)
        WatercourseThen.share.messageSituation(info: data, situation: .Calling)
    }
}

// MARK: - ManagingDirectorObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ReadFactoryReactiveCompatible: ManagingDirectorObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func actionTable(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        reinInErrno(cmd: (String(str_managerName) + String(str_imageValue.prefix(7))))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func equalColor(data _: [String: Any]) {}
}

// MARK: - LabelObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ReadFactoryReactiveCompatible: LabelObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func loadDown(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_makeName.suffix(6)) + "elCall") { // 取消通话异常
            //: stop_randomVideo()
            someAdd()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(str_colorValue) + String(str_tableText)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            someAdd()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(str_currentValue.lowercased() + String(str_toValue))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.detailKey(showMsg: msg)
            //: uploadFailTime(type: 2)
            atOn(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(str_colorValue) + String(str_tableText)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(str_frameData.lowercased() + String(str_clearModelName.suffix(8)))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(str_currentValue.lowercased() + String(str_toValue))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.reinInErrno(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(str_playerBirthActualValue) + String(str_listenData) + String(str_aspectValue)), errorNo == FactorySumLiteral.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            someAdd()
            //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else { return }
            //: IndoorsPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            IndoorsPushManager.share.rechargeVideo(clickEvent: kLet_voiceName, sufficient: false)
            //: uploadFailTime(type: 1)
            atOn(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        reinInErrno(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ReadFactoryReactiveCompatible {
    //: func uploadFailTime(type: Int) {
    func atOn(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(str_toData.suffix(5)) + str_contentName.replacingOccurrences(of: "response", with: "e"))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(str_taskStatusEqualName.prefix(6)) + "itin" + String(str_pathName.prefix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kLet_depthIconValue.fromCell(eventID: kLet_infoUserData, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func spoon(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(str_taskStatusEqualName.prefix(6)) + "itin" + String(str_pathName.prefix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(str_assetMakeData.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kLet_depthIconValue.fromCell(eventID: kLet_minData, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ReadFactoryReactiveCompatible {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func exaggerateMini() {
        //: guard WatercourseThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard WatercourseThen.share.appStatus == PublishWidthTotal.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !WatercourseThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !WatercourseThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = AlbumKeyRepresentable.randow
        //: if WatercourseThen.share.loginUserMode.freeCallTimes > 0 && WatercourseThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if WatercourseThen.share.loginUserMode.freeCallTimes > 0, WatercourseThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if WatercourseThen.share.appUserConfigMode.callBtnStyle == 1 {
            if WatercourseThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_pushActionData) + String(str_valueData)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = WatercourseThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = WatercourseThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(str_atText)).alongNext(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.showSize(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(str_atText)).alongNext(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.showSize(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = WatercourseThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = WatercourseThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = WatercourseThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = WatercourseThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !WatercourseThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !WatercourseThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func exaggerateSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func indoors() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(kLet_environmentContent + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func appDoingEffect() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(exaggerateMini),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: kLet_inviteData,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(playerError),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kLet_qualityName,
                                               //: object: nil)
                                               object: nil)
    }
}
