
//: Declare String Begin

/*: "nil" :*/
fileprivate let str_labelData:String = "nineed"

/*: "Edit profiles" :*/
fileprivate let str_thumbName:[Character] = ["E","d","i","t"," "]
fileprivate let str_labData:String = "valueofiles"

/*: "Done" :*/
fileprivate let str_playMenuName:String = "Donereturn price background equal"

/*: "#999999" :*/
fileprivate let str_formatViewValue:String = "#999999"

/*: "uid" :*/
fileprivate let str_arrayTitle:String = "uimortal"

/*: "StreamViewCell" :*/
fileprivate let str_addBurnText:String = "succeed collectionTalk"
fileprivate let str_infoValue:String = "log center that number tableditN"
fileprivate let str_contentData:String = "country press dataameCell"

/*: "ManagerThen" :*/
fileprivate let str_closeTitle:[Character] = ["T","a","l","k","i","n","g","E"]
fileprivate let str_actionValue:[Character] = ["d","i","t"]
fileprivate let str_picValue:String = "Signmove conversation color"

/*: "Save the change?" :*/
fileprivate let str_rawValue:[Character] = ["S","a","v","e"," ","t","h","e"]
fileprivate let str_topName:[Character] = [" ","c","h","a","n","g","e","?"]

/*: "Cancel" :*/
fileprivate let str_actualName:String = "else label makeCancel"

/*: "Save" :*/
fileprivate let str_mainValue:[Character] = ["S","a","v","e"]

/*: "nickname" :*/
fileprivate let str_titleData:String = "nmakekname"

/*: "birthday" :*/
fileprivate let str_modelEqualGiftValue:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "desc" :*/
fileprivate let str_editTitle:String = "desfield"

/*: "Modify the success" :*/
fileprivate let str_birdData:[Character] = ["M","o","d","i","f","y"," ","t","h","e"," ","s","u","c","c","e","s","s"]

/*: "pic" :*/
fileprivate let str_labValue:String = "piposition"

/*: "[ :*/
fileprivate let str_nextIdentityValue:String = "model"

/*: ]" :*/
fileprivate let str_managerTitle:[Character] = ["]"]

/*: "pids" :*/
fileprivate let str_rowContent:[Character] = ["p","i","d","s"]

/*: "aboutMe" :*/
fileprivate let str_fileTitle:String = "ABOUT"
fileprivate let str_lightContent:String = "Memode frame make block title"

/*: "interest" :*/
fileprivate let str_removeInfoContent:String = "INTERES"
fileprivate let str_rawText:[Character] = ["t"]

/*: "tagIds" :*/
fileprivate let str_managerColorText:String = "tagIdsguard if to in message"

/*: "category" :*/
fileprivate let str_textValue:String = "categocollection"

/*: "An error occurred, please try again" :*/
fileprivate let str_partyName:[UInt8] = [0x14,0x3b,0x75,0x30,0x27,0x27,0x3a,0x27,0x75,0x3a,0x36,0x36,0x20,0x27,0x27,0x30,0x31,0x79,0x75,0x25,0x39,0x30,0x34,0x26,0x30,0x75,0x21,0x27,0x2c,0x75,0x34,0x32,0x34,0x3c,0x3b]

private func viewTransaction(date num: UInt8) -> UInt8 {
    return num ^ 85
}

/*: "imgUrl" :*/
fileprivate let str_frameValue:String = "end text directionimgUrl"

/*: "videoUrl" :*/
fileprivate let str_labelValue:[Character] = ["v"]
fileprivate let str_groupApplyTitle:[Character] = ["i","d","e","o","U","r","l"]

/*: "Video cover submitted successfully" :*/
fileprivate let str_sharedLineContent:[UInt8] = [0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x73,0x20,0x64,0x65,0x74,0x74,0x69,0x6d,0x62,0x75,0x73,0x20,0x72,0x65,0x76,0x6f,0x63,0x20,0x6f,0x65,0x64,0x69,0x56]

/*: "FactoryView" :*/
fileprivate let str_modelName:String = "with make equalTalk"
fileprivate let str_hideName:String = "tVideoCcolor gift view"
fileprivate let str_equalValue:String = "ewould"

/*: "video_url" :*/
fileprivate let str_paintToData:String = "vicontrolo"
fileprivate let str_attributeData:String = "to data self_url"

/*: "img_url" :*/
fileprivate let str_shareUserName:String = "awake typeimg_url"

/*: "status" :*/
fileprivate let str_beName:String = "STATU"
fileprivate let str_feedValue:String = "scale"

/*: "BottomCurrentReactiveCompatible" :*/
fileprivate let str_makeData:String = "index data model sharedTalki"
fileprivate let str_managerValue:String = "nor live arrayPhotoCell"

/*: "AnniversaryObjectProtocol" :*/
fileprivate let str_textTitle:[Character] = ["T","a","l","k","i","n","g"]
fileprivate let str_spaceValue:String = "Edactivity any case"
fileprivate let str_eventButtonPageContent:String = "self contentitBi"
fileprivate let str_backgroundEditValue:String = "Cellstring return capture"

/*: "NativenessMeCell" :*/
fileprivate let str_layerName:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","A","b","o","u","t","M","e","C"]
fileprivate let str_rowUserData:[Character] = ["e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let str_addName:[Character] = ["U","I","T","a","b","l","e","V"]
fileprivate let str_startSizeText:String = "height user ofiewCell"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AutomaticallyPhotoDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class AutomaticallyPhotoDelegate: MusicViewController {
    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [ShowHandyJSON] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [ShowHandyJSON] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [ReadMeasurable] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (str_labelData.replacingOccurrences(of: "need", with: "l"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.modelCard()
        //: self.title = "Edit profiles".localized
        self.title = (String(str_thumbName) + str_labData.replacingOccurrences(of: "value", with: "pr")).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(str_playMenuName.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (str_formatViewValue.capitalized)), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.conversationSize(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(upClick), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = WatercourseThen.share.loginUserMode.aboutMe!
        seleteAboutme = WatercourseThen.share.loginUserMode.aboutMe!
        //: seleteInters = WatercourseThen.share.loginUserMode.interest!
        seleteInters = WatercourseThen.share.loginUserMode.interest!
        //: designView()
        startAcross()
        //: GetGallery()
        component()
        //: getVideoInfo()
        cookie()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(addUser(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        AtAdView.toAction()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension AutomaticallyPhotoDelegate {
    //: func GetGallery() {
    func component() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = WatercourseThen.share.loginUserMode.userID
        dict[(str_arrayTitle.replacingOccurrences(of: "mortal", with: "d"))] = WatercourseThen.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        AnteriorNarisRequestTool.sectionCompletion(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<ReadMeasurable>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [ReadMeasurable])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func pastCover() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_addBurnText.suffix(4)) + "ingE" + String(str_infoValue.suffix(4)) + String(str_contentData.suffix(7))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: StreamViewCell = i as! StreamViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_closeTitle) + String(str_actionValue) + String(str_picValue.prefix(4)) + "Cell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ManagerThen = i as! ManagerThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&WatercourseThen.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&WatercourseThen.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && WatercourseThen.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && WatercourseThen.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && WatercourseThen.share.loginUserMode.birthday != self.birthday) || (signStr != (str_labelData.replacingOccurrences(of: "need", with: "l")) && WatercourseThen.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = ReloadAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            AlongThen.jumpTime(message: (String(str_rawValue) + String(str_topName)).localized, leftBtnTitle: (String(str_actualName.suffix(6))).localized, rightBtnTitle: (String(str_mainValue)).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                AlongThen.teachingMethod()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                AlongThen.teachingMethod()
                //: self.uploadTool(isBack: true)
                self.sawLog(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func upClick() {
        //: uploadTool(isBack: false)
        sawLog(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func sawLog(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_addBurnText.suffix(4)) + "ingE" + String(str_infoValue.suffix(4)) + String(str_contentData.suffix(7))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: StreamViewCell = i as! StreamViewCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_closeTitle) + String(str_actionValue) + String(str_picValue.prefix(4)) + "Cell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ManagerThen = i as! ManagerThen
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && WatercourseThen.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, WatercourseThen.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(str_titleData.replacingOccurrences(of: "make", with: "ic"))] = nameStr
        }
        //: if birthday.count > 0 && WatercourseThen.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, WatercourseThen.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(str_modelEqualGiftValue))] = birthday
        }
        //: if signStr != "nil" && WatercourseThen.share.loginUserMode.signature != signStr {
        if signStr != (str_labelData.replacingOccurrences(of: "need", with: "l")), WatercourseThen.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(str_editTitle.replacingOccurrences(of: "field", with: "c"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        screen(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && WatercourseThen.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.female.rawValue {
            //: uploadVideo()
            visualCommunicationImagePlace()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        AtAdView.sizeUp()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.customAfter(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            AtAdView.toAction()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.sizeExecuteMsg(showMsg: (String(str_birdData)).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: WatercourseThen.share.loginUserMode.nickname = self.nameStr
                    WatercourseThen.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: WatercourseThen.share.loginUserMode.birthday = self.birthday
                    WatercourseThen.share.loginUserMode.birthday = self.birthday
                }
                //: WatercourseThen.share.loginUserMode.signature = self.signStr
                WatercourseThen.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.detailKey(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func screen(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            derive()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                translateAdd()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            littleness(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            littleness(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func translateAdd() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].image()! as NSData
            //: dict["pic"] = resultData
            dict[(str_labValue.replacingOccurrences(of: "position", with: "c"))] = resultData
            //: ProgressHUD.show()
            AtAdView.sizeUp()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            AnteriorNarisRequestTool.counternalRepresentation(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.sizeExecuteMsg(showMsg: (String(str_birdData)).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.component()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func derive() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(String(str_rowContent))] = str
        //: ProgressHUD.show()
        AtAdView.sizeUp()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.getDoing(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.translateAdd()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.sizeExecuteMsg(showMsg: (String(str_birdData)).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.component()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func littleness(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [ShowHandyJSON] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (str_fileTitle.lowercased() + String(str_lightContent.prefix(2)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (str_removeInfoContent.lowercased() + String(str_rawText))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(str_managerColorText.prefix(6)))] = str
        //: dict["category"] = category
        dict[(str_textValue.replacingOccurrences(of: "collection", with: "ry"))] = category
        //: ProgressHUD.show()
        AtAdView.sizeUp()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.editCompletion(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            AtAdView.toAction()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.sizeExecuteMsg(showMsg: (String(str_birdData)).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: WatercourseThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    WatercourseThen.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: WatercourseThen.share.loginUserMode.interest = self.seleteInters
                    WatercourseThen.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func visualCommunicationImagePlace() {
        //: ProgressHUD.show()
        AtAdView.sizeUp()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        KeyInsertListener.shared.success(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.detailKey(showMsg: String(bytes: str_partyName.map{viewTransaction(date: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(str_frameValue.suffix(6)))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(str_labelValue) + String(str_groupApplyTitle))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            AnteriorNarisRequestTool.present(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.detailKey(showMsg: String(bytes: str_partyName.map{viewTransaction(date: $0)}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.sizeExecuteMsg(showMsg: String(bytes: str_sharedLineContent.reversed(), encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(str_modelName.suffix(4)) + "ingEdi" + String(str_hideName.prefix(7)) + str_equalValue.replacingOccurrences(of: "would", with: "ll")) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: FactoryView = i as! FactoryView
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.item(videoPlayPath: dict[(String(str_labelValue) + String(str_groupApplyTitle))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.cell(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func cookie() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.makeInfo { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(str_paintToData.replacingOccurrences(of: "control", with: "de") + String(str_attributeData.suffix(4)))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(str_shareUserName.suffix(7)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(str_beName.lowercased() + str_feedValue.replacingOccurrences(of: "scale", with: "s"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension AutomaticallyPhotoDelegate {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func addUser(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(str_addBurnText.suffix(4)) + "ingE" + String(str_infoValue.suffix(4)) + String(str_contentData.suffix(7))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: StreamViewCell = i as! StreamViewCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(str_closeTitle) + String(str_actionValue) + String(str_picValue.prefix(4)) + "Cell") {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: ManagerThen = i as! ManagerThen
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension AutomaticallyPhotoDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if WatercourseThen.share.loginUserMode.sex == Gender.female.rawValue {
            if WatercourseThen.share.loginUserMode.sex == PushDownStackScalarLiteral.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((kLet_scaleName - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.mic(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.mic(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(str_modelName.suffix(4)) + "ingEdi" + String(str_hideName.prefix(7)) + str_equalValue.replacingOccurrences(of: "would", with: "ll"))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? FactoryView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = FactoryView(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.cell(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.groundThroughPath(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.item(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(str_makeData.suffix(5)) + "ngEdit" + String(str_managerValue.suffix(9)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? BottomCurrentReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = BottomCurrentReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.addSelete(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.photo()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(str_addBurnText.suffix(4)) + "ingE" + String(str_infoValue.suffix(4)) + String(str_contentData.suffix(7)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? StreamViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = StreamViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(str_textTitle) + String(str_spaceValue.prefix(2)) + String(str_eventButtonPageContent.suffix(4)) + "rthday" + String(str_backgroundEditValue.prefix(4)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AnniversaryObjectProtocol
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = AnniversaryObjectProtocol(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(str_closeTitle) + String(str_actionValue) + String(str_picValue.prefix(4)) + "Cell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? ManagerThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = ManagerThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(str_layerName) + String(str_rowUserData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? NativenessMeCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = NativenessMeCell(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.appose()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.inviteAdd(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.inviteAdd(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = MixedViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.compareEqualsTo(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.compareEqualsTo(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.compareEqualsTo(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.compareEqualsTo(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(str_addName) + String(str_startSizeText.suffix(7)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func mic(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [ShowHandyJSON]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if GazetteThen.share.interfaceLang == AssemblyPlantMirrorPath.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.conversationSize(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > kLet_scaleName - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - EditFailDelegateReactiveCompatible

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension AutomaticallyPhotoDelegate: EditFailDelegateReactiveCompatible {
    //: func freshSeleteTag() {
    func pastTag() {
        //: seleteAboutme =  WatercourseThen.share.loginUserMode.aboutMe!
        seleteAboutme = WatercourseThen.share.loginUserMode.aboutMe!
        //: seleteInters =  WatercourseThen.share.loginUserMode.interest!
        seleteInters = WatercourseThen.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - WhiteThen

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension AutomaticallyPhotoDelegate: WhiteThen {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func withPhoto(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = ReadMeasurable()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func snapshot(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func off(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension AutomaticallyPhotoDelegate {
    //: private func designView() {
    private func startAcross() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(FactoryView.self, forCellReuseIdentifier: (String(str_modelName.suffix(4)) + "ingEdi" + String(str_hideName.prefix(7)) + str_equalValue.replacingOccurrences(of: "would", with: "ll")))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(BottomCurrentReactiveCompatible.self, forCellReuseIdentifier: (String(str_makeData.suffix(5)) + "ngEdit" + String(str_managerValue.suffix(9))))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(StreamViewCell.self, forCellReuseIdentifier: (String(str_addBurnText.suffix(4)) + "ingE" + String(str_infoValue.suffix(4)) + String(str_contentData.suffix(7))))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(AnniversaryObjectProtocol.self, forCellReuseIdentifier: (String(str_textTitle) + String(str_spaceValue.prefix(2)) + String(str_eventButtonPageContent.suffix(4)) + "rthday" + String(str_backgroundEditValue.prefix(4))))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(ManagerThen.self, forCellReuseIdentifier: (String(str_closeTitle) + String(str_actionValue) + String(str_picValue.prefix(4)) + "Cell"))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(NativenessMeCell.self, forCellReuseIdentifier: (String(str_layerName) + String(str_rowUserData)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
