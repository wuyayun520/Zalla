
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_viewContent:[UInt8] = [0xd,0xa,0xd,0x10,0x4c,0x7,0xb,0x0,0x1,0x16,0x5e,0x4d,0x44,0xc,0x5,0x17,0x44,0xa,0xb,0x10,0x44,0x6,0x1,0x1,0xa,0x44,0xd,0x9,0x14,0x8,0x1,0x9,0x1,0xa,0x10,0x1,0x0]

private func nameMake(user num: UInt8) -> UInt8 {
    return num ^ 100
}

/*: "Voice greeting" :*/
fileprivate let str_replyData:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t"]
fileprivate let str_topText:String = "infog"

/*: "Add" :*/
fileprivate let str_modelHiddenTitleData:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let str_socialValue:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let str_upData:String = "addStmake load add tool"

/*: "list" :*/
fileprivate let str_equalTopValue:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let str_addWithHisName:String = "acountdio"

/*: "length" :*/
fileprivate let str_bagName:[Character] = ["l","e","n","g","t"]
fileprivate let str_actionLineValue:String = "H"

/*: "url" :*/
fileprivate let str_selectedData:String = "namerl"

/*: "Successfully added, please wait for approval" :*/
fileprivate let str_inputName:[UInt8] = [0x2e,0x50,0x3e,0x3e,0x40,0x4e,0x4e,0x41,0x50,0x47,0x47,0x54,0xfb,0x3c,0x3f,0x3f,0x40,0x3f,0x7,0xfb,0x4b,0x47,0x40,0x3c,0x4e,0x40,0xfb,0x52,0x3c,0x44,0x4f,0xfb,0x41,0x4a,0x4d,0xfb,0x3c,0x4b,0x4b,0x4d,0x4a,0x51,0x3c,0x47]

fileprivate func bindVideo(to num: UInt8) -> UInt8 {
    let value = Int(num) + 37
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Are you sure to delete this message？" :*/
fileprivate let str_matchData:[UInt8] = [0xbb,0x88,0x9f,0xda,0x83,0x95,0x8f,0xda,0x89,0x8f,0x88,0x9f,0xda,0x8e,0x95,0xda,0x9e,0x9f,0x96,0x9f,0x8e,0x9f,0xda,0x8e,0x92,0x93,0x89,0xda,0x97,0x9f,0x89,0x89,0x9b,0x9d,0x9f,0x15,0x46,0x65]

private func cropGift(object num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "Cancel" :*/
fileprivate let str_photoValue:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let str_equalValue:[Character] = ["D","e","l","e","t"]
fileprivate let str_textData:[Character] = ["e"]

/*: "id" :*/
fileprivate let str_liveName:[Character] = ["i","d"]

/*: "Please wait for the audit result" :*/
fileprivate let str_miniTitle:[UInt8] = [0xe3,0xdf,0xd6,0xd2,0xc0,0xd6,0x93,0xc4,0xd2,0xda,0xc7,0x93,0xd5,0xdc,0xc1,0x93,0xc7,0xdb,0xd6,0x93,0xd2,0xc6,0xd7,0xda,0xc7,0x93,0xc1,0xd6,0xc0,0xc6,0xdf,0xc7]

private func labelName(top num: UInt8) -> UInt8 {
    return num ^ 179
}

/*: "The upper limit has been reached" :*/
fileprivate let str_filterTitle:[UInt8] = [0x7c,0x40,0x4d,0x8,0x5d,0x58,0x58,0x4d,0x5a,0x8,0x44,0x41,0x45,0x41,0x5c,0x8,0x40,0x49,0x5b,0x8,0x4a,0x4d,0x4d,0x46,0x8,0x5a,0x4d,0x49,0x4b,0x40,0x4d,0x4c]

private func targetPush(error num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "filePath" :*/
fileprivate let str_hiddenData:String = "fimodel"
fileprivate let str_insideTitle:String = "data dismiss color letePath"

/*: "time" :*/
fileprivate let str_equalContent:String = "tviewe"

/*: "AudioCell" :*/
fileprivate let str_teachingName:[Character] = ["T","a","l","k","i"]
fileprivate let str_collectTitle:String = "N"
fileprivate let str_timeVideoData:String = "gGreview label model bottom transition"
fileprivate let str_destroyData:String = "s self language ifioCell"

/*: "#FF506D" :*/
fileprivate let str_soundTitle:String = "text"
fileprivate let str_toTitle:String = "name506D"

/*: "btn_message_delete_nor" :*/
fileprivate let str_valueLimitContent:[Character] = ["b","t","n","_","m","e","s"]
fileprivate let str_listValue:String = "random stringsage_del"
fileprivate let str_needData:String = "price view self error cellete_nor"

/*: "Currently mute" :*/
fileprivate let str_interestedData:[Character] = ["C","u","r","r","e","n","t","l","y"," ","m","u","t"]
fileprivate let str_languageData:[Character] = ["e"]

/*: "Edit Title" :*/
fileprivate let str_popName:String = "color main let key modelEdi"
fileprivate let str_whiteData:[Character] = ["t"," ","T","i","t","l","e"]

/*: "OK" :*/
fileprivate let str_giftValue:String = "location"

/*: "remark" :*/
fileprivate let str_sendValue:String = "relistark"

/*: "Click the button below to add\ncontent" :*/
fileprivate let str_commentTitle:[UInt8] = [0x10,0x39,0x36,0x30,0x38,0xed,0x41,0x35,0x32,0xed,0x2f,0x42,0x41,0x41,0x3c,0x3b,0xed,0x2f,0x32,0x39,0x3c,0x44,0xed,0x41,0x3c,0xed,0x2e,0x31,0x31,0xd7,0x30,0x3c,0x3b,0x41,0x32,0x3b,0x41]

fileprivate func makeTo(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 205
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let str_markData:[UInt8] = [0x32,0x17,0x17,0x53,0x1e,0x6,0x1f,0x7,0x1a,0x3,0x1f,0x16,0x53,0x5,0x1c,0x1a,0x10,0x16,0x53,0x14,0x1,0x16,0x16,0x7,0x1a,0x1d,0x14,0x0,0x5f,0x53,0x12,0x1d,0x17,0x53,0x7,0x1b,0x16,0x53,0x0,0xa,0x0,0x7,0x16,0x1e,0x53,0x4,0x1a,0x1f,0x1f,0x53,0x0,0x16,0x1d,0x17,0x53,0x5,0x1c,0x1a,0x10,0x16,0x53,0x1e,0x16,0x0,0x0,0x12,0x14,0x16,0x0,0x53,0x11,0xa,0x53,0x7,0x6,0x1,0x1d,0x0,0x48,0x79,0x30,0x1f,0x1a,0x10,0x18,0x53,0x7,0x1b,0x16,0x53,0x11,0x6,0x11,0x11,0x1f,0x16,0x53,0x7,0x1c,0x53,0x3,0x1f,0x12,0xa,0x53,0x7,0x1b,0x16,0x53,0x5,0x1c,0x1a,0x10,0x16,0x5f,0x53,0x0,0x1f,0x1a,0x17,0x16,0x53,0x7,0x1c,0x53,0x7,0x1b,0x16,0x53,0x5,0x1c,0x1a,0x10,0x16,0x53,0x12,0x1,0x16,0x12,0x53,0x7,0x1c,0x53,0x17,0x16,0x1f,0x16,0x7,0x16,0x53,0x1a,0x7,0x5f,0x53,0x12,0x1d,0x17,0x53,0x7,0x1b,0x16,0x53,0x5,0x1c,0x1a,0x10,0x16,0x53,0x7,0x1a,0x7,0x1f,0x16,0x53,0x10,0x12,0x1d,0x53,0x11,0x16,0x53,0x16,0x17,0x1a,0x7,0x16,0x17,0x5d]

private func serviceBirthData(equal num: UInt8) -> UInt8 {
    return num ^ 115
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AudioToolDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class AudioToolDelegate: MusicViewController {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: LogThen?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: AreaReactiveCompatible?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: AudioCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_viewContent.map{nameMake(user: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(str_replyData) + str_topText.replacingOccurrences(of: "info", with: "in")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.modelCard()
        //: designView()
        putThrough()
        //: getAudioListData()
        audioTarget()
        //: setMutedDetection()
        fileCan()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(selectBlock(notification:)), name: kLet_tabContent, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(change(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(line),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: kLet_tabContent,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.contrive()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ImageMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [ImageMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: kLet_environmentText - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(str_modelHiddenTitleData)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.conversationSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.distancePlay(colors: UIColor.memoryImageAwake(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fromText), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension AudioToolDelegate {
    //: func getAudioListData() {
    func audioTarget() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_socialValue))] = 1
        //: ProgressHUD.show()
        AtAdView.sizeUp()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.table(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            AtAdView.toAction()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.ticket()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(str_upData.prefix(5)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(str_equalTopValue))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ImageMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ImageMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func uploadTime(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(str_socialValue))] = 1
        //: dict["audio"] = data
        dict[(str_addWithHisName.replacingOccurrences(of: "count", with: "u"))] = data
        //: dict["length"] = Int(time)
        dict[(String(str_bagName) + str_actionLineValue.lowercased())] = Int(time)

        //: ProgressHUD.show()
        AtAdView.sizeUp()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        AnteriorNarisRequestTool.makeEqual(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            AtAdView.toAction()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(str_selectedData.replacingOccurrences(of: "name", with: "u"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.sizeExecuteMsg(showMsg: String(bytes: str_inputName.map{bindVideo(to: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.audioTarget()
                //: self.recordView?.dismiss()
                self.recordView?.ingredient()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func selectedIndex(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ReloadAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.conversationSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        AlongThen.jumpTime(message: String(bytes: str_matchData.map{cropGift(object: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(str_photoValue)).localized, rightBtnTitle: (String(str_equalValue) + String(str_textData)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            AlongThen.teachingMethod()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            AlongThen.teachingMethod()
            //: self.stopPlay()
            self.line()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ImageMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            AnteriorNarisRequestTool.rankCompletion(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.audioTarget()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension AudioToolDelegate {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func selectBlock(notification _: NSNotification) {
        //: stopPlay()
        line()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func change(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            line()
        }
    }

    //: @objc func addButtonClick() {
    @objc func fromText() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard NameSocketManager.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailKey(showMsg: kLet_networkViewTitle)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ColorPermissionTool.withDeadlineAt(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.detailKey(showMsg: kLet_intimateText)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.detailKey(showMsg: String(bytes: str_miniTitle.map{labelName(top: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.detailKey(showMsg: String(bytes: str_filterTitle.map{targetPush(error: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.line()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = LogThen(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.observer()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(str_hiddenData.replacingOccurrences(of: "model", with: "l") + String(str_insideTitle.suffix(5)))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.uploadTime(data: data!, time: dict[(str_equalContent.replacingOccurrences(of: "view", with: "im"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension AudioToolDelegate {
    //: func setMutedDetection() {
    func fileCan() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        MuteReactiveCompatible.shared.scale()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        MuteReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension AudioToolDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(str_teachingName) + str_collectTitle.lowercased() + String(str_timeVideoData.prefix(4)) + "etAud" + String(str_destroyData.suffix(6)))
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: AudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! AudioCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.viewPush(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.singleTo(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.singleTo(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            MuteReactiveCompatible.shared.scale()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.cradle(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.line()
            //: self.editTitle(index: indexPath.section)
            self.nomDeGuerre(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(str_equalValue) + String(str_textData)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.selectedIndex(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (str_soundTitle.replacingOccurrences(of: "text", with: "#") + str_toTitle.replacingOccurrences(of: "name", with: "FF")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.selectedIndex(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.withView(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.withView(name: (String(str_valueLimitContent) + String(str_listValue.suffix(8)) + String(str_needData.suffix(7)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = WithoutImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func cradle(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isTalking else {
        guard !NameSocketManager.shared.isTalking else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.detailKey(showMsg: kLet_networkViewTitle)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.contrive()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? AudioCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.singleTo(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.detailKey(showMsg: (String(str_interestedData) + String(str_languageData)).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = AreaReactiveCompatible.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.contrive()
        //: self.player?.play(urlStr: model.content)
        self.player?.listFinish(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? AudioCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.singleTo(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func nomDeGuerre(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = SaveReactiveCompatible(title: (String(str_popName.suffix(3)) + String(str_whiteData)).localized, leftBtnTitle: (String(str_photoValue)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.orientation()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            AtAdView.sizeUp()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: ImageMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(str_sendValue.replacingOccurrences(of: "list", with: "m"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            AnteriorNarisRequestTool.save(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                AtAdView.toAction()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func line() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.contrive()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.singleTo(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension AudioToolDelegate: ImageToolDelegate {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func user(status: BastardySignedNumeric) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            line()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            line()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension AudioToolDelegate {
    //: private func setupEmptyDataView() {
    private func ticket() {
        //: var style = EmptyStyle()
        var style = ImageEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: str_commentTitle.map{makeTo(gift: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .modelCard()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func putThrough() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(AudioCell.self, forCellReuseIdentifier: (String(str_teachingName) + str_collectTitle.lowercased() + String(str_timeVideoData.prefix(4)) + "etAud" + String(str_destroyData.suffix(6))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kLet_scaleName, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.touchColor()
        label.textColor = UIColor.touchColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.rugular(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: str_markData.map{serviceBirthData(equal: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
