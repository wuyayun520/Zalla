
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let str_colorData:[UInt8] = [0xc6,0xd7,0xd7,0xcb,0xc2,0xd8,0xce,0xc6,0xd7,0xd8,0xd7,0xc6,0xde,0xd8,0xd4,0xd2,0xc4,0xc4,0xc2,0xc2,0xc3,0xd8,0xc9,0xc8,0xd3,0xce,0xc1,0xce,0xc4,0xc6,0xd3,0xce,0xc8,0xc9]

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let str_toValue:[UInt8] = [0x85,0x94,0x94,0x88,0x81,0x9b,0x8d,0x85,0x94,0x9b,0x97,0x91,0x86,0x97,0x87,0x96,0x8d,0x86,0x81,0x9b,0x97,0x91,0x87,0x87,0x81,0x81,0x80,0x9b,0x8a,0x8b,0x90,0x8d,0x82,0x8d,0x87,0x85,0x90,0x8d,0x8b,0x8a]

private func blockListAdd(prompt num: UInt8) -> UInt8 {
    return num ^ 196
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let str_awakeData:String = "mf/recfalse equal view content"
fileprivate let str_makeName:String = "equal var view videoe/cr"
fileprivate let str_listSkinText:String = "eateApview self"

/*: "productId" :*/
fileprivate let str_styleDismissPlayName:[Character] = ["p","r","o"]
fileprivate let str_textRowData:String = "position count var enter andductId"

/*: "source" :*/
fileprivate let str_atName:[Character] = ["s","o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let str_itemText:String = "IAP充值创\u{5efa}订"
fileprivate let str_viewMenuTitleData:[Character] = ["单","\u{5931}","败",":"]

/*: , 参数: :*/
fileprivate let str_normalTitle:String = ", 参of:"

/*: ." :*/
fileprivate let str_loadMatchContent:String = "."

/*: "orderNum" :*/
fileprivate let str_minOpenContent:[Character] = ["o","r","d"]
fileprivate let str_contentName:String = "erNumof image color return inside"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let str_makeData:[Character] = ["m","f","/","r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a"]
fileprivate let str_styleText:String = "i text typeyNotify"

/*: "IAP充值校验失败: :*/
fileprivate let str_numberValue:[Character] = ["I","A","P","充","值","\u{6821}","验","失","败",":"]

/*: "transactionId" :*/
fileprivate let str_infoValue:String = "tramakea"
fileprivate let str_equalTitle:[Character] = ["c","t","i","o","n","I","d"]

/*: "reportMoney" :*/
fileprivate let str_pathData:[Character] = ["r","e","p","o","r","t","M","o","n","e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let str_rangeName:String = "error as else more monthmf/Au"
fileprivate let str_backgroundTitleText:String = "toSub/Apptime let type"
fileprivate let str_editText:String = "empty let mode userleCre"

/*: "IAP订阅创建订单失败: :*/
fileprivate let str_ofData:[Character] = ["I","A","P","订","阅"]
fileprivate let str_viewValue:[Character] = ["创"]
fileprivate let str_pingTitle:String = "建订单\u{5931}败:"

/*: "orderId" :*/
fileprivate let str_statusContent:[UInt8] = [0x92,0x8f,0x99,0x98,0x8f,0xb4,0x99]

private func deleteYou(mode num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let str_endName:String = "mf/Aucase comment var user"
fileprivate let str_popContent:String = "attention make to to letb/App"
fileprivate let str_touchTitle:String = "uyouess"

/*: "IAP订阅校验失败: :*/
fileprivate let str_containerContent:String = "top self text make labIAP订阅校"
fileprivate let str_infoTitle:String = "验失\u{8d25}:"

/*: "App" :*/
fileprivate let str_contentMakeName:String = "Appsection size"

/*: "OrderTransactionInfo_ :*/
fileprivate let str_upViewValue:String = "load return my selfOrderT"
fileprivate let str_finishDisplayTitle:String = "ctionInblock voice make"
fileprivate let str_iconData:String = "fo_view let self view"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let str_tabValue:[UInt8] = [0x90,0xad,0xbb,0xba,0xad,0x8b,0xad,0xbe,0xb1,0xac,0xbe,0xbc,0xab,0xb6,0xb0,0xb1,0x96,0xb1,0xb9,0xb0,0x80,0x8c,0xaa,0xbd,0xac,0xbc,0xad,0xb6,0xbd,0xba,0x80]

/*: "verifyData" :*/
fileprivate let str_handleContent:[UInt8] = [0x8f,0x9c,0x8b,0x90,0x9f,0x80,0xbd,0x98,0x8d,0x98]

private func targetInside(tap num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let str_awakeIconValue:[Character] = ["I","A","P","查","\u{8be2}","内"]
fileprivate let str_bottomTitle:[Character] = ["购","商","品","失","\u{8d25}",","," ","p"]
fileprivate let str_numberRenderTitle:String = "rolab"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let str_modelData:String = "extension self inIAP查"
fileprivate let str_logData:String = "\u{8d25}, pro"
fileprivate let str_topViewValue:String = ".couif let today size mask"

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let str_playerBackText:[Character] = ["I","A","P","\u{67e5}","询","\u{5185}","购","商","品","失","\u{8d25}",","," ","E","r","r","o","r"]
fileprivate let str_toFillValue:String = "cell"

/*: "IAP-purchased: :*/
fileprivate let str_selectMakeData:[Character] = ["I","A","P","-","p"]
fileprivate let str_cellName:String = "ulayer"
fileprivate let str_equalText:String = "chased:connection to empty extension return"

/*: , 订单号: :*/
fileprivate let str_bottomValue:String = ", 订单号:"

/*: , 商品Id: :*/
fileprivate let str_pairText:[Character] = [","," ","商","\u{54c1}","I","d",":"]

/*: , 苹果transId: :*/
fileprivate let str_cellData:String = ","
fileprivate let str_onlyValue:String = " 苹果tdismiss on action error back"

/*: "IAP-failed: :*/
fileprivate let str_makeRecommendData:[Character] = ["I","A","P","-","f","a","i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let str_statusData:[Character] = ["I","A","P","-","d","e","f","e","r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let str_methodImageLetData:String = "IAP未知touch view ease error else"
fileprivate let str_toTitle:[Character] = ["交","易","类","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let str_positionName:String = " 未知"
fileprivate let str_giftText:String = "的交易类型"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let str_sizeValue:String = "IAP\u{83b7}取"
fileprivate let str_upName:String = "据失败, "
fileprivate let str_addValue:String = "I"
fileprivate let str_modeData:[Character] = ["o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let str_labelName:String = ", ordgesture request super"

/*: , payType: :*/
fileprivate let str_imageViewData:String = "message raw make, pa"
fileprivate let str_appEndingName:String = "yType:height number value height"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NameLikeTransactionObserver.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kLet_bottomSelectName = NSNotification.Name(rawValue: String(bytes: str_colorData.map{$0^135}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let kLet_buildTitle = NSNotification.Name(rawValue: String(bytes: str_toValue.map{blockListAdd(prompt: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let kLet_tabRateValue = 9

/// 支付类型
//: enum ApplePayType {
enum MixedType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum StatusMirrorPath: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double) -> Void
typealias IAPcompletionHandle = (StatusMirrorPath, Double, MixedType) -> Void

//: class AppleIAPManager: NSObject {
class NameLikeTransactionObserver: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: MixedType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = NameLikeTransactionObserver()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(clickView),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func clickView() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension NameLikeTransactionObserver {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func conversationHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = IndexRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(str_awakeData.prefix(6)) + "harg" + String(str_makeName.suffix(4)) + String(str_listSkinText.prefix(6)) + "plePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_styleDismissPlayName) + String(str_textRowData.suffix(6)))] = productId
        //: dict["source"] = source
        dict[(String(str_atName))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_endVersionTitle.getStarted(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                EnableReactiveCompatible.nameBy(msg: (str_itemText + String(str_viewMenuTitleData)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(str_minOpenContent) + String(str_contentName.prefix(5)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func activityAdd(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = IndexRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(str_makeData) + String(str_styleText.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_endVersionTitle.getStarted(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    EnableReactiveCompatible.nameBy(msg: (String(str_numberValue)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.messageName(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.earn()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_bottomSelectName, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(str_pathData))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension NameLikeTransactionObserver {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func noneRetailStore(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = IndexRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(str_rangeName.suffix(5)) + String(str_backgroundTitleText.prefix(9)) + String(str_editText.suffix(5)) + "ateOrder")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_styleDismissPlayName) + String(str_textRowData.suffix(6)))] = productId
        //: dict["source"] = source
        dict[(String(str_atName))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_endVersionTitle.getStarted(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                EnableReactiveCompatible.nameBy(msg: (String(str_ofData) + String(str_viewValue) + str_pingTitle) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: str_statusContent.map{deleteYou(mode: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func sendWill(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = IndexRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(str_endName.prefix(5)) + "toSu" + String(str_popContent.suffix(5)) + "lePayS" + str_touchTitle.replacingOccurrences(of: "you", with: "cc"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        kLet_endVersionTitle.getStarted(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    EnableReactiveCompatible.nameBy(msg: (String(str_containerContent.suffix(6)) + str_infoTitle) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.messageName(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.noNext()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLet_buildTitle, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(String(str_pathData))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension NameLikeTransactionObserver {
    /// 初始化数据
    //: private func iap_initData() {
    private func designPath() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = fee(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = fee(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func fee(payType: MixedType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = earn()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = noNext()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func earn() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_contentMakeName.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_timeContent.string(forKey: kLet_userGiftHeightTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(str_upViewValue.suffix(6)) + "ransa" + String(str_finishDisplayTitle.prefix(7)) + String(str_iconData.prefix(3))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func noNext() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(str_contentMakeName.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = kLet_timeContent.string(forKey: kLet_userGiftHeightTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: str_tabValue.map{$0^223}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func exist(_ transactionId: String, _ payType: MixedType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: str_handleContent.map{targetInside(tap: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: str_handleContent.map{targetInside(tap: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension NameLikeTransactionObserver {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func withTransactions() {
        //: iap_initData()
        designPath()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            imageRetry(dict[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            imageRetry(dict[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func imageRetry(_ transactionId: String?, _ payType: MixedType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        messageName(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension NameLikeTransactionObserver {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func above(productId: String, payType: MixedType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        designPath()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            conversationHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.makeRequest(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            noneRetailStore(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.makeRequest(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func makeRequest(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            EnableReactiveCompatible.nameBy(msg: (String(str_awakeIconValue) + String(str_bottomTitle) + str_numberRenderTitle.replacingOccurrences(of: "lab", with: "du") + "ctId:") + "\(productId).")
            //: self.completionHandle?(.notArrow, 0)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.nameQuit()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func nameQuit() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension NameLikeTransactionObserver: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            EnableReactiveCompatible.nameBy(msg: (String(str_modelData.suffix(4)) + "询内购商\u{54c1}失" + str_logData + "ducts" + String(str_topViewValue.prefix(4)) + "nt为0."))
            //: self.completionHandle?( .noProductId, 0)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        EnableReactiveCompatible.nameBy(msg: (String(str_playerBackText) + str_toFillValue.replacingOccurrences(of: "cell", with: ":")) + "\(error).")
        //: self.completionHandle?( .noProductId, 0)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension NameLikeTransactionObserver: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    EnableReactiveCompatible.nameBy(msg: (String(str_selectMakeData) + str_cellName.replacingOccurrences(of: "layer", with: "r") + String(str_equalText.prefix(7))) + "\(currentPayType)" + (str_bottomValue.capitalized) + "\(String(describing: createOrderId))" + (String(str_pairText)) + "\(transaction.payment.productIdentifier)" + (str_cellData.capitalized + String(str_onlyValue.prefix(4)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    messageName(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EnableReactiveCompatible.nameBy(msg: (String(str_makeRecommendData)) + "\(currentPayType)" + (str_bottomValue.capitalized) + "\(String(describing: createOrderId))" + (String(str_pairText)) + "\(transaction.payment.productIdentifier)" + (str_cellData.capitalized + String(str_onlyValue.prefix(4)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EnableReactiveCompatible.nameBy(msg: (String(str_statusData)) + "\(currentPayType)" + (str_bottomValue.capitalized) + "\(String(describing: createOrderId))" + (String(str_pairText)) + "\(transaction.payment.productIdentifier)" + (str_cellData.capitalized + String(str_onlyValue.prefix(4)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EnableReactiveCompatible.nameBy(msg: (String(str_methodImageLetData.prefix(5)) + String(str_toTitle)) + "\(currentPayType)" + (str_bottomValue.capitalized) + "\(String(describing: createOrderId))" + (String(str_pairText)) + "\(transaction.payment.productIdentifier)" + (str_cellData.capitalized + String(str_onlyValue.prefix(4)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((str_positionName.capitalized + str_giftText.capitalized))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func messageName(_ transactionId: String, _ payType: MixedType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = exist(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            EnableReactiveCompatible.nameBy(msg: (str_sizeValue + "本\u{5730}\u{6536}据数" + str_upName.capitalized + "transact" + str_addValue.lowercased() + String(str_modeData)) + "\(transactionId)" + (String(str_labelName.prefix(5)) + "erId:") + "\(String(describing: createOrderId))" + (String(str_imageViewData.suffix(4)) + String(str_appEndingName.prefix(6))) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId || $0[String(bytes: str_statusContent.map{deleteYou(mode: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_statusContent.map{deleteYou(mode: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_handleContent.map{targetInside(tap: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.earn()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId || $0[String(bytes: str_statusContent.map{deleteYou(mode: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: str_statusContent.map{deleteYou(mode: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_handleContent.map{targetInside(tap: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.noNext()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > kLet_tabRateValue {
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            activityAdd(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(str_infoValue.replacingOccurrences(of: "make", with: "ns") + String(str_equalTitle))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            sendWill(transactionId, params: paramsArr.first!)
        }
    }
}
