
//: Declare String Begin

/*: / :*/
fileprivate let str_equalText:String = "/"

/*:  ( :*/
fileprivate let str_postText:String = "app("

/*: ; build: :*/
fileprivate let str_pictureEventData:String = "progress equal false input; build:"

/*: ; iOS  :*/
fileprivate let str_startData:[Character] = [";"," ","i","O","S"," "]

/*: ;  :*/
fileprivate let str_indexCenterData:String = "domain "

/*: )" :*/
fileprivate let str_leadingData:[Character] = [")"]

/*: "audio" :*/
fileprivate let str_voiceInfoName:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let str_coverValue:String = "show"

/*: .wav" :*/
fileprivate let str_managerTitle:String = "message scale.wav"

/*: "audio/wav" :*/
fileprivate let str_makePicAppearText:[Character] = ["a","u","d","i","o","/","w","a"]
fileprivate let str_currentValue:String = "top"

/*: .jpg" :*/
fileprivate let str_profileValue:String = ".jpgstring point"

/*: "image/jpeg" :*/
fileprivate let str_tentName:String = "color return view normal userimage/j"
fileprivate let str_groupTitle:String = "pequalg"

/*: "://app." :*/
fileprivate let str_coverData:String = "://app.number app let indicator"

/*: "data" :*/
fileprivate let str_selectedData:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let str_picName:[Character] = [":","n","u","l","l"]

/*: ":{}" :*/
fileprivate let str_addValue:[Character] = [":","{","}"]

/*: "Request failed, Try again later" :*/
fileprivate let str_equalName:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x54,0x20,0x2c,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x74,0x73,0x65,0x75,0x71,0x65,0x52]

/*: "json error" :*/
fileprivate let str_makeTitle:String = "json ecenter view info self data"
fileprivate let str_colorVoiceText:String = "itemitemoitem"

/*: "platform=iphone&version= :*/
fileprivate let str_cellValue:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&"]
fileprivate let str_goingTitle:String = "self var makeversion="

/*: &packageId= :*/
fileprivate let str_backgroundData:String = "show count&pack"
fileprivate let str_iconFromValue:[Character] = ["="]

/*: &bundleId= :*/
fileprivate let str_maxData:String = "&bundlmodel share use"
fileprivate let str_atName:String = "from starteId="

/*: &lang= :*/
fileprivate let str_ofData:String = "&lang=center type cookie"

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let str_locationData:String = "?!@#"
fileprivate let str_dataName:[Character] = ["*","+",",",":",";","=","\'"]

/*: "`<>()[]{}/\\| " :*/
fileprivate let str_timeRowName:String = "`<>(over["

/*: "/route/slowindex" :*/
fileprivate let str_topData:String = "color type to draft/route/sl"
fileprivate let str_nativeValue:String = "oempty"
fileprivate let str_thenName:[Character] = ["i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let str_moreClearData:[Character] = ["/","r","o","u","t","e","/","i","n","d","e"]
fileprivate let str_conversationName:String = "name"

/*: "?path= :*/
fileprivate let str_makeData:String = "tip top white super?path="

/*: "&GJNonceStr= :*/
fileprivate let str_contentText:[Character] = ["&","G","J","N","o","n","c","e","S","t","r","="]

/*: "& :*/
fileprivate let str_inputData:String = "device"

/*: = :*/
fileprivate let str_makeValue:String = "="

/*: "&params= :*/
fileprivate let str_starTitle:[Character] = ["&","p","a","r","a","m","s","="]

/*: "/ :*/
fileprivate let str_equalTitle:[Character] = ["/"]

/*: "? :*/
fileprivate let str_frameContent:String = "?"

/*: "token" :*/
fileprivate let str_succeedValue:String = "tokonn"

/*: "请求成功 :*/
fileprivate let str_toValue:[Character] = ["请","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let str_targetName:String = "photophotophotophotophotophotophotophoto"

/*: "AA" :*/
fileprivate let str_okCreateRefreshData:String = "aa"

/*: "BB" :*/
fileprivate let str_progressValue:String = "size"

/*: "下载完成 -  :*/
fileprivate let str_localData:String = "下载完成 - "

/*: "/Documents/" :*/
fileprivate let str_scaleName:String = "/Docequal me wrap"

/*: ".mp4" :*/
fileprivate let str_equalLayerData:String = ".mp4click let return count"

/*: "移动地址: :*/
fileprivate let str_arrayName:[Character] = ["移","\u{52a8}","\u{5730}","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let str_devicePriceTitle:[Character] = ["后","台","任","务","\u{4e0b}","载","回","来"]

/*: "下载成功 : :*/
fileprivate let str_withText:String = "下kind成功 :"

/*: "Net Error" :*/
fileprivate let str_bagName:[Character] = ["N","e","t"," ","E","r","r","o"]
fileprivate let str_bottomName:[Character] = ["r"]

/*: "下载失败 : :*/
fileprivate let str_modelValue:String = "\u{4e0b}载失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StraddleSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let kLet_storageStatusContent = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let kLet_succeedName = "\(kLet_maxProgressData)/\(kLet_lengthName) (\(kLet_clickReplaceHeightValue)" + (String(str_pictureEventData.suffix(8))) + "\(kLet_productionUseData)" + (String(str_startData)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ScaleErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let kLet_endVersionTitle = StraddleSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class StraddleSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func getStarted(model: IndexRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.dataObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.doFinishCompletion(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.completion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func doFinishCompletion(model: IndexRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.counterperson(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.messageBar(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftFailure(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: kLet_lengthFailTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailKey(showMsg: kLet_lengthFailTitle)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(str_voiceInfoName)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.willStamp())+\(kLet_voiceValue)" + (String(str_managerTitle.suffix(4))), mimeType: (String(str_makePicAppearText) + str_currentValue.replacingOccurrences(of: "top", with: "v")))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(str_profileValue.prefix(4))), mimeType: (String(str_tentName.suffix(7)) + str_groupTitle.replacingOccurrences(of: "equal", with: "e")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftFailure(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: kLet_lengthFailTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailKey(showMsg: kLet_lengthFailTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func completion(model: IndexRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.counterperson(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.messageBar(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: kLet_firstName ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_storageStatusContent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftFailure(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: kLet_lengthFailTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailKey(showMsg: kLet_lengthFailTitle)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = kLet_storageStatusContent }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftFailure(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: kLet_lengthFailTitle))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        detailKey(showMsg: kLet_lengthFailTitle)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func giftFailure(model: IndexRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            GazetteThen.share.concatenate(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if kLet_firstName, model.requestServer.contains((String(str_coverData.prefix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.resumeView(key: GazetteThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(str_selectedData)) + "\"" + (String(str_picName)), with: "" + "\"" + (String(str_selectedData)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !kLet_firstName {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(str_picName)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<ResponseModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ColorStrideable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ScaleErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    detailKey(showMsg: responseModel.msg ?? String(bytes: str_equalName.reversed(), encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ColorStrideable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: kLet_screenPathName, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: (String(str_makeTitle.prefix(6)) + str_colorVoiceText.replacingOccurrences(of: "item", with: "r")).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func counterperson(model: IndexRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(str_cellValue) + String(str_goingTitle.suffix(8))) + "\(kLet_licenseRequestDoingTitle)" + (String(str_backgroundData.suffix(5)) + "ageId" + String(str_iconFromValue)) + "\(kLet_voiceValue)" + (String(str_maxData.prefix(6)) + String(str_atName.suffix(4))) + "\(kLet_clickReplaceHeightValue)" + (String(str_ofData.prefix(6))) + "\(GazetteThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if kLet_firstName, serverUrl.contains((String(str_coverData.prefix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if GazetteThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_topData.suffix(9)) + str_nativeValue.replacingOccurrences(of: "empty", with: "w") + String(str_thenName)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_moreClearData) + str_conversationName.replacingOccurrences(of: "name", with: "x"))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(str_makeData.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_contentText)) + "\(GazetteThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.takeDown(key: GazetteThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(str_starTitle)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func messageBar(model: IndexRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(kLet_succeedName)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (str_succeedValue.replacingOccurrences(of: "on", with: "e")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func listenerTo(model: IndexRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.counterperson(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(str_toValue)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (str_targetName.replacingOccurrences(of: "photo", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (str_localData.capitalized) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_scaleName.prefix(4)) + "uments/") + NSDate.willStamp() + (String(str_equalLayerData.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(str_arrayName)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(str_devicePriceTitle)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = CellDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func fresh(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: kLet_localContent.showUponCompute(), with: kLet_barRecordScreenText)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_withText.replacingOccurrences(of: "kind", with: "载")) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ScaleErrorResponse(errorCode: ColorStrideable.Normal.rawValue, errorMsg: (String(str_bagName) + String(str_bottomName)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: (String(str_bagName) + String(str_bottomName)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_modelValue) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func anyTask(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: kLet_localContent.showUponCompute(), with: kLet_barRecordScreenText)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (str_withText.replacingOccurrences(of: "kind", with: "载")) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ScaleErrorResponse(errorCode: ColorStrideable.Normal.rawValue, errorMsg: (String(str_bagName) + String(str_bottomName)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ScaleErrorResponse(errorCode: ColorStrideable.NetError.rawValue, errorMsg: (String(str_bagName) + String(str_bottomName)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (str_modelValue) + "\(errMsg)")
            }
        }
    }
}
