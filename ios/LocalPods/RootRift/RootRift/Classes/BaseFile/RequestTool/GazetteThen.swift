
//: Declare String Begin

/*: "http://" :*/
fileprivate let str_sizeButtonTitle:String = "text make selfhttp://"

/*: "https://" :*/
fileprivate let str_viewValue:String = "https:/message button let"
fileprivate let str_directValue:String = "/"

/*: "http:// :*/
fileprivate let str_tempValue:String = "height view color infohttp://"

/*: "serverSelctIndex =  :*/
fileprivate let str_backName:[Character] = ["s","e","r","v","e","r","S"]
fileprivate let str_mpTitle:[Character] = ["e","l","c","t","I","n","d"]
fileprivate let str_cellValue:String = "ex = voice return title"

/*: "serverUrlStr =  :*/
fileprivate let str_valueTitle:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," ","="," "]

/*: "app. :*/
fileprivate let str_withName:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let str_livePathValue:[Character] = [".","c","o","m"]

/*: "IM/getMyBlackList" :*/
fileprivate let str_createVoiceContent:String = "IM/gequal section size"
fileprivate let str_pointValue:String = "ackListgift raw"

/*: "mf/business/sendMsg" :*/
fileprivate let str_conversationContent:String = "contact add to supermf/bu"
fileprivate let str_pathName:String = "log table/sendMs"
fileprivate let str_spaceName:[Character] = ["g"]

/*: "mf/business/sendGift" :*/
fileprivate let str_managerValue:String = "mf/buthe last view self item"
fileprivate let str_indexInfoData:String = "originness"
fileprivate let str_thenValue:String = "gift request dataGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let str_giftCookieName:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let str_toolName:String = "image"
fileprivate let str_barCellData:String = "om/sshare let make cell seek"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let str_picTitle:[Character] = ["m","f","/","c","h","a","t","R","o","o"]
fileprivate let str_topData:[Character] = ["m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let str_buttonValue:String = "reccontentr"
fileprivate let str_commentValue:[Character] = ["p","p","l"]
fileprivate let str_actionContent:[Character] = ["e","P","a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let str_midName:[Character] = ["u","s","e","r","/","a","p","p","l","e","L","o","g","i","n"]

/*: "en" :*/
fileprivate let str_arrayName:String = "interactionn"

/*: "ar" :*/
fileprivate let str_fieldTitle:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let str_equalName:String = "cells"

/*: "pt" :*/
fileprivate let str_appText:String = "task"

/*: "GJ21FB" :*/
fileprivate let str_procedureText:String = "pointJ21FB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let str_loadValue:[Character] = ["完","成"]
fileprivate let str_byTitle:String = "了检测,请"

/*: "接口请求失败" :*/
fileprivate let str_sendPortionData:[Character] = ["接","口","请","求","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let str_lineName:[Character] = ["完","成","\u{4e86}","检","测",",","\u{9700}"]
fileprivate let str_timeTitle:String = "let top color要更换serv"
fileprivate let str_pathTitle:[Character] = ["e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let str_videoContent:String = "Hdatadata"
fileprivate let str_addData:[Character] = ["P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let str_kindViewMakeName:String = "__SCOindex add text user skin"

/*: "tap" :*/
fileprivate let str_hiddenContent:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let str_zoneBeanData:String = "tsaven"

/*: "ipsec" :*/
fileprivate let str_previousLabelViewData:String = "imessagesec"

/*: "ppp" :*/
fileprivate let str_dropData:String = "socialsocialsocial"

/*: "func__updateTalkingCookies =  :*/
fileprivate let str_weightData:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o","o","k"]
fileprivate let str_topValue:String = "ies = hidden block if mode view"

/*: "https:// :*/
fileprivate let str_viewResValue:String = "modeltps"
fileprivate let str_actionTitle:String = ":buttonbutton"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let str_rangeMakeTitle:[UInt8] = [0xed,0xfc,0xf5,0xea,0xe6,0xe6,0xeb,0xec,0xf3,0xec,0xfb,0xec,0xda,0xe8,0xf4,0xec,0xca,0xf6,0xf6,0xf2,0xf0,0xec,0xfa,0xde,0xf0,0xfb,0xef,0xdc,0xf9,0xf3,0xa7,0xc4,0xa7]

fileprivate func innerView(kit num: UInt8) -> UInt8 {
    let value = Int(num) - 135
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let str_keyName:String = "pullID"

/*: "PHPSESSID" :*/
fileprivate let str_upValue:String = "PHPnumberEnumbernumberID"

/*: "func__saveAllCookies =  :*/
fileprivate let str_modelData:String = "fufeedbackc"
fileprivate let str_contentLanguageValue:String = "white crop modellCoo"

/*: "func__saveAllCookies  error" :*/
fileprivate let str_stageValue:String = "func_return if"
fileprivate let str_deadlineName:String = "title model else textAllC"
fileprivate let str_frameTitle:String = "capability view color lineookies "
fileprivate let str_pointData:[Character] = [" ","e","r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let str_defineName:[Character] = ["🍪"," ","\u{5220}","\u{9664}"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GazetteThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum AssemblyPlantMirrorPath: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class GazetteThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = GazetteThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        blockError()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(str_sizeButtonTitle.suffix(7)))) || newValue.hasPrefix((String(str_viewValue.prefix(7)) + str_directValue.capitalized)) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if kLet_tableContent && !kLet_firstName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_tempValue.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(str_tempValue.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    concatenate(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func blockError() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = kLet_timeContent.integer(forKey: kLet_searchData)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(str_backName) + String(str_mpTitle) + String(str_cellValue.prefix(5))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = titleUrl()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_sizeButtonTitle.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_tableContent && !kLet_firstName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_sizeButtonTitle.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(str_valueTitle)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func titleUrl() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func reloading(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(str_withName)) + "\(kLet_barRecordScreenText)" + (String(str_livePathValue)), (String(str_withName)) + "\(kLet_barRecordScreenText)" + (String(str_livePathValue))]
    //: }()

    lazy var slowPathArr: Array = {
        return ["IM/getMyBlackList",
                "mf/business/sendMsg",
                "mf/business/sendGift",
                "mf/chatRoom/sendMsg",
                "mf/chatRoom/sendGift",
                "recharge/applePayNotify",
                "user/appleLogin"]
    }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = ListMacroDefine.getSystemLangCode()
        let lang = ListMacroDefine.upOf()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .iconTitle(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (str_procedureText.replacingOccurrences(of: "point", with: "G")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension GazetteThen {
    //: func checkServerUrl() {
    func restaurantAttendantOff() {
        //: if WatercourseThen.share.networkStatus != .Unavailable {
        if WatercourseThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            redEnter()
            //: } else {
        } else {
            //: func_starCodeTime()
            writeTime()
        }
    }

    //: func func_starCodeTime() {
    func writeTime() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.redEnter()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func exampleFinishTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func redEnter() {
        //: if WatercourseThen.share.networkStatus == .Unavailable {
        if WatercourseThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        exampleFinishTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ResultReactiveCompatible.nameAcross { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(str_loadValue) + str_byTitle.capitalized + "\u{6c42}状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(str_sendPortionData)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.writeTime()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(str_lineName) + String(str_timeTitle.suffix(7)) + String(str_pathTitle)))
                    //: self.func__changeHostUrl()
                    self.creature()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func creature() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        exampleFinishTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        kLet_timeContent.set(serverSelctIndex, forKey: kLet_searchData)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_sizeButtonTitle.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_tableContent, !kLet_firstName {
            //: serverPrefix = "http://"
            serverPrefix = (String(str_sizeButtonTitle.suffix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension GazetteThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func makeConstraint() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(str_videoContent.replacingOccurrences(of: "data", with: "T") + String(str_addData))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func frameGesture() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(str_kindViewMakeName.prefix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(str_hiddenContent))) || keyStr.contains((str_zoneBeanData.replacingOccurrences(of: "save", with: "u"))) || keyStr.contains((str_previousLabelViewData.replacingOccurrences(of: "message", with: "p"))) || keyStr.contains((str_dropData.replacingOccurrences(of: "social", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension GazetteThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func concatenate(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(str_weightData) + String(str_topValue.prefix(6))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = lightColored()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(str_sizeButtonTitle.suffix(7))), with: (String(str_viewValue.prefix(7)) + str_directValue.capitalized))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(str_viewValue.prefix(7)) + str_directValue.capitalized)) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (str_viewResValue.replacingOccurrences(of: "model", with: "ht") + str_actionTitle.replacingOccurrences(of: "button", with: "/")) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            thanDownResource(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        listEdit()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func thanDownResource(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: str_rangeMakeTitle.map{innerView(kit: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func lightColored() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(str_sizeButtonTitle.suffix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if kLet_tableContent, !kLet_firstName {
            //: serverPrefix = "https://"
            serverPrefix = (String(str_viewValue.prefix(7)) + str_directValue.capitalized)
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(str_withName)) + "\(kLet_barRecordScreenText)" + (String(str_livePathValue)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func pushView(note _: Notification) {
        //: if WatercourseThen.share.loginUid.isEmptyString {
        if WatercourseThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            phoneGift()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            anyKey()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func phoneGift() {
        //: var htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_keyName.replacingOccurrences(of: "pull", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_upValue.replacingOccurrences(of: "number", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_keyName.replacingOccurrences(of: "pull", with: "U")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_upValue.replacingOccurrences(of: "number", with: "S")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((str_keyName.replacingOccurrences(of: "pull", with: "U"))), h5Cookie.keys.contains((str_keyName.replacingOccurrences(of: "pull", with: "U"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((str_upValue.replacingOccurrences(of: "number", with: "S"))), h5Cookie.keys.contains((str_upValue.replacingOccurrences(of: "number", with: "S"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func anyKey() {
        //: let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = WatercourseThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == WatercourseThen.share.loginUserMode.userID {
            if cookie.name == (str_keyName.replacingOccurrences(of: "pull", with: "U")), cookie.value == WatercourseThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (str_upValue.replacingOccurrences(of: "number", with: "S")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (str_keyName.replacingOccurrences(of: "pull", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (str_upValue.replacingOccurrences(of: "number", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((str_keyName.replacingOccurrences(of: "pull", with: "U"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_keyName.replacingOccurrences(of: "pull", with: "U"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((str_upValue.replacingOccurrences(of: "number", with: "S"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(str_upValue.replacingOccurrences(of: "number", with: "S"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func listEdit() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (str_modelData.replacingOccurrences(of: "feedback", with: "n") + "__saveA" + String(str_contentLanguageValue.suffix(5)) + "kies = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            kLet_timeContent.set(cookieData, forKey: kLet_recordSuccessNetName)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(str_stageValue.prefix(5)) + "_save" + String(str_deadlineName.suffix(4)) + String(str_frameTitle.suffix(7)) + String(str_pointData)))
        }
    }

    //: func func__setAppCookies() {
    func managerCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = kLet_timeContent.object(forKey: kLet_recordSuccessNetName)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func filePushColor() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        kLet_timeContent.removeObject(forKey: kLet_recordSuccessNetName)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
